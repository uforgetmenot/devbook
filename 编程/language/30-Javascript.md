# JavaScript ES6 开发知识点

## 1. 变量声明
### 1.1 let 和 const
- let 块级作用域
- const 常量声明
- 变量提升差异

### 1.2 解构赋值
- 数组解构
- 对象解构
- 默认值设置

## 2. 函数增强
### 2.1 箭头函数
- 基本语法
- this 绑定规则
- 使用场景

### 2.2 默认参数
- 参数默认值
- 与解构结合使用

### 2.3 剩余参数
- rest 参数语法
- 与 arguments 区别

## 3. 模板字符串
### 3.1 基本用法
- 反引号语法
- 变量插值
- 多行字符串

### 3.2 标签模板
- 模板标签函数
- 原始字符串

## 4. 对象增强
### 4.1 属性简写
- 属性名简写
- 方法简写

### 4.2 计算属性名
- 动态属性名
- Symbol 作为属性名

## 5. 数组方法
### 5.1 新增方法
- Array.from()
- Array.of()
- find() 和 findIndex()
- includes()

### 5.2 迭代器方法
- keys()
- values()
- entries()

## 6. 类和继承
### 6.1 Class 语法
- 类声明
- 构造函数
- 实例方法

### 6.2 继承
- extends 关键字
- super 调用
- 静态方法

## 7. 模块系统
### 7.1 导入导出
- export 语法
- import 语法
- 默认导出

### 7.2 动态导入
- import() 函数
- 条件导入

## 8. Promise 和异步
### 8.1 Promise
- 基本用法
- then/catch/finally
- Promise.all/race/allSettled

### 8.2 async/await
- 异步函数声明
- await 表达式
- 错误处理

## 9. 迭代器和生成器
### 9.1 迭代器协议
- Symbol.iterator
- 自定义迭代器

### 9.2 生成器函数
- function* 语法
- yield 表达式
- 生成器对象

## 10. 新数据类型
### 10.1 Symbol
- 基本用法
- 全局 Symbol
- 内置 Symbol

### 10.2 Set 和 Map
- Set 集合
- Map 映射
- WeakSet 和 WeakMap

## 11. Proxy 和 Reflect
### 11.1 Proxy 代理
- 拦截器配置
- 常用 trap 方法

### 11.2 Reflect 反射
- 反射方法
- 与 Proxy 结合使用