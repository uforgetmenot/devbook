# 智能合约开发完全指南

> 面向 0-5 年经验的开发者，系统掌握智能合约开发的完整流程，从 Solidity 语法到生产部署。

---

## 学习者画像与目标

### 学习者背景
- **编程基础**：熟悉 JavaScript/TypeScript/Python
- **区块链认知**：了解区块链基本概念、交易流程
- **开发经验**：有 Web 开发经验，熟悉 Git、命令行
- **痛点**：不了解 Solidity 语法、不清楚如何部署合约、担心安全问题

### 学习终极目标
- **掌握 Solidity**：能够编写安全、高效的智能合约
- **理解 EVM**：深入了解以太坊虚拟机运行机制
- **开发流程**：熟练使用 Hardhat/Foundry 等开发工具
- **测试与审计**：编写完整的测试用例，识别常见漏洞
- **生产部署**：能够将合约部署到主网并维护

### 阶段性成果
- **阶段 1**：编写并部署第一个 HelloWorld 合约
- **阶段 2**：实现标准 ERC-20 Token 合约
- **阶段 3**：开发复杂的 DeFi 协议合约
- **阶段 4**：通过安全审计，部署到生产环境

---

## 知识结构总览

| 模块 | 核心主题 | 基础要点 | 实战聚焦 | 进阶方向 |
|------|----------|----------|----------|----------|
| 模块一 | Solidity 基础 | 语法、数据类型、函数 | HelloWorld 合约 | 高级特性、设计模式 |
| 模块二 | EVM 与 Gas | 虚拟机原理、Gas 机制 | Gas 优化技巧 | 深度优化、汇编 |
| 模块三 | Token 标准 | ERC-20、ERC-721、ERC-1155 | 发币与 NFT | 自定义扩展 |
| 模块四 | DeFi 合约 | DEX、借贷、Staking | Uniswap 克隆 | MEV、跨链 |
| 模块五 | 安全审计 | 常见漏洞、审计方法 | 实战审计案例 | 形式化验证 |
| 模块六 | 开发工具 | Hardhat、Foundry、Remix | 完整开发流程 | CI/CD、监控 |

---

## 学习路径总览

| 阶段 | 建议时长 | 学习目标 | 核心任务 | 成果验收 |
|------|----------|----------|----------|----------|
| 阶段 0：环境准备 | 1 天 | 搭建开发环境 | 安装工具、配置项目 | 成功编译第一个合约 |
| 阶段 1：Solidity 基础 | 1 周 | 掌握语法和基本概念 | 学习数据类型、函数、继承 | 编写 5+ 个练习合约 |
| 阶段 2：Token 开发 | 3-5 天 | 实现标准 Token | 开发 ERC-20、ERC-721 | 部署自己的 Token |
| 阶段 3：DeFi 实战 | 1-2 周 | 构建 DeFi 协议 | 实现 Swap、Staking | 完成 DeFi 项目 |
| 阶段 4：安全与测试 | 1 周 | 测试与安全加固 | 编写测试、审计代码 | 测试覆盖率 >80% |
| 阶段 5：生产部署 | 3-5 天 | 部署与维护 | 主网部署、监控 | 成功部署生产合约 |

---

## 模块一：Solidity 语言基础

### 1.1 环境搭建

#### 开发工具选择

**方案 1：Remix（推荐入门）**
- 优点：无需安装，在线 IDE，快速上手
- 缺点：功能有限，不适合大型项目
- 访问：https://remix.ethereum.org

**方案 2：Hardhat（推荐进阶）**
```bash
# 安装 Node.js（v16+）
# 创建项目
mkdir my-contract
cd my-contract
npm init -y

# 安装 Hardhat
npm install --save-dev hardhat

# 初始化 Hardhat 项目
npx hardhat init
# 选择 "Create a TypeScript project"

# 安装依赖
npm install --save-dev @nomicfoundation/hardhat-toolbox
npm install --save-dev @openzeppelin/contracts

# 项目结构
# contracts/     - 合约源代码
# test/          - 测试文件
# scripts/       - 部署脚本
# hardhat.config.ts - 配置文件
```

**方案 3：Foundry（推荐专业开发）**
```bash
# 安装 Foundry
curl -L https://foundry.paradigm.xyz | bash
foundryup

# 创建项目
forge init my-contract
cd my-contract

# 项目结构
# src/           - 合约源代码
# test/          - 测试文件（Solidity）
# script/        - 部署脚本
# foundry.toml   - 配置文件

# 安装依赖
forge install OpenZeppelin/openzeppelin-contracts

# 编译
forge build

# 测试
forge test
```

### 1.2 第一个合约：HelloWorld

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

/**
 * @title HelloWorld
 * @dev 最简单的智能合约示例
 */
contract HelloWorld {
    // 状态变量：存储在区块链上
    string public message;
    address public owner;
    uint256 public updateCount;

    // 事件：用于记录重要操作
    event MessageUpdated(
        address indexed updater,
        string oldMessage,
        string newMessage,
        uint256 timestamp
    );

    // 构造函数：部署时执行一次
    constructor(string memory _initialMessage) {
        message = _initialMessage;
        owner = msg.sender;
        updateCount = 0;
    }

    // 修饰器：权限控制
    modifier onlyOwner() {
        require(msg.sender == owner, "Not the owner");
        _;
    }

    // 查询函数：不消耗 Gas（view）
    function getMessage() public view returns (string memory) {
        return message;
    }

    // 修改函数：需要支付 Gas
    function updateMessage(string memory _newMessage) public {
        string memory oldMessage = message;
        message = _newMessage;
        updateCount++;

        emit MessageUpdated(msg.sender, oldMessage, _newMessage, block.timestamp);
    }

    // 仅所有者可调用
    function resetMessage() public onlyOwner {
        message = "Reset by owner";
        updateCount++;
    }

    // 转移所有权
    function transferOwnership(address newOwner) public onlyOwner {
        require(newOwner != address(0), "Invalid address");
        owner = newOwner;
    }
}
```

**部署脚本（Hardhat）**：
```typescript
// scripts/deploy.ts
import { ethers } from "hardhat";

async function main() {
  const HelloWorld = await ethers.getContractFactory("HelloWorld");
  const hello = await HelloWorld.deploy("Hello, Blockchain!");

  await hello.waitForDeployment();
  const address = await hello.getAddress();

  console.log("HelloWorld deployed to:", address);
  console.log("Initial message:", await hello.getMessage());
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
```

**运行**：
```bash
# 编译
npx hardhat compile

# 部署到本地网络
npx hardhat run scripts/deploy.ts --network localhost

# 部署到测试网（需配置 hardhat.config.ts）
npx hardhat run scripts/deploy.ts --network sepolia
```

### 1.3 数据类型详解

#### 值类型（Value Types）

**1. 布尔类型**：
```solidity
bool public isActive = true;

// 逻辑运算
bool result = true && false;  // false (与)
bool result = true || false;  // true (或)
bool result = !true;          // false (非)
```

**2. 整数类型**：
```solidity
// 无符号整数（只能为正数）
uint8 public smallNumber;    // 0 to 2^8 - 1 (255)
uint256 public bigNumber;    // 0 to 2^256 - 1
uint public defaultNumber;   // 默认为 uint256

// 有符号整数（可以为负数）
int8 public temperature = -10;
int256 public balance;
int public signedNumber;     // 默认为 int256

// 运算
uint a = 10;
uint b = 3;
uint sum = a + b;       // 13 (加法)
uint diff = a - b;      // 7 (减法)
uint product = a * b;   // 30 (乘法)
uint quotient = a / b;  // 3 (整数除法)
uint remainder = a % b; // 1 (取余)
uint power = a ** 2;    // 100 (幂运算)

// Solidity 0.8.0+ 默认开启溢出检查
// 如需禁用（节省 Gas）：
unchecked {
    uint c = a + b;  // 禁用溢出检查，不会 revert
}
```

**3. 地址类型**：
```solidity
address public owner;
address payable public recipient;  // 可接收 ETH 的地址

// 地址属性
owner.balance;           // 账户余额（单位：wei）
recipient.transfer(1 ether);  // 转账（失败时 revert）
recipient.send(1 ether);      // 转账（返回 bool）
recipient.call{value: 1 ether}("");  // 低级调用

// 地址运算
address(0);              // 零地址
address(this);           // 当前合约地址
address(0x123...);       // 直接指定地址
```

**4. 字节数组**：
```solidity
bytes1 public singleByte = 0xFF;
bytes32 public hash;     // 常用于存储哈希值

// 操作
bytes32 data = 0x1234...;
bytes1 first = data[0];  // 获取第一个字节
uint len = data.length;  // 长度（32）
```

**5. 枚举类型**：
```solidity
enum Status {
    Pending,    // 0
    Active,     // 1
    Completed,  // 2
    Cancelled   // 3
}

Status public currentStatus = Status.Pending;

function setStatus(Status _status) public {
    currentStatus = _status;
}

function isPending() public view returns (bool) {
    return currentStatus == Status.Pending;
}
```

#### 引用类型（Reference Types）

**1. 数组**：
```solidity
// 固定长度数组
uint[5] public fixedArray;

// 动态数组
uint[] public dynamicArray;
address[] public users;

// 数组操作
function arrayOperations() public {
    // 添加元素（仅动态数组）
    users.push(msg.sender);

    // 删除元素（不改变长度，设为默认值）
    delete users[0];

    // 删除最后一个元素（改变长度）
    users.pop();

    // 获取长度
    uint len = users.length;

    // 访问元素
    address first = users[0];
}

// 内存数组（临时）
function createMemoryArray() public pure returns (uint[] memory) {
    uint[] memory arr = new uint[](5);
    arr[0] = 1;
    return arr;
}

// 二维数组
uint[][] public matrix;
```

**2. 结构体（Struct）**：
```solidity
struct User {
    address wallet;
    string name;
    uint256 balance;
    bool isActive;
}

// 存储 映射
mapping(address => User) public users;

// 创建结构体
function createUser(string memory _name) public {
    // 方法1：命名参数
    users[msg.sender] = User({
        wallet: msg.sender,
        name: _name,
        balance: 0,
        isActive: true
    });

    // 方法2：位置参数
    User storage user = users[msg.sender];
    user.wallet = msg.sender;
    user.name = _name;
    user.balance = 0;
    user.isActive = true;
}

// 嵌套结构体
struct Order {
    uint256 id;
    User buyer;
    uint256 amount;
}
```

**3. 映射（Mapping）**：
```solidity
// 简单 映射
mapping(address => uint256) public balances;
mapping(uint256 => string) public names;

// 嵌套 映射
mapping(address => mapping(address => uint256)) public allowances;

// 映射操作
function mappingOperations() public {
    // 设置
    balances[msg.sender] = 100;

    // 获取（不存在时返回默认值0）
    uint256 balance = balances[msg.sender];

    // 删除（恢复默认值）
    delete balances[msg.sender];

    // 注意：无法遍历 映射，需配合数组
}

// 配合数组实现枚举
address[] public userList;
mapping(address => bool) public userExists;

function addUser(address _user) public {
    if (!userExists[_user]) {
        userList.push(_user);
        userExists[_user] = true;
    }
}
```

#### 数据位置（Data Location）

```solidity
contract DataLocation {
    uint[] public storageArray;  // 存储在链上

    function example(uint[] memory memArray) public {
        // storage: 链上存储，持久化，昂贵
        uint[] storage sArray = storageArray;
        sArray.push(1);  // 修改 storageArray

        // memory: 临时内存，函数执行后释放，便宜
        uint[] memory mArray = new uint[](5);
        mArray[0] = 1;  // 仅在内存中

        // calldata: 只读，用于外部函数参数，最便宜
        // memArray 在 0.8.0+ 默认为 calldata
    }

    // 注意：
    // - 状态变量默认 storage
    // - 函数参数默认 memory（结构体/数组/映射/字符串）
    // - 局部变量的引用类型必须显式声明
}
```

### 1.4 函数详解

#### 函数可见性

```solidity
contract Visibility {
    uint private privateVar = 1;
    uint internal internalVar = 2;
    uint public publicVar = 3;

    // private: 仅当前合约
    function privateFunc() private pure returns (uint) {
        return 1;
    }

    // internal: 当前合约及继承合约
    function internalFunc() internal pure returns (uint) {
        return 2;
    }

    // public: 所有人可调用（内外部）
    function publicFunc() public pure returns (uint) {
        return 3;
    }

    // external: 仅外部可调用（内部需用 this.）
    function externalFunc() external pure returns (uint) {
        return 4;
    }

    function test() public view {
        privateFunc();      // ✓
        internalFunc();     // ✓
        publicFunc();       // ✓
        this.externalFunc(); // ✓（需要 this）
    }
}
```

#### 函数修饰符

```solidity
contract FunctionModifiers {
    address public owner;
    uint public value;

    constructor() {
        owner = msg.sender;
    }

    // pure: 不读不写状态
    function add(uint a, uint b) public pure returns (uint) {
        return a + b;
    }

    // view: 只读状态，不修改
    function getValue() public view returns (uint) {
        return value;
    }

    // payable: 可接收 ETH
    function deposit() public payable {
        value += msg.value;
    }

    // 普通函数: 可修改状态
    function setValue(uint _value) public {
        value = _value;
    }
}
```

#### 自定义修饰器

```solidity
contract Modifiers {
    address public owner;
    bool public paused;

    mapping(address => bool) public whitelist;

    constructor() {
        owner = msg.sender;
    }

    // 权限控制
    modifier onlyOwner() {
        require(msg.sender == owner, "Not owner");
        _;  // 占位符，表示被修饰函数的代码
    }

    // 状态检查
    modifier whenNotPaused() {
        require(!paused, "Contract is paused");
        _;
    }

    // 条件判断
    modifier onlyWhitelisted() {
        require(whitelist[msg.sender], "Not whitelisted");
        _;
    }

    // 组合多个修饰器（从左到右执行）
    function sensitiveOperation()
        public
        onlyOwner
        whenNotPaused
    {
        // 函数逻辑
    }

    // 带参数的修饰器
    modifier minAmount(uint _min) {
        require(msg.value >= _min, "Insufficient amount");
        _;
    }

    function purchase() public payable minAmount(0.1 ether) {
        // 购买逻辑
    }

    // 修饰器可以有多个占位符
    modifier validAddress(address _addr) {
        require(_addr != address(0), "Invalid address");
        _;
    }
}
```

### 1.5 继承与接口

#### 合约继承

```solidity
// 父合约
contract Parent {
    uint public parentValue;

    function setParentValue(uint _value) public virtual {
        parentValue = _value;
    }
}

// 子合约
contract Child is Parent {
    uint public childValue;

    // 重写父合约函数
    function setParentValue(uint _value) public override {
        parentValue = _value * 2;
    }

    function setChildValue(uint _value) public {
        childValue = _value;
    }
}

// 多重继承
contract A {
    function foo() public virtual pure returns (string memory) {
        return "A";
    }
}

contract B is A {
    function foo() public virtual override pure returns (string memory) {
        return "B";
    }
}

contract C is A, B {
    // 必须重写所有父合约，不然编译报错
    function foo() public override(A, B) pure returns (string memory) {
        return super.foo();  // 调用 B.foo()
    }
}
```

#### 抽象合约

```solidity
// 包含未实现函数的合约
abstract contract AbstractContract {
    // 抽象函数（无实现）
    function getValue() public virtual returns (uint);

    // 普通已实现函数
    function calculate(uint x) public pure returns (uint) {
        return x * 2;
    }
}

// 子合约必须实现所有抽象函数
contract ConcreteContract is AbstractContract {
    uint private value;

    function getValue() public override returns (uint) {
        return value;
    }
}
```

#### 接口

```solidity
// 接口定义
interface IERC20 {
    function totalSupply() external view returns (uint256);
    function balanceOf(address account) external view returns (uint256);
    function transfer(address to, uint256 amount) external returns (bool);

    event Transfer(address indexed from, address indexed to, uint256 value);
}

// 接口特点：
// 1. 不能包含状态变量
// 2. 不能包含构造函数
// 3. 不能继承非接口合约
// 4. 所有函数必须为 external
// 5. 不能包含函数实现

// 实现接口
contract MyToken is IERC20 {
    mapping(address => uint256) private _balances;
    uint256 private _totalSupply;

    function totalSupply() external view override returns (uint256) {
        return _totalSupply;
    }

    function balanceOf(address account) external view override returns (uint256) {
        return _balances[account];
    }

    function transfer(address to, uint256 amount) external override returns (bool) {
        _balances[msg.sender] -= amount;
        _balances[to] += amount;
        emit Transfer(msg.sender, to, amount);
        return true;
    }
}

// 使用接口调用其他合约
contract TokenUser {
    function useToken(address tokenAddress) public {
        IERC20 token = IERC20(tokenAddress);
        uint256 balance = token.balanceOf(msg.sender);
        // ...
    }
}
```

### 1.6 错误处理

```solidity
contract ErrorHandling {
    address public owner;
    uint public value;

    constructor() {
        owner = msg.sender;
    }

    // 1. require - 条件检查，失败时退还剩余Gas
    function requireExample(uint _value) public {
        require(_value > 0, "Value must be positive");
        require(msg.sender == owner, "Only owner");
        value = _value;
    }

    // 2. assert - 不应该失败的检查，失败时消耗所有Gas
    // （用于检查不变量）
    function assertExample() public {
        uint oldValue = value;
        value += 1;
        assert(value > oldValue);  // 必须为 true，否则是致命错误
    }

    // 3. revert - 主动回滚
    function revertExample(uint _value) public {
        if (_value == 0) {
            revert("Value cannot be zero");
        }
        value = _value;
    }

    // 4. 自定义错误（0.8.4+，节省 Gas）
    error InvalidValue(uint provided, uint minimum);
    error Unauthorized(address caller);

    function customErrorExample(uint _value) public {
        if (_value < 10) {
            revert InvalidValue(_value, 10);
        }
        if (msg.sender != owner) {
            revert Unauthorized(msg.sender);
        }
        value = _value;
    }

    // 5. try/catch - 外部调用错误处理
    function tryCatchExample(address target) public {
        try IERC20(target).transfer(msg.sender, 100) returns (bool success) {
            // 成功处理
            require(success, "Transfer failed");
        } catch Error(string memory reason) {
            // require/revert 错误
            emit LogError(reason);
        } catch Panic(uint errorCode) {
            // assert 错误
            emit LogPanic(errorCode);
        } catch (bytes memory lowLevelData) {
            // 其他错误
            emit LogBytes(lowLevelData);
        }
    }

    event LogError(string reason);
    event LogPanic(uint errorCode);
    event LogBytes(bytes data);
}
```

---

## 模块二：ERC Token 标准

### 2.1 ERC-20：同质化代币

#### 标准接口

```solidity
// IERC20 接口
interface IERC20 {
    // 总供应量
    function totalSupply() external view returns (uint256);

    // 账户余额
    function balanceOf(address account) external view returns (uint256);

    // 转账
    function transfer(address to, uint256 amount) external returns (bool);

    // 查询授权
    function allowance(address owner, address spender) external view returns (uint256);

    // 授权
    function approve(address spender, uint256 amount) external returns (bool);

    // 授权转账
    function transferFrom(address from, address to, uint256 amount) external returns (bool);

    // 事件
    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);
}
```

#### 完整实现

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

contract ERC20Token {
    string public name;
    string public symbol;
    uint8 public decimals;
    uint256 public totalSupply;

    mapping(address => uint256) private _balances;
    mapping(address => mapping(address => uint256)) private _allowances;

    event Transfer(address indexed from, address indexed to, uint256 value);
    event Approval(address indexed owner, address indexed spender, uint256 value);

    constructor(
        string memory _name,
        string memory _symbol,
        uint8 _decimals,
        uint256 _initialSupply
    ) {
        name = _name;
        symbol = _symbol;
        decimals = _decimals;

        _mint(msg.sender, _initialSupply);
    }

    function balanceOf(address account) public view returns (uint256) {
        return _balances[account];
    }

    function transfer(address to, uint256 amount) public returns (bool) {
        address owner = msg.sender;
        _transfer(owner, to, amount);
        return true;
    }

    function allowance(address owner, address spender) public view returns (uint256) {
        return _allowances[owner][spender];
    }

    function approve(address spender, uint256 amount) public returns (bool) {
        address owner = msg.sender;
        _approve(owner, spender, amount);
        return true;
    }

    function transferFrom(address from, address to, uint256 amount) public returns (bool) {
        address spender = msg.sender;
        _spendAllowance(from, spender, amount);
        _transfer(from, to, amount);
        return true;
    }

    // 内部函数
    function _transfer(address from, address to, uint256 amount) internal {
        require(from != address(0), "ERC20: transfer from the zero address");
        require(to != address(0), "ERC20: transfer to the zero address");

        uint256 fromBalance = _balances[from];
        require(fromBalance >= amount, "ERC20: transfer amount exceeds balance");

        unchecked {
            _balances[from] = fromBalance - amount;
            _balances[to] += amount;
        }

        emit Transfer(from, to, amount);
    }

    function _mint(address account, uint256 amount) internal {
        require(account != address(0), "ERC20: mint to the zero address");

        totalSupply += amount;
        unchecked {
            _balances[account] += amount;
        }

        emit Transfer(address(0), account, amount);
    }

    function _burn(address account, uint256 amount) internal {
        require(account != address(0), "ERC20: burn from the zero address");

        uint256 accountBalance = _balances[account];
        require(accountBalance >= amount, "ERC20: burn amount exceeds balance");

        unchecked {
            _balances[account] = accountBalance - amount;
            totalSupply -= amount;
        }

        emit Transfer(account, address(0), amount);
    }

    function _approve(address owner, address spender, uint256 amount) internal {
        require(owner != address(0), "ERC20: approve from the zero address");
        require(spender != address(0), "ERC20: approve to the zero address");

        _allowances[owner][spender] = amount;
        emit Approval(owner, spender, amount);
    }

    function _spendAllowance(address owner, address spender, uint256 amount) internal {
        uint256 currentAllowance = allowance(owner, spender);
        if (currentAllowance != type(uint256).max) {
            require(currentAllowance >= amount, "ERC20: insufficient allowance");
            unchecked {
                _approve(owner, spender, currentAllowance - amount);
            }
        }
    }
}
```

#### 扩展功能

```solidity
// 可铸造（Mintable）
contract MintableERC20 is ERC20Token {
    address public owner;

    constructor() ERC20Token("Mintable Token", "MTK", 18, 0) {
        owner = msg.sender;
    }

    modifier onlyOwner() {
        require(msg.sender == owner, "Not owner");
        _;
    }

    function mint(address to, uint256 amount) public onlyOwner {
        _mint(to, amount);
    }
}

// 可销毁（Burnable）
contract BurnableERC20 is ERC20Token {
    function burn(uint256 amount) public {
        _burn(msg.sender, amount);
    }

    function burnFrom(address account, uint256 amount) public {
        _spendAllowance(account, msg.sender, amount);
        _burn(account, amount);
    }
}

// 可暂停（Pausable）
contract PausableERC20 is ERC20Token {
    bool public paused;
    address public owner;

    modifier whenNotPaused() {
        require(!paused, "Token is paused");
        _;
    }

    function pause() public onlyOwner {
        paused = true;
    }

    function unpause() public onlyOwner {
        paused = false;
    }

    function transfer(address to, uint256 amount)
        public
        override
        whenNotPaused
        returns (bool)
    {
        return super.transfer(to, amount);
    }
}
```

### 2.2 ERC-721：非同质化代币（NFT）

#### 标准接口

```solidity
interface IERC721 {
    // 账户余额（拥有的 NFT 数量）
    function balanceOf(address owner) external view returns (uint256);

    // 获取所有者
    function ownerOf(uint256 tokenId) external view returns (address);

    // 安全转账
    function safeTransferFrom(address from, address to, uint256 tokenId) external;
    function safeTransferFrom(address from, address to, uint256 tokenId, bytes calldata data) external;

    // 普通转账
    function transferFrom(address from, address to, uint256 tokenId) external;

    // 授权
    function approve(address to, uint256 tokenId) external;
    function setApprovalForAll(address operator, bool approved) external;

    // 查询授权
    function getApproved(uint256 tokenId) external view returns (address);
    function isApprovedForAll(address owner, address operator) external view returns (bool);

    // 事件
    event Transfer(address indexed from, address indexed to, uint256 indexed tokenId);
    event Approval(address indexed owner, address indexed approved, uint256 indexed tokenId);
    event ApprovalForAll(address indexed owner, address indexed operator, bool approved);
}

// 元数据扩展
interface IERC721Metadata {
    function name() external view returns (string memory);
    function symbol() external view returns (string memory);
    function tokenURI(uint256 tokenId) external view returns (string memory);
}
```

#### 简化实现

```solidity
contract ERC721Token {
    string public name;
    string public symbol;

    // tokenId => owner
    mapping(uint256 => address) private _owners;

    // owner => token count
    mapping(address => uint256) private _balances;

    // tokenId => approved address
    mapping(uint256 => address) private _tokenApprovals;

    // owner => operator => approved
    mapping(address => mapping(address => bool)) private _operatorApprovals;

    event Transfer(address indexed from, address indexed to, uint256 indexed tokenId);
    event Approval(address indexed owner, address indexed approved, uint256 indexed tokenId);
    event ApprovalForAll(address indexed owner, address indexed operator, bool approved);

    constructor(string memory _name, string memory _symbol) {
        name = _name;
        symbol = _symbol;
    }

    function balanceOf(address owner) public view returns (uint256) {
        require(owner != address(0), "Invalid owner");
        return _balances[owner];
    }

    function ownerOf(uint256 tokenId) public view returns (address) {
        address owner = _owners[tokenId];
        require(owner != address(0), "Token doesn't exist");
        return owner;
    }

    function approve(address to, uint256 tokenId) public {
        address owner = ownerOf(tokenId);
        require(to != owner, "Approval to current owner");
        require(
            msg.sender == owner || isApprovedForAll(owner, msg.sender),
            "Not authorized"
        );

        _tokenApprovals[tokenId] = to;
        emit Approval(owner, to, tokenId);
    }

    function getApproved(uint256 tokenId) public view returns (address) {
        require(_owners[tokenId] != address(0), "Token doesn't exist");
        return _tokenApprovals[tokenId];
    }

    function setApprovalForAll(address operator, bool approved) public {
        require(operator != msg.sender, "Approve to caller");
        _operatorApprovals[msg.sender][operator] = approved;
        emit ApprovalForAll(msg.sender, operator, approved);
    }

    function isApprovedForAll(address owner, address operator) public view returns (bool) {
        return _operatorApprovals[owner][operator];
    }

    function transferFrom(address from, address to, uint256 tokenId) public {
        require(_isApprovedOrOwner(msg.sender, tokenId), "Not authorized");
        _transfer(from, to, tokenId);
    }

    // 内部函数
    function _transfer(address from, address to, uint256 tokenId) internal {
        require(ownerOf(tokenId) == from, "Transfer from incorrect owner");
        require(to != address(0), "Transfer to zero address");

        // 先删除授权
        delete _tokenApprovals[tokenId];

        unchecked {
            _balances[from] -= 1;
            _balances[to] += 1;
        }
        _owners[tokenId] = to;

        emit Transfer(from, to, tokenId);
    }

    function _mint(address to, uint256 tokenId) internal {
        require(to != address(0), "Mint to zero address");
        require(_owners[tokenId] == address(0), "Token already minted");

        unchecked {
            _balances[to] += 1;
        }
        _owners[tokenId] = to;

        emit Transfer(address(0), to, tokenId);
    }

    function _burn(uint256 tokenId) internal {
        address owner = ownerOf(tokenId);

        delete _tokenApprovals[tokenId];

        unchecked {
            _balances[owner] -= 1;
        }
        delete _owners[tokenId];

        emit Transfer(owner, address(0), tokenId);
    }

    function _isApprovedOrOwner(address spender, uint256 tokenId) internal view returns (bool) {
        address owner = ownerOf(tokenId);
        return (
            spender == owner ||
            isApprovedForAll(owner, spender) ||
            getApproved(tokenId) == spender
        );
    }
}
```

---

## 模块三：测试与安全

### 3.1 单元测试（Hardhat）

```typescript
// test/HelloWorld.test.ts
import { expect } from "chai";
import { ethers } from "hardhat";

describe("HelloWorld", function () {
  let helloWorld: any;
  let owner: any;
  let addr1: any;

  beforeEach(async function () {
    // 部署合约
    const HelloWorld = await ethers.getContractFactory("HelloWorld");
    [owner, addr1] = await ethers.getSigners();
    helloWorld = await HelloWorld.deploy("Initial Message");
    await helloWorld.waitForDeployment();
  });

  describe("Deployment", function () {
    it("Should set the right owner", async function () {
      expect(await helloWorld.owner()).to.equal(owner.address);
    });

    it("Should set the initial message", async function () {
      expect(await helloWorld.getMessage()).to.equal("Initial Message");
    });

    it("Should initialize update count to 0", async function () {
      expect(await helloWorld.updateCount()).to.equal(0);
    });
  });

  describe("Update Message", function () {
    it("Should update message", async function () {
      await helloWorld.updateMessage("New Message");
      expect(await helloWorld.getMessage()).to.equal("New Message");
    });

    it("Should increment update count", async function () {
      await helloWorld.updateMessage("New Message");
      expect(await helloWorld.updateCount()).to.equal(1);
    });

    it("Should emit MessageUpdated event", async function () {
      await expect(helloWorld.updateMessage("New Message"))
        .to.emit(helloWorld, "MessageUpdated")
        .withArgs(owner.address, "Initial Message", "New Message");
    });
  });

  describe("Reset Message", function () {
    it("Should allow owner to reset", async function () {
      await helloWorld.resetMessage();
      expect(await helloWorld.getMessage()).to.equal("Reset by owner");
    });

    it("Should reject non-owner reset", async function () {
      await expect(
        helloWorld.connect(addr1).resetMessage()
      ).to.be.revertedWith("Not the owner");
    });
  });
});

// 运行测试
// npx hardhat test
// npx hardhat test --grep "Should update message"
```

### 3.2 常见安全漏洞

#### 1. 重入攻击（Reentrancy）

```solidity
// ❌ 漏洞代码
contract Vulnerable {
    mapping(address => uint) public balances;

    function withdraw() public {
        uint balance = balances[msg.sender];
        require(balance > 0, "No balance");

        // 先转账，后更新状态（错误！）
        (bool success, ) = msg.sender.call{value: balance}("");
        require(success, "Transfer failed");

        balances[msg.sender] = 0; // 太晚了！
    }
}

// ✅ 修复：Checks-Effects-Interactions 模式
contract Secure {
    mapping(address => uint) public balances;
    bool private locked;

    modifier noReentrancy() {
        require(!locked, "No reentrancy");
        locked = true;
        _;
        locked = false;
    }

    function withdraw() public noReentrancy {
        uint balance = balances[msg.sender];
        require(balance > 0, "No balance");

        // 先更新状态（正确！）
        balances[msg.sender] = 0;

        // 再转账
        (bool success, ) = msg.sender.call{value: balance}("");
        require(success, "Transfer failed");
    }
}
```

#### 2. 整数溢出

```solidity
// Solidity 0.8.0+ 默认开启溢出检查
// 但使用 unchecked 时仍需小心

// ❌ 可能溢出
function unsafeAdd(uint a, uint b) public pure returns (uint) {
    unchecked {
        return a + b; // 可能溢出
    }
}

// ✅ 安全检查
function safeAdd(uint a, uint b) public pure returns (uint) {
    uint c = a + b;
    require(c >= a, "Overflow");
    return c;
}
```

#### 3. 访问控制缺失

```solidity
// ❌ 漏洞代码
contract Vulnerable {
    address public owner;

    function setOwner(address newOwner) public {
        // 任何人都可以调用！
        owner = newOwner;
    }
}

// ✅ 修复
contract Secure {
    address public owner;

    constructor() {
        owner = msg.sender;
    }

    modifier onlyOwner() {
        require(msg.sender == owner, "Not owner");
        _;
    }

    function setOwner(address newOwner) public onlyOwner {
        require(newOwner != address(0), "Invalid address");
        owner = newOwner;
    }
}
```

---

## 总结与检查清单

### 技能检查

**Solidity 基础**：
- [ ] 掌握数据类型（值类型、引用类型）
- [ ] 熟练使用函数（可见性、修饰符）
- [ ] 理解继承、接口、抽象合约
- [ ] 熟练使用修饰器和错误处理

**Token 开发**：
- [ ] 能够实现标准 ERC-20 Token
- [ ] 能够实现标准 ERC-721 NFT
- [ ] 理解 approve + transferFrom 模式
- [ ] 掌握扩展功能（Mintable、Burnable、Pausable）

**测试与安全**：
- [ ] 编写完整的单元测试
- [ ] 识别常见安全漏洞
- [ ] 应用安全编码模式
- [ ] 使用 OpenZeppelin 合约库

### 下一步学习

1. **高级主题**：
   - Gas 优化技巧
   - 代理合约（Proxy Pattern）
   - 可升级合约
   - Layer 2 开发

2. **实战项目**：
   - DeFi 协议（DEX、借贷、Staking）
   - NFT 市场
   - DAO 治理合约
   - 游戏合约

3. **深入研究**：
   - EVM 原理与汇编
   - 形式化验证
   - 安全审计方法
   - MEV 与 Flashbots

---

**文档信息**：
- 版本：v1.0
- 最后更新：2024
- 适用对象：0-5 年经验的开发者
- 建议学习时长：2-3 周（配合实践）

**相关文档**：区块链核心概念.md、Web3-基础与库选择.md、Web3-开发实战.md、Dapp.md